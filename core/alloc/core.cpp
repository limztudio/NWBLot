// limztudio@gmail.com
////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////


#include "core.h"

#include <tbb/scalable_allocator.h>


////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////


NWB_ALLOC_BEGIN


////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////


void* CoreAlloc(usize size){
    return scalable_malloc(size);
}
void* CoreRealloc(void* p, usize size){
    return scalable_realloc(p, size);
}
void* CoreReallocAligned(void* p, usize size, usize align){
    return scalable_aligned_realloc(p, size, align);
}
void* CoreAllocAligned(usize size, usize align){
    return scalable_aligned_malloc(size, align);
}

usize CoreMsize(void* ptr)noexcept{
    return static_cast<usize>(scalable_msize(ptr));
}

void CoreFree(void* ptr)noexcept{
    scalable_free(ptr);
}
void CoreFreeSize(void* ptr, usize size)noexcept{
    static_cast<void>(size);
    scalable_free(ptr);
}
void CoreFreeAligned(void* ptr)noexcept{
    scalable_aligned_free(ptr);
}
void CoreFreeSizeAligned(void* ptr, usize size)noexcept{
    static_cast<void>(size);
    scalable_aligned_free(ptr);
}


////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////


NWB_ALLOC_END


////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////

