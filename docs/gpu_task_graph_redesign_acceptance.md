# GPU task-graph redesign acceptance audit

**Decision:** conditional closeout; **not** strict final architecture acceptance.

**Reviewed baseline:** `c62605d9` (`Fix acceptance policy regressions`), 2026-08-11.

**Follow-up implementation:** physical queue identity, frontier-safe deferred scheduling, graph-bound presentation,
graph-owned per-frame ImGui uploads, opt-in ready-frontier native recording, and opt-in same-class physical queue
routing including explicit cross-family ownership handoffs, actual-device stale packet-recording recreation coverage, qualified dedicated-Transfer recovery-frontier
coverage, graph-owned public setup uploads,
graph-owned deferred mesh-view, scene-light, and scene-shading frame updates, and graph-owned decoded texture-asset
uploads, graph-owned runtime-skinning dispatch packets, graph-owned opaque material draw-stream and sampled-image resource sets, graph-owned alias-free split- and unsplit-AVBOIT regular Occupancy, Extinction, and Accumulation plus CSG-only Occupancy, Extinction, and Accumulation compute-emulation handoffs, exact-two-, three-, four-, or five-draw shared-output regular opaque and unsplit-AVBOIT Occupancy, Extinction, and Accumulation handoffs, and a graph-owned unsplit AVBOIT Integration tail,
graph-owned opaque CSG receiver/cutter/context/interval streams and typed work-region interval clears, graph-owned alias-free opaque CSG interval-sample compute-emulation handoffs, graph-owned transparent CSG interval-producer
streams, graph-owned AVBOIT typed target clears plus transparent occupancy, extinction, and accumulation material/CSG streams and sampled-image resource sets, and
graph-owned AVBOIT final G-buffer state handoffs, graph-owned normal Shadow Visibility hardware/software traversal
entry-state handoffs, graph-owned normal Software Caustics, Hardware Caustics, and Surfel GI typed persistent-initialization clears, descriptor-visible entry-state, and prepared-frame age/free-to-cell-head-clear-to-hash-build-to-Spawn-to-trace-build-args-to-trace-to-resolve-to-upsample-to-consumer handoffs, graph-owned CSG clip-buffer entry-state handoffs, and graph-owned current and lagged deferred bindless-selector, ray-trace material-context
selector, caustic
emission-target, surfel-frame constant, shadow material-context batch and retained hybrid hardware fallback context restore,
software-only and hybrid scene-BVH uploads plus fresh-pair/cache-reuse frozen traversal tables, software-only
per-mesh SW-BVH typed sentinel/callback-boundary setup/build/refit and hybrid per-mesh SW-BVH build/refit,
opaque and healthy hybrid hardware TLAS, and opaque and hybrid hardware BLAS build transactions,
2026-08-13.

The task/resource-graph migration is accepted as the current bounded renderer architecture: graph declaration owns
semantic dependencies, compile-time queue selection, packet construction, and inter-task barrier planning; native
recording remains the ordinary path; and optional command IR is tooling-only. This decision does not certify every
completion criterion in the original redesign proposal. It records the exact work that remains before that proposal
can receive an unconditional final sign-off.

## Accepted scope

- The legacy `FrameExecutionPlan` family is absent; the deferred renderer builds a shared task graph and consumes
  compiler-derived packet ranges.
- Task IDs, resource uses, explicit-cycle rejection, deterministic ordered RAW/WAR/WAW inference, queue selection,
  packet generation, graph state seeds, and barrier/ownership planning are implemented and covered by focused tests.
  Resource-version cycle expression and its resource-derived/mixed diagnostic cases are deferred by
  [`W-CYCLE-001`](#w-cycle-001--resource-derived-and-mixed-cycle-expression).
- Accepted/rejected packet lifecycle is transactional. Native recording is still the default runtime path; command IR
  capture, validation, replay, and the direct-Vulkan `CopyBuffer` lowerer are opt-in tooling.
- Vulkan-only descriptor-buffer bindless rendering remains fail-closed. The obsolete lane-policy RHI compatibility
  facade is removed; direct RHI callers select a physical queue class or exact physical queue ID.
- Large setup uploads have an automatic Transfer-preferred route with Graphics/Compute fallback. When an explicitly
  enabled auxiliary physical queue of the selected class exists, an isolated large setup graph may deliberately
  offload its producer there, then returns its graph-owned readiness bridge to the exact primary physical queue of
  that class before publishing the public handle. Cross-family offload promotes the producer class into the resource
  sharing contract; same-family offload retains exclusive sharing. The external dedicated-Transfer performance proof
  is explicitly deferred by the user for this closeout.
- Representable synchronous `Graphics::setupBuffer` and `Graphics::setupTexture` payloads now declare a compiler-owned
  upload graph: caller bytes are retained as immutable graph blobs, the built-in buffer/texture task publishes the
  graph-visible final state and accepted producer token, and dependent graph-owned consumer-queue readiness packets
  preserve the returned-handle compatibility contract. Every supported public buffer and texture upload now uses that
  graph path. Buffer regions with an unaligned offset or byte size reject before they can form an invalid Vulkan copy;
  generic raw combined depth/stencil payloads reject until an explicit per-aspect upload API exists; and
  `keepInitialState + Unknown` uploads reject because they cannot publish a safe final state. None can enter a native
  fallback.
- Texture assets decode every UASTC mip/slice into immutable batch regions before graph declaration. The batch helper
  retains each region as a graph upload blob, serializes the subresources to one terminal accepted token, declares
  `ShaderResource` as the visible final state, and keeps the automatic Transfer -> Compute -> Graphics route plus
  graph-owned consumer readiness packets. When a compatible same-class auxiliary route is explicitly enabled, the
  first region of a large batch offloads there and direct serial successors retain that exact physical transport; a
  cross-family batch route is accepted only when the already-created texture's sharing mask names the producer
  class, and the terminal graph-owned primary bridge remains the returned-handle boundary. Bindless sampled-image
  allocation/commit follows only after that graph batch accepts.
- Runtime skinning now derives immutable per-mesh dispatch plans before recording. Active joint palettes and one-time
  bindless selectors upload into the same primary-Graphics graph packet as deformation, meshlet-bounds, and optional
  normal-repack compute work; no-active poses similarly merge their three-region rest-to-skinned copy with the
  bounds/repack continuation. The graph-owned compute task consumes selectors as `ConstantBuffer` (automatic
  tracking restores their cross-frame `Common` state), publishes every renderer-visible output as `ShaderResource`,
  and commits selector residency plus dirty-state changes only when the packet accepts. Releasing a pose still forces
  one bind-pose copy/repack.
- In the shared deferred path, ImGui vertex/index payloads and requested font/texture updates are retained as
  immutable graph blobs, lowered through the built-in upload tasks, and made dependencies of the terminal overlay
  or upload-completion packet. Small deltas stay on Graphics; amortizable updates prefer Transfer. The non-renderer
  and custom-callback fallback now routes its requested textures through the same compiler/recorder/transaction
  lifecycle as an isolated graph, including terminal acceptance-only status publication. A safe ordinary fallback
  overlay reuses the frozen draw snapshot as that graph's terminal Graphics task; opaque custom callbacks use a
  separate synchronous standalone graph task. Rejected callback-free work remains pending for a graph retry, while
  rejection after an opaque callback requests device recreation instead of submitting the commands directly.
  Persistent ImGui buffers and textures are created with the Graphics/Compute/Transfer sharing contract their
  graph upload tasks require; large immutable UI uploads may explicitly use an auxiliary same-class transport, while
  the overlay or standalone completion task is explicitly required to resolve to primary Graphics.
- The shared deferred graphics prefix resolves changed mesh-view, scene-light, and scene-shading data before graph
  compilation. Each payload is retained as an immutable graph blob, uploaded through a Graphics-routed built-in
  buffer task, and committed to its CPU mirror only when the matching packet is accepted. These automatic-state
  buffers return to `Common` at packet close; their first declared consumer owns the transition to
  `ConstantBuffer` or `ShaderResource`.
- The deferred G-buffer clear is now an explicit serial bundle of five built-in Graphics clear tasks (albedo,
  normal, world position, depth, and terminal opaque color). The first and terminal task bracket the existing
  deferred-clear timing scope inside their native clear commands, and recording rejects a compiled route unless
  both endpoints share the same Graphics packet; the opaque-color task therefore remains the final owner of the
  later asynchronous handoff.
- A target generation's current deferred bindless selector now follows the same acceptance-safe graph path. Its
  immutable slot payload is retained before declaration and, when not yet resident, a tiny Graphics-routed built-in
  upload precedes and merges into the first Shadow Preparation packet. Its automatic retained state is
  `ConstantBuffer`, matching the descriptor-visible state at every native packet close; the graph's declared
  selector use and packet handoffs therefore need no normal-frame native state bridge. That packet alone commits
  `slotsUploaded` on acceptance, and graph-recorded Deferred Lighting, Composite, and Present use the declared
  selector instead of re-reading or rewriting mutable CPU slot data. The direct helper remains for non-graph
  compatibility callers. Active lagged Lighting now retains its history selector as a separate immutable graph
  blob immediately before Deferred Lighting, pinned and merged into that dedicated Compute packet. Its automatic
  selector still publishes `Common`, then Lighting owns the `ConstantBuffer` transition and its existing external
  prior-history completion wait. The existing Lighting acceptance callback alone commits the history residency bit,
  so rejected packets retry the same graph-owned upload without a stale CPU-side commit.
- Ray-trace material-context selector slots are resolved only after Shadow Visibility preflight has frozen every
  backing buffer and descriptor slot. The immutable 32-byte payload is retained as a graph blob and uploaded to
  `Common` immediately before, and merged into, Shadow Preparation. Shadow Preparation retains the logical
  `ConstantBuffer` write handoff so later Compute trace consumers wait on the accepted first Graphics packet rather
  than an upload frontier. This per-frame selector has no residency bit: packet rejection discards the preflight
  plan and the next graph build resolves and retains a fresh payload. The native direct writer remains only for
  non-graph compatibility callers.
- The normal deferred Shadow Visibility task now declares its descriptor-visible hardware and software traversal
  entry states: sampled G-buffer inputs (including depth as `ShaderResource`), selector and scene constant buffers,
  traversal/material streams, typed TLAS state (lowered through retained backing storage), and its output/scratch UAVs. Its graph callback, first
  soft-shadow geometry downsample, and late transparent software trace static buffer batch no longer reissue those
  entry transitions. On a fully prepared soft-transparent route, opaque production, opaque first wavelet, opaque
  resolve tail, transparent trace, an active-temporal transparent merge, transparent first wavelet, and terminal
  resolve tail are mergeable Compute callbacks in the existing Shadow effects packet; the compiler owns current soft geometry's
  `UnorderedAccess`-to-`ShaderResource` handoff before the first wavelet, the opaque-result-to-trace same-UAV
  handoff, the opaque trace and geometry `UnorderedAccess`-to-`ShaderResource` handoffs into that wavelet, its
  half-result `UnorderedAccess`-to-`ShaderResource` handoff into the tail, the transparent trace output's
  `UnorderedAccess`-to-`ShaderResource` handoff into temporal merge (or directly into first wavelet when temporal
  merge is inactive), the selected temporal history/moment outputs' `UnorderedAccess`-to-`ShaderResource` handoff
  into first wavelet, and that wavelet's half-result handoff into the terminal tail. The split opaque and transparent
  callbacks no longer restate their graph-owned first-wavelet output or tail input/visibility states; transparent
  resolve timing begins in temporal merge when active and the terminal resolve tail retains the timing, acceptance,
  recovery, history-publication, and output-state endpoint. Direct or
  minimal callers retain native setup by default. On every eligible prepared adaptive frame, declaration freezes
  the private lifecycle plan even when neither optional primitive is due. Snapshot frames record a typed stats clear
  before Shadow Visibility and the stats readback after it, while compact frames independently record a typed counter
  clear before visibility in the same Compute/Graphics packet. The diagnostic tick and any readback handoff publish only after that callback records and
  the shared packet accepts; completed readbacks retire before the next graph is prepared. The retained monolithic
  route also records an unconditional typed white all-lit visibility clear immediately before that callback in the
  same packet: producers overwrite it, while a missing producer leaves the established fallback without a
  callback-local transition. Direct compatibility and split-soft fallback clears, other adaptive/hybrid
  compatibility transitions, and later intra-task soft/hybrid UAV fences remain native ownership.
- The normal deferred Software Caustics task now consumes graph-declared descriptor-visible shared inputs: sampled
  world/depth textures, bindless/scene/material/mesh constant buffers, emission and frozen software-BVH traversal
  streams, plus scene/material/light buffers. Its depth use is `ShaderResource`, matching the bindless sampled-image
  shader read; the callback no longer restates that entry batch. The normal graph owns the unconditional black
  irradiance clear, a typed unsigned accumulator reset whenever temporal decay is disabled, and, for a fresh temporal
  target, a typed accumulator-bootstrap clear at the front of the same caustics packet. Both reset mirrors publish
  only after that packet's producer accepts. Once the temporal target is initialized, a route-selected graph-owned
  decay task declares its exact accumulator range as `UnorderedAccess`, starts the existing photon timing interval,
  and merges immediately before the photon producer; the producer receives the compiler-owned same-UAV fence and
  closes that interval. Its first geometry-downsample dispatch also consumes the declared world/depth
  `ShaderResource` and resolve-geometry `UnorderedAccess` states without reasserting them natively. The selected
  photon producer, geometry downsample, resolve-prepare, first-wavelet, second-wavelet, third-wavelet,
  fourth-wavelet, fifth-wavelet, upsample, and resource-free timing-close callbacks are distinct merged callbacks in
  that same caustics packet:
  geometry starts the retained full-resolve timing interval; prepare declares the exact accumulator, geometry cache,
  and parity-selected ping-pong input as `ShaderResource` plus its target as `UnorderedAccess`; first-wavelet reads
  that target as `ShaderResource` while writing the counterpart as `UnorderedAccess`; second-wavelet reads that first
  output as `ShaderResource` while writing back to the parity-selected surface as `UnorderedAccess`; third-wavelet
  reads that second output as `ShaderResource` while writing its counterpart as `UnorderedAccess`; fourth-wavelet
  reads that third output as `ShaderResource` while writing back to the parity-selected surface as
  `UnorderedAccess`; and fifth-wavelet reads that fourth output as `ShaderResource` while writing the fixed upsample
  input as `UnorderedAccess`. The compiler therefore lowers the photon/geometry, all five ping-pong, and the final
  fifth-wavelet-to-upsample in-packet `UnorderedAccess`-to-`ShaderResource` handoffs before consuming callbacks
  record; the resource-free timing-close callback then closes the retained interval. When no photon producer records,
  geometry, prepare, all five wavelets, and upsample emit no dispatch while timing-close discards its pending measure,
  and the graph-owned black irradiance clear remains final. Packet-identity validation retains the established timing
  and acceptance endpoint. Direct callers retain native setup and the complete direct resolve/upsample compatibility
  path.
  The normal graph also no longer repeats unchanged world/depth sampled-state setup across each wavelet pass.
- The normal deferred Surfel GI path now consumes graph-declared descriptor-visible entry states for its sampled
  world/normal textures, bindless/material/surfel/scene constants, frozen trace inputs and snapshots, lights,
  persistent pool/cell/counter/argument/free-list buffers, and half-resolution output. On a prepared frame with all
  required GI pipelines, a mergeable `Surfel GI Age Free` task consumes surfel constants as `ConstantBuffer` and the
  pool/counter/free-list as `UnorderedAccess`; a following graph-owned typed cell-head clear writes
  `NWB_SURFEL_CELL_INVALID` as `CopyDest`; a mergeable `Surfel GI Hash Build` task then reads surfel constants and
  reads/writes the pool and cell heads as `UnorderedAccess`; a following mergeable `Surfel GI Spawn` task reads the
  world/normal textures and surfel constants, and reads/writes the pool, cell heads, counter, and free list as
  `UnorderedAccess`; a following mergeable `Surfel GI Trace Build Args` task reads surfel constants and the counter
  as `ConstantBuffer` and `UnorderedAccess`, writes trace arguments as `UnorderedAccess`, and consumes Spawn's
  compiler-lowered counter `UnorderedAccess`-to-`UnorderedAccess` handoff; a following mergeable `Surfel GI Trace`
  task consumes the compiler-lowered trace-arguments `UnorderedAccess`-to-`IndirectArgument` handoff, its
  descriptor-visible trace inputs, and the live pool as `UnorderedAccess`; a following mergeable `Surfel GI Resolve`
  task consumes compiler-lowered live-pool and cell-head `UnorderedAccess`-to-`ShaderResource` handoffs plus its
  declared world/normal `ShaderResource` and surfel-constant `ConstantBuffer` inputs, then writes half-resolution
  irradiance as `UnorderedAccess`; and the final Upsample callback consumes the compiler-lowered half-irradiance
  `UnorderedAccess`-to-`ShaderResource` handoff and writes full-resolution irradiance as `UnorderedAccess`. The
  prepared graph leaves that full-resolution output in `UnorderedAccess` for its exact downstream consumer: live
  Lighting lowers it to `ShaderResource`, while active lagged-history copy lowers the current output to `CopySource`
  (bootstrap capture passes through Lighting's `ShaderResource` read first). The irradiance clear, age/free task,
  cell-head clear, hash-build task, Spawn task, trace-build-args task, Trace task, Resolve task, and Upsample callback
  must share the existing semantic GI packet, preserving its effects acceptance endpoint and, on Compute, its single
  async timing interval; the return transition is attributed to the consuming packet prologue. The
  fallback/compatibility route retains the complete native age/free, cell-head-clear, hash-build, Spawn,
  trace-build-args, Trace, Resolve, and Upsample sequence plus its native full-resolution irradiance
  `ShaderResource` exit; direct callers also retain native setup, while per-pass UAV ordering remains task-local.
- First-use normal Surfel GI initialization now records four ordered typed `CopyDest` clears—pool `0`, cell heads
  `NWB_SURFEL_CELL_INVALID`, counter `0`, and free list `0`—rather than a native clear callback. A resource-free
  lifecycle tail marks the existing CPU mirror pending only after every clear records, publishes initialization only
  after their shared packet accepts, and restores the retry state on rejection. The first clear remains the semantic
  preparation/state-range anchor; its explicit immediate successors retain that accepted Compute-preferred packet
  even when the later pool/cell snapshot routes to a dedicated Transfer queue. Direct compatibility callers retain
  the native setup helper and use the same lifecycle publication methods.
- The normal deferred Hardware Caustics task now consumes graph-declared descriptor-visible static producer inputs:
  sampled world/depth, hardware mesh attributes, material/instance/emission/light streams, and mesh-view,
  bindless/material-context/scene constants. Its callback no longer restates that entry batch. The normal graph
  owns the unconditional black irradiance clear, a typed unsigned accumulator reset whenever temporal decay is
  disabled, and, for a fresh temporal target, a typed accumulator-bootstrap clear at the front of the same caustics
  packet. Both reset mirrors publish only after that packet's producer accepts. Once the temporal target is
  initialized, a graph-owned Graphics decay task declares the exact accumulator range as `UnorderedAccess`, starts
  the existing photon timing interval, and merges immediately before the hardware photon producer; the producer
  receives the compiler-owned same-UAV fence and closes that interval. Its first geometry-downsample dispatch also
  consumes the declared world/depth `ShaderResource` and resolve-geometry `UnorderedAccess` states without
  reasserting them natively. The selected photon producer, geometry downsample, resolve-prepare, first-wavelet,
  second-wavelet, third-wavelet, fourth-wavelet, fifth-wavelet, upsample, and resource-free timing-close callbacks are
  distinct merged callbacks in that same caustics packet: geometry starts the retained full-resolve timing interval;
  prepare declares the exact
  accumulator, geometry cache, and parity-selected ping-pong input as `ShaderResource` plus its target as
  `UnorderedAccess`; first-wavelet reads that target as `ShaderResource` while writing the counterpart as
  `UnorderedAccess`; second-wavelet reads that first output as `ShaderResource` while writing back to the
  parity-selected surface as `UnorderedAccess`; third-wavelet reads that second output as `ShaderResource` while
  writing its counterpart as `UnorderedAccess`; fourth-wavelet reads that third output as `ShaderResource` while
  writing back to the parity-selected surface as `UnorderedAccess`; and fifth-wavelet reads that fourth output as
  `ShaderResource` while writing the fixed upsample input as `UnorderedAccess`. The compiler therefore lowers the
  photon/geometry, all five ping-pong, and final fifth-wavelet-to-upsample in-packet `UnorderedAccess`-to-
  `ShaderResource` handoffs before consuming callbacks record; the resource-free timing-close callback then closes the
  retained interval. When no photon producer records, geometry, prepare, all five wavelets, and upsample emit no
  dispatch while timing-close discards its pending measure, and the graph-owned black irradiance clear remains final.
  Packet-identity validation retains the established timing and acceptance endpoint. Direct callers retain native setup
  and the complete direct resolve/upsample compatibility path. The normal graph also no longer repeats unchanged
  world/depth sampled-state setup across each wavelet pass.
- Caustic preflight now freezes the exact refractive-instance AABB stream after it has selected buffer capacity and
  descriptor residency. A nonempty immutable blob uploads after the material-context selector and merges into the
  same Shadow Preparation packet, publishing automatic-state `Common`. Shadow Preparation keeps the logical
  `ShaderResource` write handoff so software and hardware caustic consumers wait on that accepted packet rather
  than the upload frontier. Empty preflight snapshots authoritatively skip both the upload and record-time regather;
  rejection discards the preflight snapshot and the next build gathers fresh bytes. The native writer remains only
  for non-graph compatibility callers.
- Surfel GI now freezes its 80-byte per-frame constant payload—frame index, round-robin divisor, pool dimensions,
  and render extent—before graph recording. An active frame retains a tiny immutable `Common` upload after the
  caustic stream and merges it into Shadow Preparation, which owns the logical `ConstantBuffer` handoff to the
  later asynchronous GI work. There is intentionally no residency bit: a rejected packet rebuilds the immutable
  payload from the preserved frame state, while the later Surfel GI path remains responsible for frame progression.
  The direct writer remains only for non-graph compatibility callers.
- Shadow-trace material context now freezes its ABI-coupled instance-material table, instance records, and typed
  material byte stream together before recording. For hardware-selected and naturally software-only routes, a complete
  all-or-nothing immutable triple chains into Shadow Preparation and publishes automatic-state `Common`; Shadow
  Preparation retains the logical `ShaderResource` writes so later Compute trace work waits on the accepted first
  Graphics packet. Every healthy hybrid transparent hardware-to-software frame freezes both a fresh complete
  software-compatible triple and the complete preflight-retained hardware fallback triple before the software plan
  supersedes the hardware plan. Both triples are immutable graph blobs. If the optional software tail cannot record,
  its declared writes restore only the frozen hardware blobs before accepting opaque HW shadows. Missing fallback
  capture fails preflight; a late destination, capacity, descriptor, or source-version mismatch rejects the merged
  packet so discard handling requests a fresh preflight. An accepted pure-software material-cache hit freezes and
  validates its retained triple-buffer identity, counts, and hash without emitting another upload. Accepted scene-
  cache reuse likewise freezes its retained storage identity, while hybrid uses that scene identity alongside its
  fresh software material snapshot. Recording never regathers CPU comparison data or uploads material bytes through
  a native retry. Fresh static cache contents commit only after Shadow Preparation accepts; rejected metadata plans
  invalidate their previously accepted reuse keys.
- The CPU-built software scene-BVH now freezes its node records and leaf-instance stream as one immutable pair on
  the software-only route and the independent hybrid software tail. The node leaf ranges address that exact instance
  stream, so both `Common` uploads chain into Shadow Preparation and are handed off there as `ShaderResource` to the
  later Compute shadow, caustic, and Surfel consumers. Its static-scene cache commits only from the accepted
  preparation packet. Every healthy hybrid preflight retains one distinct-mesh traversal table with owning mesh
  buffers, descriptor slots, byte sizes, primitive counts, runtime versions, and relevant ECS mutation versions.
  A fresh pair retains immutable graph blobs; accepted static-cache reuse instead retains the exact buffer,
  descriptor, capacity, count, and hash identity without emitting another upload. Recording validates that frozen
  plan without rebuilding CPU scene data or repopulating descriptor tables. A stale or missing plan fails the
  optional software tail and restores the declared opaque-HW context. The healthy hybrid TLAS build retains its
  separate immutable instance plan and narrow acceleration-structure build retry boundary.
- Software-only per-mesh SW-BVH builds and refits now freeze their selected mesh inputs, node/parent buffers,
  descriptor slots, bounds, rebuild/refit decision, and the shared sort/payload/counter scratch generation before
  graph compilation. In the pure-software prepared route, the graph now supplies those frozen position/index inputs
  as `ShaderResource` before the recorder runs, including a retained `AccelStructBuildInput` seed from an earlier
  route, so it no longer restates a duplicate native entry bridge. They otherwise record serially inside Shadow
  Preparation; parent links and shared scratch stay in their true `UnorderedAccess` state and join the accepted
  cross-frame state seed. Mesh topology/refit flags commit only after that state handoff accepts. Hybrid frames use
  the frozen plan for their independent per-mesh SW-BVH portion too. Their software continuation now records in a
  serial packet-local `Shadow Preparation Hybrid Software Tail` callback after the hardware build and before the
  accelerator-structure finalizer, but remains in the same accepting first Graphics packet so its opaque fallback,
  timing ticket, and acceptance state remain atomic. When both frozen mesh plans and every position/index import
  are present, Shadow Preparation declares the frozen BLAS inputs as `AccelStructBuildInput` and the tail declares
  every frozen SW-BVH input as `ShaderResource`, so the compiler lowers the shared `AccelStructBuildInput` to
  `ShaderResource` boundary before the SW recorder runs. A missing mesh import or incomplete frozen per-mesh plan
  retains the native acceleration-structure state bridge; if the optional tail cannot record, the valid hardware
  fallback can submit only after its declared material blobs restore successfully. Material-context and scene-BVH
  bytes never take a native retry. Healthy hybrid scene construction freezes independent TLAS and software traversal
  plans, while the same state-only imports preserve UAV producer state across a later route switch.
- Opaque and healthy hybrid hardware TLAS builds now retain exact preflight instance descriptors together with the
  referenced BLAS, selected TLAS, and backing-buffer handles. The frozen native build stays inside the existing
  Shadow Preparation Graphics packet, while the graph supplies its `AccelStructWrite` entry state and an adjacent
  state-only finalizer supplies the descriptor-visible `AccelStructRead` handoff for both the typed TLAS and its
  backing buffer. The finalizer is explicitly retained in the same accepting packet even with direct Compute
  consumers, so static-cache publication and the cross-frame backing-state seed remain atomic. A hybrid capture or
  record-time mismatch discards only the frozen TLAS plan and retries the established direct native write-to-read
  build, preserving the graph-owned software-compatible material context and the valid hardware opaque fallback.
- Hardware BLAS builds and refits now freeze the selected static or runtime mesh operation with its exact
  position/index buffers, BLAS, backing generation, and rebuild/refit decision. The plan records before the frozen
  TLAS in the same Shadow Preparation packet; the graph supplies its typed BLAS/backing `AccelStructWrite` states
  and the shared state-only accelerator-structure finalizer supplies their descriptor-visible `AccelStructRead`
  handoff in that same accepting packet. When no software tail is frozen, the graph verifies each prepared
  position/index pair belongs to the shared trace-geometry import set, deduplicates aliases, and establishes its
  `AccelStructBuildInput` entry state before native BLAS recording; post-G-buffer Normalize returns every selected
  trace geometry input to `ShaderResource`. The same verified frozen-plan collection extends across the hybrid
  packet-local tail: it declares the SW-BVH input reads as `ShaderResource` and owns the intermediate handoff.
  A missing frozen import simply retains the native compatibility bridge; direct/retry callers retain it too. Every
  live BLAS backing plus its position/index pair
  participates in the accepted cross-frame state seed, including a direct fallback after a record-time mesh
  replacement. Hybrid frames now use this independent frozen per-mesh work too: a mismatched or failed frozen record
  discards the plan and retries the established direct loop, preserving the valid opaque-HW fallback. Pending-build
  and refit counters commit only after an accepted frozen handoff; transparent hybrid TLAS scene construction retains
  its direct retry boundary only when its independent frozen plan cannot be used.
- The opaque G-buffer now freezes its material draw ordering and CSG CPU frame payload during graph declaration.
  Its instance and typed-material bytes are retained as immutable graph blobs and uploaded through Graphics-routed
  built-in buffer tasks after deferred clear. The tasks publish the buffers' automatic `Common` close boundary; the
  G-buffer declares the transient `ShaderResource` reads. Its frozen regular/receiver-surface material draws also
  resolve, deduplicate, and declare their prepared `SampledImage2D` texture assets as one immutable
  `ShaderResource` set; opaque CSG interval sampling owns the equivalent set for its frozen CSG draws. CSG
  receiver-range, cutter, clip-context, and interval
  sample bytes from that same frozen payload are immutable graph blobs, uploaded in order before the G-buffer. The
  graph declares the receiver/cutter `ShaderResource` reads and the context/sample plus target-generation deferred
  bindless-slot `ConstantBuffer` reads. Its two persistent CSG interval values (`csgIntervalId` over all peel
  layers and slice-zero `csgReceiverEventCount`) now clear through serial typed rectangular unsigned-integer graph
  primitives immediately before the opaque native producer; their first/last recording hooks preserve the original
  CSG-clear timing interval, then explicitly hand off from `CopyDest` to `UnorderedAccess`. All three peel arrays
  (`csgCapBackNormal`, `csgIntervalDepth`, and `csgIntervalId`) and receiver-surface event images now declare their
  exact `UnorderedAccess` ranges in the opaque G-buffer producer. The following opaque receiver-span callback
  declares the two event reads and two span writes, and the following interval-combine callback declares the four
  removed-interval output ranges before it records. Those producers no longer stage their initial StorageImage
  states natively. The graph owns the two receiver-surface-to-span, two span-to-combine, three peel-to-combine, and
  four combine-to-opaque-material/cap handoffs. Prepared-transparent AVBOIT now records mergeable receiver-span
  and interval-combine callbacks: Span owns the two receiver-event reads and two span writes, while Combine owns
  the three peel plus two span `UnorderedAccess` inputs and four removed-output writes before phase-local occupancy
  uploads may replace the frozen CSG stream. The compiler lowers the two receiver-surface-to-span, three
  peel-to-combine, two span-to-combine, and four combine-to-occupancy UAV handoffs in the established AVBOIT-pre
  packet; its first frozen upload starts that semantic packet so it cannot coalesce with the separately timed and
  accepted Hardware Caustics packet on Graphics-only topology. Direct, unprepared, and empty-work compatibility
  callers retain their aggregate span/combine fences. The normal AVBOIT target-clear sequence is now a serial chain
  of nine typed graph primitives (three black texture clears, five unsigned buffer clears including the overflow
  sentinel, and a white transmittance clear) before occupancy; its first/last texture hooks retain the existing
  `AvboitClear` timing interval and runtime checks keep the chain in AVBOIT Pre's accepted Graphics packet. The
  no native aggregate clear helper remains. Transparent AVBOIT sampling and wider CSG target
  lifecycle remain deliberately outside this bounded task.
  The transparent CSG interval producer now does the same within AVBOIT-pre:
  it freezes its fresh mesh-view work region, receiver-surface draw ordering, material instance/typed data, and CSG
  receiver/cutter/context/sample payloads during graph declaration. Its first serial Graphics upload starts the
  established AVBOIT-pre packet and the remaining uploads merge into it, so that packet consumes the graph-owned
  bytes before later AVBOIT phases overwrite their shared buffers. Its prepared path also uses paired typed
  rectangular-`UInt` graph clears before native interval
  production, while an unprepared compatibility path retains the direct helper. The subsequent transparent
  occupancy phase now freezes its own material instance/typed stream and, when it contains CSG draws, its
  receiver/cutter/clip-context stream. Its serial Graphics uploads run
  after interval generation and merge into that same AVBOIT-pre packet; it deliberately retains the interval
  producer's full-resolution sample-state payload for low-resolution sampling. The prepared native occupancy
  consumer never regathers or rewrites those streams, and the renderer verifies that its task is in the interval
  task's accepted packet. When both prepared CSG interval production and prepared occupancy CSG sampling are
  present, occupancy also declares the four removed-interval aliases as exact-range `UnorderedAccess` reads, so
  the compiler lowers their same-packet UAV handoff before its native thunk records. Direct occupancy retains its
  native sample-state bridge. The following extinction phase freezes a
  separate material instance/typed stream and,
  for CSG draws, receiver/cutter/clip-context data; it intentionally reads rather than rewrites the interval
  sample state. Its prepared CSG consumer also declares the same four exact-range `UnorderedAccess` reads: on
  Graphics-only frames the handoff remains in the AVBOIT-pre packet, and on the split route the compiler preserves
  four state seeds and waits for both the interval-producer and depth-warp packets before the Graphics extinction
  thunk records. Direct and unprepared extinction paths retain the native bridge. On Graphics-only frames its
  upload chain and native post-occupancy tail merge into the same AVBOIT-pre packet; on the split route they merge
  into the existing Graphics extinction packet after depth warp.
  The final accumulation phase freezes its own material instance/typed payload and, for CSG draws,
  receiver/cutter/clip-context payload after integration; it also retains the interval producer's full-resolution
  sample state as a read-only input. When both prepared interval production and prepared accumulation CSG sampling
  are present, accumulation declares the same four exact-range `UnorderedAccess` reads. On the split route their
  source state seeds survive the depth-warp, extinction, and integration packets and add the required producer
  wait before accumulation records; direct and unprepared accumulation paths retain the native bridge. Its serial
  Graphics upload chain and prepared consumer merge into the terminal Graphics AVBOIT packet—after integration on
  the split route and after the Graphics-only post-extinction tail on the normal route. A following mergeable
  Graphics finalizer declares the two accumulation attachments as
  `ShaderResource` before Deferred Composite consumes them, while the read-only depth attachment retains its
  explicit compatibility handoff. Runtime checks require every phase stream and finalizer to share its native
  consumer's accepted packet and keep the AVBOIT range at one or five packets. Direct helpers remain only for
  non-graph compatibility callers. Across opaque G-buffer/interval sampling and prepared transparent interval,
  occupancy, extinction, and accumulation callbacks, those exact graph declarations now also own the CSG
  receiver/cutter `ShaderResource` and clip-context/interval-sample `ConstantBuffer` entry states. The native
  helper remains for direct, unprepared, or empty gathered-work paths; prepared-transparent AVBOIT now exposes its
  receiver-surface-to-span, span/peel-to-combine, and combine-to-occupancy handoffs to the graph. The frozen
  transparent CSG interval producer and each phase-local AVBOIT material plan now also resolve their exact prepared
  `SampledImage2D` assets through the material cache, deduplicate typed texture imports, and declare them as
  phase-local immutable `ShaderResource` sets; incomplete preparation retains the existing native compatibility
  path rather than publishing a partial bindless set.
- The compiler derives stable packet recording-frontier depths from packet dependencies. The native recorder may
  record an explicitly opted-in frontier concurrently with isolated per-packet state scratch and native command
  buffers; submission, timing, and failure reporting remain in stable compiler order. Command-IR capture and
  legacy external-state overrides deliberately retain serial recording.
- A Vulkan device may opt into every additional `VkQueue` from each active selected primary Graphics, dedicated
  Compute, and dedicated Transfer family, plus one deterministic cross-family alternate for each class when that
  separate policy is enabled. The Device registry assigns each a distinct physical-queue identity; only graph tasks
  that explicitly allow same-class routing may use them. The compiler uses deterministic current-cost balancing,
  broad class callers remain on their primary queue, and a same-family handoff uses the graph packet's timeline
  dependency without emitting a queue-family ownership transfer.
- Windowed graph presentation is pinned to the primary physical Graphics transport. The backend rejects a claimed
  terminal signal from every auxiliary Graphics queue, and the deferred renderer rejects a Present or terminal
  overlay that resolves anywhere else. An auxiliary route needs an explicit acquired-image and swap-chain-sharing
  contract before it can become a presentation endpoint.
- Recorded graph and submission-transaction state is generation-bound. Recompiling after graph reset invalidates
  stale recording state until it is reset for the new compiled graph, releasing the old packet-owned command-list
  handles before recording can resume.
- A late recovery packet is an independent Graphics tail that derives one current-generation timeline wait from
  every accepted non-destination physical queue. The transaction-level test covers Compute and Transfer sources;
  the dedicated-Transfer headless smoke accepts a graph-owned Transfer upload, rejects its dependent Graphics
  suffix, then uses the generic accepted-frontier helper to late-record and submit the recovery tail. A test-owned
  `vkQueueSubmit2` observer verifies that the accepted Transfer submission's timeline semaphore and value become the
  recovery submission's exact native wait, with all-command stage scope and device index zero. The dedicated-Transfer half is
  topology-gated and skips only when the adapter lacks a dedicated Transfer-only family.

## Verification performed

| Check | Result |
| --- | --- |
| Serial rebuild of graph, descriptor-buffer smoke, Transfer-profile, and command-IR-profile targets | passed |
| Recompile of `impl/ecs_render/kernel/system.cpp` | passed |
| `ctest -C dbg -R '^(nwb_graphics_task_graph_tests|nwb_descriptor_buffer_tests)$'` | 2/2 passed |
| `graphics_task_graph_tests` | 37/37 passed |
| `descriptor_buffer_tests` | 67 passed; 9 expected skips because this host has no dedicated Compute-only or Transfer-only family |
| Project policy checks (`return_value_handling`, test `std::` use, interop containers) | passed |
| Command-IR and Transfer A/B launcher/runner self-tests | passed |
| Immutable renderer-baseline launcher/runner self-tests | passed; the test-owned `python launcher.py renderer-baseline <profile>` workflow captures a source-revisioned BMP, immutable checksum manifest, frozen scene settings, and validation log for opaque textured, AVBOIT, static/skinned CSG, soft-shadow, caustic, surfel-GI, and stress scenes. Transparent AVBOIT, animated skinned CSG, and the stress scene are frame-locked at 96 completed submissions, while temporal soft shadows, caustics, and Surfel GI use a 360-frame warm-up, all through smoke-only controls with a fixed `1/60` simulation delta; each capture waits for the renderer to suspend new submissions before taking the image. The stress control leaves its M4 async-lighting capture contract unchanged. A later candidate can compare only against a matching profile/environment/capture mode/fixed delta/settle-duration/validation-mode baseline and writes an amplified diff plus threshold report. No target-scene baseline artifact is claimed by this infrastructure-only check alone. |
| Formal immutable current-renderer corpus `current-renderer-v1`: AMD BC-250 (RADV GFX1013), Vulkan validation | [`current_renderer_corpus.json`](../tests/ab/renderer_baseline/current_renderer_corpus.json) now cryptographically pins the ignored artifact tree under `.cozter/out/ab-results/renderer-baseline/`: opaque-texture `20260818_085522` → `085535` (mean abs 0.181, max 17); static-csg `085551` → `085604` (0.067, 192); transparent-avboit `082350` → `082401` (0.012, 4); skinned-csg `082312` → `082325` (0.207, 74); soft-shadows `084125` → `084153` (0.229, 81); caustics `082923` → `082944` (0.028, 2); surfel-gi `083523` → `083545` (0.071, 8); stress `085339` → `085413` (0.036, 4). The test-owned runner verifies every baseline BMP, manifest, and runtime log before capture, applies conservative recorded limits, and rejects any attempt to relax them. This is the formal forward pixel reference for current graph-renderer output on the qualified adapter; it is explicitly not a legacy-to-graph parity verdict. |
| Async-render lifecycle evidence: frame-lagged and M4 harnesses | passed with topology-gated execution; their parser/launcher self-tests pass, and freshly rebuilt Vulkan runs on AMD BC-250 (RADV GFX1013) each return the intentional `77` skip because no dedicated Compute-only family exists. This confirms that neither workflow silently treats the Graphics fallback as asynchronous coverage; it does not replace the still-required target-GPU timing/pixel evidence or change the default-disabled rollout policy. |
| Command-IR profile, 4,096 records, Vulkan validation | passed; capture remained allocation-free and direct `CopyBuffer` replay was 3.63% faster than `Core::CommandList` replay |
| Physical queue identity follow-up: graph unit binary | 37/37 passed |
| Physical queue identity follow-up: descriptor-buffer smoke binary | 68 passed; 9 expected skips because this host has no dedicated Compute-only or Transfer-only family |
| Retired-device submission token | verified: graph bindings reject a stale generation, and Vulkan rejects it at the native submission boundary |
| Graph-owned ImGui setup-upload follow-up: `graphics_task_graph_tests` | 42/42 passed, including small Graphics-retained and large Transfer-preferred terminal upload routing |
| Graph-owned ImGui setup-upload follow-up: `descriptor_buffer_tests` | 71 passed; 9 expected skips because this host has no dedicated Compute-only or Transfer-only family |
| Ready-frontier recording follow-up: graph-unit binary | 43/43 passed, including deterministic compiler-derived frontier depth |
| Ready-frontier recording follow-up: descriptor-buffer smoke binary | 72 passed; 9 expected skips because this host has no dedicated Compute-only or Transfer-only family; two independent immutable uploads recorded through a worker frontier and submitted in compiler order |
| Ready-frontier recording follow-up: `nwb_ecs_ui` and `nwb_ecs_render` | passed |
| Worker-sharded native command arena follow-up: graph unit and descriptor-buffer smoke | passed; explicit nonzero ready-frontier worker leases now allocate from one growable Vulkan command-pool shard per physical queue/worker pair, while each tracked buffer remains timeline-retired and reusable only by that worker. Default/direct lease zero keeps a private pool because unregistered compatibility callers may record from unrelated threads. The real Vulkan worker-frontier test holds two Graphics packets on distinct workers concurrently, and the full smoke also covers rejection, garbage collection, and device recreation. `graphics_task_graph_tests` passed 132/132; descriptor smoke passed 150 with 21 expected topology skips. |
| Same-class Graphics routing follow-up: graph-unit binary | 45/45 passed, including deterministic opt-in balancing, same-family state planning, duplicate native queue rejection, and stale recording/transaction recreation |
| Same-class Graphics routing follow-up: descriptor-buffer smoke binary | 72 passed; 10 expected skips. The added real-Vulkan route skipped because this adapter exposes only one Graphics queue; the existing 9 skips lack dedicated Compute-only or Transfer-only families |
| Cross-family same-class Graphics routing follow-up: graph-unit binary | 124/124 passed, including default same-family retention plus deterministic explicit cross-family routing and exclusive Buffer ownership release/acquire planning |
| Cross-family same-class Graphics routing follow-up: descriptor-buffer smoke binary | 147 passed; 17 expected topology skips. The added real-Vulkan route requests a separately enabled alternate Graphics family, verifies compiler release/acquire barriers, records/submits its exact physical timeline dependency, and reads the copied buffer back; this adapter cleanly skips because it exposes no alternate Graphics-capable family |
| Cross-family same-class Compute/Transfer routing follow-up: graph unit and descriptor-buffer smoke | passed; device discovery can now register one alternate dedicated Compute and Transfer family alongside the primary route when both device-level opt-ins are enabled. `ResourceQueueSharing::AsyncCompute` and `Transfer` include the registered auxiliary families, so concurrent resources remain legal while exclusive crossings retain compiler-owned release/acquire barriers. Compiler tests exercise deterministic cross-family Compute and Transfer routing; the real Vulkan registry probe cleanly skips here because this adapter exposes no alternate dedicated family. Graph tests passed 127/127 and descriptor smoke passed 148 with 18 expected topology skips. |
| Multi-auxiliary primary-family registration: graph unit and descriptor-buffer smoke | passed; device creation now requests every valid extra queue index from each active primary Graphics, dedicated Compute, and dedicated Transfer family, carries those exact handles into the physical registry, and retains one deterministic cross-family alternate per class when separately enabled. Four-queue same-family compiler fixtures prove deterministic load distribution across Graphics, dedicated Compute, and dedicated Transfer registries. `graphics_task_graph_tests` passed 132/132; the real Vulkan descriptor smoke passed 150 with 21 expected topology skips, including clean skips because this adapter exposes no additional primary-family queues. |
| Auxiliary same-class public setup-upload adoption: graph unit and descriptor-buffer smoke | passed; large isolated setup graphs now explicitly prefer a compatible auxiliary physical queue on an otherwise-empty queue class, then append a non-routed readiness packet on that class's exact primary physical queue. This preserves the returned-handle contract while making the opt-in transport real; a cross-family Graphics producer declares `ResourceQueueSharing::Graphics` so Vulkan creates concurrent sharing across the two registered Graphics families. The compiler test proves auxiliary producer -> primary bridge routing and dependency. The real Vulkan buffer copy submits a large Graphics upload, observes its auxiliary producer token, then performs an un-waited direct primary Graphics copy; it cleanly skips on this adapter because it has no alternate Graphics transport. Graph tests passed 128/128 and descriptor smoke passed 148 with 19 expected topology skips. |
| Auxiliary same-class texture-batch adoption: graph unit, ECS contract, and descriptor-buffer smoke | passed; an explicit serial-chain affinity now keeps every directly dependent mip/slice task on the same compatible physical queue selected by the first auxiliary producer. This avoids queue ping-pong and preserves merge eligibility. Batch routing refuses a cross-family auxiliary route unless the already-created destination explicitly includes that broad queue class in its sharing mask; same-family exclusive textures remain valid. The terminal primary readiness bridge is retained. The real Vulkan proof uploads a large two-mip batch on an auxiliary Graphics queue, then reads both mips back through an un-waited direct primary Graphics command list; it cleanly skips on this adapter because it exposes no alternate Graphics transport. Graph tests passed 129/129, ECS graphics passed 27/27, and descriptor smoke passed 148 with 20 expected topology skips. |
| Auxiliary same-class ImGui upload adoption: UI build, graph unit, ECS contract, and descriptor-buffer smoke | passed; persistent ImGui vertex/index buffers and dynamically created UI textures now declare `GraphicsAsyncComputeAndTransfer` at creation, matching the graph uploader's existing Transfer/Compute policy. Large immutable UI uploads opt into same-class, cross-family routing; the terminal overlay/standalone completion is explicitly required to resolve to primary Graphics before direct fallback can proceed. Compiler coverage proves that a Graphics-class concurrent resource crosses registered Graphics families with a timeline dependency and state seed but no ownership release/acquire. `nwb_ecs_ui` rebuilt; graph tests passed 130/130, ECS graphics passed 28/28, and descriptor smoke passed 148 with 20 expected topology skips. |
| Primary-Graphics terminal-presentation boundary: swap-chain unit, graph/ECS contracts, and descriptor-buffer smoke | passed; the presentation policy admits only the exact primary physical Graphics queue, rejecting same-family and cross-family auxiliary queues before a binary signal can be queued or accepted. The deferred renderer independently rejects an off-primary scene Present or terminal overlay, so the acquired image remains on the transport named by its existing acquire/share contract. Presentation tests passed 7/7, graph tests 124/124, ECS graphics 26/26, and descriptor smoke passed 147 with 17 expected topology skips. |
| Typed acceleration-structure ownership: graph unit and descriptor-buffer smoke | passed; `AccelStruct` declarations now carry cross-packet state seeds, terminal final-state exports, and exclusive queue-family release/acquire markers through their typed backing allocation inside graph-runtime lowering. A caller no longer needs a second backing-buffer import just to transport state: compiler coverage proves cross-family `AccelStructWrite → AccelStructRead` ownership planning plus external export, while the real Vulkan test imports only a TLAS, forces a packet split, observes the producer state in its consumer, and publishes `AccelStructRead` in the accepted final snapshot. Graph tests passed 126/126 and descriptor smoke passed 148 with 17 expected topology skips. |
| Typed acceleration-structure renderer adoption: renderer build, ECS graphics, graph unit, and descriptor-buffer smoke | passed; Shadow Prepare, Shadow Visibility, hardware Surfel GI, and Hardware Caustics now declare only their retained TLAS/BLAS `AccelStruct` handles. Frozen build plans still retain backing buffers for native lifetime and late-mismatch validation, but no renderer graph resource or resource set duplicates them for state transport. The Shadow Prepare finalizer is a typed TLAS/BLAS set, and its real-Vulkan build/finalize probe imports no backing resource while verifying both backing allocations reach `AccelStructRead`. `nwb_ecs_render` rebuilt, ECS graphics passed 26/26, graph tests passed 126/126, and descriptor smoke passed 148 with 17 expected topology skips. |
| Graph-runtime accepted-frontier recovery helper: renderer build, ECS graphics, graph unit, and descriptor-buffer smoke | passed; `GpuTaskGraphSubmitter::recordAndSubmitAcceptedFrontierTask` now resolves a semantic late recovery/finalization task, validates its compiler-owned accepted-frontier join, records it, submits exact physical-queue waits, and rejects the still-unaccepted task on either failure. Deferred rendering retains only timing arming and device-recreation policy; it no longer rebuilds the recovery range or record/submit/reject sequence. The regular real-Vulkan recovery path executes the helper, and the dedicated-Transfer recovery proof uses the same helper when that topology exists. ECS graphics passed 27/27, graph tests passed 126/126, and descriptor smoke passed 148 with 17 expected topology skips. |
| Graph-runtime late-tail record/submit helper: renderer build, ECS graphics, graph unit, and descriptor-buffer smoke | passed; the shared `recordAndSubmitTask` operation records one semantic late task, offers a pre-submit immutable final-state callback, submits through the graph transaction, and rejects it on any failure. The accepted-frontier recovery helper delegates to it; Surfel counter-readback uses its callback to prepare acceptance-only persistent state, while lagged-history capture receives its producer state from graph declarations and compiler-produced seeds. Renderer code has no remaining direct semantic `recordTaskRangeInCompileOrder`/`submitTaskRangeInCompileOrder` tail pair, per-tail packet-range mirror, or recording-time state binding. ECS graphics passed 27/27, graph tests passed 126/126, and descriptor smoke passed 148 with 17 expected topology skips. |
| Actual device-recreation graph-packet follow-up: descriptor-buffer smoke binary | passed; a real headless Graphics instance releases its only retired-generation native command list before teardown while retaining its CPU-only graph plan, empty recording attempt, submission transaction, queue identity, and completion token. After recreation, the replacement recorder and submitter reject that plan without invoking its tasks or growing the replacement command arena; token validation and arena lookup reject the retired generation. A separately compiled replacement plan then acquires new-generation arena storage and submits a valid replacement token. Descriptor smoke passed Debug 304 with 27 expected skips, Optimize 303 with 28 expected skips, and Final 263 with 30 expected skips. |
| Graph-owned public setup-upload follow-up: descriptor-buffer smoke binary | 73 passed; 10 expected skips. Normal buffer and texture setup routes compile, record, and submit through one graph packet while preserving queue selection, graph-visible final states, and Graphics readiness; the dedicated-Transfer probe is hardware-skipped on this adapter |
| Public texture-upload validation closeout: graph-unit and descriptor-buffer smoke binaries | passed; combined D24S8/D32S8 raw payloads and retained-Unknown setup/batch descriptors now reject before any native submission, leaving every supported public texture upload on the graph-owned path. A future combined depth/stencil upload requires an explicit per-aspect payload/task contract. Graph tests passed 122/122 and the real Vulkan descriptor smoke passed 145 with 16 expected topology skips. |
| Public buffer-upload validation closeout: graph-unit and descriptor-buffer smoke binaries | passed; unaligned setup offsets/sizes and retained-Unknown setup descriptors now reject before any native submission, leaving every supported public buffer upload on the graph-owned path. Graph tests passed 122/122 and the real Vulkan descriptor smoke passed 146 with 16 expected topology skips. |
| Standalone ImGui texture-upload fallback: UI/ECS contracts, graph unit, and descriptor-buffer smoke | passed; no-renderer or custom-callback UI texture requests now declare immutable built-in upload tasks plus a terminal Graphics completion task through `Graphics::submitStandaloneTaskGraph`, instead of opening a native preparation command list. The terminal callback alone publishes ImGui status after every upload packet accepts; a real Vulkan regression rejects one standalone upload transaction, observes its discard, then retries and observes the compiler-owned `ShaderResource` handoff and accepted terminal token. ECS graphics passed 25/25, graph tests passed 122/122, and descriptor smoke passed 147 with 16 expected topology skips. |
| Standalone ImGui presentation fallback: UI/ECS contracts, graph unit, and descriptor-buffer smoke | passed; a no-renderer frame or an abandoned renderer-owned overlay declaration now rebuilds the ordinary frozen vertex/index/command snapshot as an isolated graph with a Graphics terminal task and physical-Graphics verification. Existing UI task declarations accept an omitted predecessor only for this standalone route, preserving their scene-output dependency when one is supplied. Opaque custom callbacks use their own synchronous standalone graph task; rejected callback-free frames remain pending for graph retry and rejected opaque work requests recreation. ECS graphics passed 25/25, graph tests passed 122/122, and descriptor smoke passed 147 with 16 expected topology skips. |
| Graph-owned public setup-upload readiness bridges: ECS graphics contract and descriptor-buffer smoke | passed; the returned-handle compatibility bridge no longer submits a raw zero-command list after graph acceptance. Every selected consumer queue instead receives an explicit dependent no-op graph packet, so compiler-owned physical queue waits establish normal queue order before `setupBuffer`, `setupTexture`, or a texture batch returns. The source upload token remains the public accepted token. ECS graphics passed 26/26 and descriptor smoke passed 147 with 16 expected topology skips; the dedicated-Transfer execution proof cleanly skips on this adapter. |
| Graph-owned deferred-frame upload follow-up: `nwb_ecs_graphics_tests`, `nwb_graphics_task_graph_tests`, and `nwb_descriptor_buffer_tests` | 3/3 passed |
| Retire unreachable deferred scene-shading native uploader: static call-path audit | passed; `RendererDeferredSystem::updateSceneShadingBuffer` had no callers, while the surviving prepare/acceptance APIs feed the graph-owned scene-light and scene-shading upload tasks. The native `writeBuffer` dispatcher and its public declaration are removed so deferred scene updates cannot silently bypass graph acceptance. |
| Retire unreachable AVBOIT/material native upload dispatchers: renderer build, ECS graph contract, graph unit, and descriptor-buffer smoke | passed; every supported transparent AVBOIT phase now records only from its declaration-time immutable snapshot and fails closed before recording when that snapshot is unavailable. The unused aggregate AVBOIT dispatcher plus its native mesh-view, material-instance/material-typed, CSG receiver/cutter/slot, and interval-state upload helpers are removed, so no normal transparent pass can re-gather or write mutable phase data while a graph packet records. Packet-local generated-vertex interleaving and the graph-only ImGui retry/recreation behavior remain unchanged. `nwb_ecs_render` rebuilt; ECS graphics passed 34/34, graph tests 132/132, and descriptor smoke passed 150 with 21 expected topology skips. |
| AVBOIT phase-local immutable upload isolation: descriptor-buffer smoke | passed; a real Vulkan chain uses three distinct Occupancy, Extinction, and Accumulation blobs to overwrite one material-instance-style buffer at the same range. Caller storage is zeroed before recording, each forced serial Graphics packet is accepted and read back before its successor, and every phase retains its own bytes. A rejected capture discards all three accepted-token outputs before the retry. Descriptor smoke passed 151 with 21 expected topology skips. |
| Fail-closed transparent CSG Pre snapshot contract: renderer build, ECS graph contract, graph unit, and descriptor-buffer smoke | passed; AVBOIT Pre now rejects an uploaded-stream/snapshot mismatch instead of accepting an empty interval producer. Declaration captures the Pre, Span, and Combine snapshots before publishing the stream flag, and the source contract fixes the absence of a live `CsgFrameState` fallback. `nwb_ecs_render` rebuilt; ECS graphics passed 41/41, graph tests 132/132, and descriptor smoke passed 151 with 21 expected topology skips. |
| Retire unreachable AVBOIT native target-clear dispatcher: renderer build, ECS graph contract, graph unit, and descriptor-buffer smoke | passed; `clearAvboitTargets` and its aggregate native nine-clear helper had no callers, so both are removed. The ordered typed clear primitives in AVBOIT Pre are the sole target-initialization path, and the AVBOIT compatibility contract now rejects either legacy symbol. `nwb_ecs_render` rebuilt; ECS graphics passed 41/41, graph tests 132/132, and descriptor smoke passed 150 with 21 expected topology skips. |
| Retire unreachable ray-trace material-context native uploader: renderer build, ECS graph contract, graph unit, and descriptor-buffer smoke | passed; Shadow Prepare always receives its preflight-frozen material-context selector from the mandatory graph upload task, so the stale direct selector writer and graph-owned boolean escape hatch are removed. The callback now accepts only the compiler-established immutable `ConstantBuffer` state; at this checkpoint hybrid hardware/software fallback behavior was unchanged, and the later blob-only closure supersedes it. `nwb_ecs_render` rebuilt; ECS graphics passed 35/35, graph tests 132/132, and descriptor smoke passed 150 with 21 expected topology skips. |
| Retire unreachable caustic-emission native uploader: renderer build, ECS graph contract, graph unit, and descriptor-buffer smoke | passed; an active frozen caustic-emission target stream is always retained and uploaded by Shadow Prepare’s graph task, while an empty stream remains an explicit no-caustic frame. The stale direct writer and its boolean escape hatch are removed, so recording cannot replace the declared buffer bytes after compilation; the hybrid fallback at this checkpoint was later strengthened to blob-only restoration. `nwb_ecs_render` rebuilt; ECS graphics passed 36/36, graph tests 132/132, and descriptor smoke passed 150 with 21 expected topology skips. |
| Retire unreachable surfel-frame constant native uploader: renderer build, ECS graph contract, graph unit, and descriptor-buffer smoke | passed; active Surfel GI frame constants are retained as an immutable graph blob before Shadow Prepare records, so the stale direct writer and both Shadow Prepare/hybrid-tail boolean escape hatches are removed. The callbacks now consume only the declared constant-buffer upload; the hybrid hardware fallback itself remains intact. `nwb_ecs_render` rebuilt; ECS graphics passed 37/37, graph tests 132/132, and descriptor smoke passed 150 with 21 expected topology skips. |
| Retire unreachable current deferred-bindless native uploader: renderer build, ECS graph contract, graph unit, and descriptor-buffer smoke | passed; the target-generation selector is either already accepted or introduced by the mandatory `render.deferred.bindless_slots_upload` graph task before its first consumer. Deferred Lighting, Composite, and Present no longer carry a native fallback flag or write path, while Shadow Prepare retains only acceptance-time residency publication. Lagged-history selector handling remains a separate compatibility boundary. `nwb_ecs_render` rebuilt; ECS graphics passed 38/38, graph tests 132/132, and descriptor smoke passed 150 with 21 expected topology skips. |
| Retire unreachable lagged-lighting selector native uploader: renderer build, ECS graph contract, graph unit, and descriptor-buffer smoke | passed; the optional history selector is already accepted or is introduced by `render.lagged_lighting.bindless_slots_upload` as a declared Lighting dependency. The direct writer and renderer-callback flag are removed, while the existing graph scheduling/state-source decision and acceptance-only `slotsUploaded` publication remain unchanged. `nwb_ecs_render` rebuilt; ECS graphics passed 39/39, graph tests 132/132, and descriptor smoke passed 150 with 21 expected topology skips. |
| Retire unreachable bulk shadow-material native uploader: renderer build, ECS graph contract, graph unit, and descriptor-buffer smoke | passed; the declared-but-never-called bulk instance/material writer is removed. The immutable graph batch and the separately verified hybrid hardware-context restore remain the only supported paths; the later blob-only hybrid closure below also retires that path's stale direct-writer boundary. `nwb_ecs_render` rebuilt; ECS graphics passed 40/40, graph tests 132/132, and descriptor smoke passed 150 with 21 expected topology skips. |
| Native renderer write compatibility-boundary audit: renderer build, ECS graph contract, graph unit, and descriptor-buffer smoke | superseded by the blob-only hybrid closure below. At this checkpoint the allowlist still contained six hybrid HW→SW material/scene writes; the current contract fixes it at zero. Adaptive-shadow diagnostics still have no native clear/copy fallback, and ImGui still has no renderer-owned direct submission path. |
| Graph-owned deferred-frame upload follow-up: rebuilt `nwb_testbed` window capture | passed; a real X11/Vulkan deferred frame completed with the graph-owned mesh-view, scene-light, and scene-shading uploads |
| Graph-owned decoded-texture upload follow-up: `nwb_assets_texture_loader`, `nwb_graphics_task_graph_tests`, and `nwb_descriptor_buffer_tests` | passed; the native smoke now reads back two graph-owned mip payloads after their caller arrays are overwritten (74 passed; 10 expected topology skips) |
| Graph-owned skinning-dispatch follow-up: `nwb_ecs_mesh_skinning`, `nwb_ecs_graphics_tests`, graph/descriptor smoke, and runtime skinning smoke | passed; one primary-Graphics graph packet combines selector/palette uploads, rest-stream copies, deform, bounds, normal repack, and acceptance-only CPU commits without a native continuation. 18/18 ECS unit tests, 50/50 graph tests, and descriptor smoke 77 passed with 11 expected topology skips; the one-character animated 339-joint Vulkan fast smoke completed its warm-up and sampled frames. |
| Retire unreachable runtime-skinning native dispatcher: skinning build, ECS graphics, and static call-path audit | passed; the superseded private command-list dispatcher and its direct selector upload, rest-copy, bounds, normal-repack, state-transition, and empty graph-copy-plan scaffolding have been removed. `submitFrameSkinningGraph` is the sole runtime path and resolves its accepted physical submission through the terminal semantic task rather than a stale compiler-packet variable. `nwb_ecs_mesh_skinning` rebuilt and ECS graphics passed 18/18; the module has no remaining native `writeBuffer`, `copyBuffer`, command-list creation, or submit call sites. |
| Declaration-owned imported skinning state follow-up: graph-unit, ECS graphics, and descriptor-buffer smoke | passed; a task now retains its filtered external state source in graph-owned metadata, so runtime skinning attaches its accepted prior-frame handoff to the deformation/bounds declarations instead of selecting a packet-specific native recording override after compilation. The source remains serial by contract; a real late-history Vulkan packet proves normal recording imports the declared state without an override. 81 graph tests, 4 focused ECS graphics tests, and 104 descriptor-buffer tests passed with 11 expected topology skips. |
| Graph-owned opaque material-stream follow-up: `nwb_ecs_graphics_tests`, graph/descriptor smoke, and runtime skinning smoke | passed; 17/17 ECS unit tests, 45/45 graph tests, descriptor smoke 74 passed with 10 expected topology skips, and the one-character Vulkan smoke completed its animated opaque-material case |
| Graph-owned opaque CSG complete-stream follow-up: static/compute opaque capture plus transparent CSG regression captures | 5/5 passed; both opaque Vulkan/X11 paths consumed graph-owned receiver, cutter, context, and interval-state buffers before Opaque G-Buffer, while all three transparent compatibility captures remained correct |
| Graph-owned transparent CSG interval-producer follow-up: ECS graphics unit plus static and skinned transparent CSG early/mid/late captures | 7/7 passed; the frozen receiver-surface payload is uploaded before AVBOIT-pre while the existing one/five-packet AVBOIT contract and visible transparent CSG cuts remain intact |
| Graph-owned transparent AVBOIT occupancy-stream follow-up: ECS graphics, task-graph, descriptor smoke, transparent multi, plus static/skinned transparent CSG captures | 10/10 passed; interval generation, phase-local occupancy uploads, and prepared occupancy remain in one accepted AVBOIT-pre Graphics packet while the visible transparent and CSG cases stay correct |
| Graph-owned transparent AVBOIT extinction-stream follow-up: FrontierSafe task-graph topology, ECS graphics, descriptor smoke, transparent multi, plus static/skinned transparent CSG captures | 10/10 passed; phase-local extinction uploads merge with native extinction without adding a sixth async packet, while Graphics-only and CSG captures remain correct |
| Graph-owned transparent AVBOIT accumulation-stream follow-up: FrontierSafe task-graph topology, ECS graphics, descriptor smoke, transparent multi, plus static/skinned transparent CSG captures | 10/10 passed; phase-local accumulation uploads merge with the final native consumer without adding a sixth async packet, while Graphics-only and transparent CSG captures remain correct |
| Graph-owned current deferred bindless-selector retained-state follow-up: 51 task-graph, 18 ECS graphics, descriptor-buffer smoke, plus opaque and early/mid/late transparent CSG captures | passed; retained `ConstantBuffer` selector state is seeded into the graph packet handoff, removing Shadow Preparation's normal-frame native state bridge while preserving graph-owned upload acceptance |
| Graph-owned post-G-buffer ordinary-state normalization follow-up: 51 task-graph, 18 ECS graphics, descriptor-buffer smoke, plus opaque and early/mid/late transparent CSG captures | passed; compiler-owned declared states now lower the normalizer's ordinary G-buffer, scene, and descriptor transitions; the later trace-geometry follow-up extends that same boundary to route-selected BLAS/SW-BVH buffers. |
| Graph-owned AVBOIT target-clear follow-up: 51 task-graph, 18 ECS graphics, descriptor-buffer smoke, plus opaque and early/mid/late transparent CSG captures | superseded by the typed-clear primitive follow-up below; the original mergeable declaration established the shared Pre packet/timing contract and retained the direct helper for compatibility callers |
| Graph-owned AVBOIT occupancy-state follow-up: 52 task-graph, 18 ECS graphics, descriptor-buffer smoke, plus opaque and early/mid/late transparent CSG captures | passed; occupancy now consumes graph-declared depth `ShaderResource` and coverage `UnorderedAccess` states without a normal-frame native bridge. Focused compiler and real-Vulkan packet tests prove the clear-to-occupancy `CopyDest` to `UnorderedAccess` transition and the separate unsplit-tail UAV dependency; direct compatibility callers retain their bridge |
| Graph-owned AVBOIT accumulation-attachment state follow-up: 53 task-graph, 18 ECS graphics, descriptor-buffer smoke, plus opaque and early/mid/late transparent CSG captures | passed; a mergeable Graphics finalizer now lowers both accumulation attachments from `RenderTarget` to `ShaderResource` before Deferred Composite. Focused compiler and real-Vulkan packet tests prove that the finalizer shares Accumulation's packet and records without a native bridge; direct callers and the read-only-depth compatibility handoff remain intact |
| Graph-owned AVBOIT final G-buffer-state follow-up: 54 task-graph, 18 ECS graphics, descriptor-buffer smoke, lagged-lighting harness, plus opaque and early/mid/late transparent CSG captures | passed; normal graph recording no longer restores AVBOIT G-buffer inputs natively. Accumulation declares its read-only deferred depth as `DepthRead`, and the mergeable Graphics finalizer returns it and both accumulation outputs to `ShaderResource`. The active transparent frame-lagged dedicated-Compute route explicitly waits for that finalizer while its immutable selector upload remains independent; direct compatibility callers retain their native framebuffer-state bridge. |
| Graph-owned Shadow Visibility entry-state follow-up: renderer build, 59 task-graph tests, 18 ECS graphics tests, descriptor-buffer smoke, and GPU-validation hybrid A/B | passed; normal deferred Shadow Visibility now receives descriptor-visible sampled G-buffer inputs (depth specifically transitions `DepthWrite` to `ShaderResource`), constants, traversal resources, typed TLAS state (lowered through its retained backing allocation), and output/scratch UAVs from graph declarations before its callback records. Focused compiler and real-Vulkan getter-only packet tests prove both producer handoffs and native-bridge-free entry. The short GPU-validation A/B passed healthy and forced-fallback arms (six timing intervals each) with no forbidden validation/runtime log. Direct callers, preflight clear fallback, and required intra-task UAV fences remain native. |
| Graph-owned Hardware Caustics entry-state follow-up: renderer build, task-graph unit, descriptor-buffer smoke, and CTest | passed; `nwb_ecs_render` rebuilt, graph tests passed 63/63, descriptor smoke passed 86 with 11 expected topology skips, and CTest passed 2/2. Normal deferred Hardware Caustics now receives descriptor-visible sampled world/depth, hardware mesh attributes, material/instance/emission/light streams, and mesh-view/bindless/material-context/scene constants before its native callback records. Focused compiler and real-Vulkan getter-only packet tests prove that incompatible graphics-prefix states are lowered before bridge-free entry. Direct callers retain native setup, while caustic clears, temporal accumulation, and resolve ping-pong remain local. |
| Graph-owned Surfel GI initialization-state follow-up: renderer build, task-graph unit, descriptor-buffer smoke, and CTest | passed; `nwb_ecs_render` rebuilt, graph tests passed 64/64, descriptor smoke passed 87 with 11 expected topology skips, and CTest passed 2/2. Normal graph initialization now receives persistent pool/cell/counter/free-list `CopyDest` states from compiler prologue barriers before its native clear callback records. Focused compiler and real-Vulkan getter-only packet tests prove all four inherited UAV states lower without a native setup bridge. Direct callers retain native setup; clear contents plus acceptance/discard publication stay task-local. |
| Graph-owned soft-shadow geometry-state follow-up: renderer build, task-graph unit, descriptor-buffer smoke, and CTest | passed; `nwb_ecs_render` rebuilt, graph tests passed 64/64, descriptor smoke passed 87 with 11 expected topology skips, and CTest passed 2/2. The first soft-shadow geometry downsample inherits declared G-buffer/scene-constant reads and its geometry-cache `UnorderedAccess` state; focused compiler and real-Vulkan getter-only packet checks prove the inherited cache state before native work begins. All subsequent trace/temporal/resolve/transparent lifecycle barriers remain native. |
| Graph-owned soft transparent-trace buffer-state follow-up: renderer build, task-graph unit, descriptor-buffer smoke, and CTest | passed; `nwb_ecs_render` rebuilt, graph tests passed 65/65, descriptor smoke passed 88 with 11 expected topology skips, and CTest passed 2/2. The late transparent software trace inherits its software traversal, scene/material/light, and selector/scene-constant buffer states from Shadow Visibility graph declarations; focused compiler and real-Vulkan getter-only packet tests prove the exact state seeds and transitions. The opaque-resolve-to-transparent image/UAV boundary and all temporal/resolve lifecycle barriers remain native. |
| Graph-owned post-G-buffer trace-geometry normalization follow-up: renderer build, task-graph unit, descriptor-buffer smoke, and CTest | passed; `nwb_ecs_render` rebuilt, graph tests passed 66/66, descriptor smoke passed 89 with 11 expected topology skips, and CTest passed 2/2. Shadow Preparation and Post-G-Buffer Normalize now rely on their declared `ShaderResource` trace-geometry uses for all frozen BLAS/SW-BVH buffers. Focused compiler and real-Vulkan getter-only packet tests prove both `VertexBuffer` and `UnorderedAccess` predecessors lower before the normalizer; acceptance-only geometry-state bookkeeping and route-local build/UAV work remain intact. |
| Graph-owned material frame-state follow-up: renderer build, task-graph unit, descriptor-buffer smoke, and CTest | passed; `nwb_ecs_render` rebuilt, graph tests passed 67/67, descriptor smoke passed 90 with 11 expected topology skips, and CTest passed 2/2. Prepared opaque, CSG, and AVBOIT material consumers now inherit their shared mesh-view `ConstantBuffer` plus material instance/typed `ShaderResource` states from graph declarations before their native draw thunks record. Focused compiler and real-Vulkan getter-only packet tests prove all three `Common` predecessors lower without the normal-frame native bridge; dynamically selected per-mesh geometry and intra-pass UAV transitions remain local. |
| Graph-owned material geometry-state follow-up: renderer build, task-graph unit, descriptor-buffer smoke, and CTest | passed; `nwb_ecs_render` rebuilt, graph tests passed 68/68, descriptor smoke passed 91 with 11 expected topology skips, and CTest passed 2/2. Prepared opaque, CSG, and AVBOIT draws now inherit a deduplicated selected-mesh-source `ShaderResource` batch from graph declarations. A physical buffer retained first by Shadow Preparation reuses its original typed graph import; focused compiler and real-Vulkan getter-only tests prove the `Common` predecessor lowers without the native normalizer. Direct/unprepared callers and generated emulation-vertex intra-draw `UnorderedAccess`/`VertexBuffer` transitions remain local. |
| Graph-owned skinning entry-state follow-up: renderer build, descriptor-buffer smoke, and CTest | passed; `nwb_ecs_render` rebuilt, graph tests passed 68/68, descriptor smoke passed 91 with 11 expected topology skips, and CTest passed 2/2. The getter-only real-Vulkan skinning smoke proves packet barriers lower both the immutable selector's `Common` to `ConstantBuffer` and a static input's `Common` to `ShaderResource`; its companion rest-copy smoke proves `CopyDest` to `ShaderResource` before the callback runs. |
| Graph-owned skinning tail-state follow-up: renderer build, descriptor-buffer smoke, and CTest | passed; `nwb_ecs_render` rebuilt, graph tests passed 68/68, descriptor smoke passed 92 with 11 expected topology skips, and CTest passed 2/2. Meshlet-bounds and repacked-attribute outputs now enter the native dispatch as graph-declared `UnorderedAccess` writes, then a mergeable getter-only finalizer lowers them to `ShaderResource` before packet close. The native smoke proves both `Common` to `UnorderedAccess` and `UnorderedAccess` to `ShaderResource` barriers in one primary-Graphics packet; only deformation-stream mid-dispatch handoffs remain local. |
| Graph-owned skinning deformation-handoff follow-up: skinning-module build, task-graph unit, descriptor-buffer smoke, and CTest | passed; `nwb_ecs_mesh_skinning` compiled, graph tests passed 68/68, descriptor smoke passed 93 with 11 expected topology skips, and CTest passed 2/2. Active-pose deformation is now its own mergeable graph task with `UnorderedAccess` position/normal/tangent outputs; bounds/repack consumes the required position/normal streams as `ShaderResource`, and the finalizer publishes tangent, bounds, and attributes. The native Vulkan smoke proves all three stages and their compiler-lowered barriers stay in one primary-Graphics packet without a normal-path native state bridge. |
| Shadow Preparation trace-geometry state-retention follow-up: renderer build, graph/ECS/descriptor smoke, CTest, and skinned-caustic captures | passed; the accepted Shadow Preparation handoff now retains normalized active and temporarily invisible trace-geometry streams. A later graph therefore imports the exact accepted `ShaderResource` state instead of losing it to the underlying buffer's `Common` creation state. Graph tests passed 68/68, ECS graphics passed 18/18, descriptor smoke passed 93 with 11 expected topology skips, CTest passed 3/3, and skinned-caustic early/mid/late Vulkan/X11 captures passed. |
| Graph-owned Surfel GI irradiance-clear follow-up: renderer build, task-graph unit, descriptor-buffer smoke, ECS graphics, CTest, and forced-software GI capture | passed; zero-coverage irradiance is now a typed graph-owned `CopyDest` clear at the front of the existing Surfel GI packet. Surfel GI explicitly merges with it on the selected Compute transport (or the Graphics fallback), preserving the effects endpoint and packet range; renderer validation rejects any split. Graph tests passed 68/68, ECS graphics passed 18/18, descriptor smoke passed 93 with 11 expected topology skips, CTest passed 3/3, and the forced-software GI Vulkan/X11 capture passed. Persistent-cell and in-dispatch UAV/indirect lifecycle barriers remain local. |
| Graph-owned caustic irradiance-clear follow-up: renderer build, task-graph unit, descriptor-buffer smoke, ECS graphics, CTest, and hardware/forced-software caustic captures | passed; both Software and Hardware Caustics now begin their established semantic packets with a typed graph-owned black irradiance `CopyDest` clear and explicitly merge the producer into it. The hardware clear retains the lagged-history external wait at its first write; renderer validation rejects any split. Graph tests passed 68/68, ECS graphics passed 18/18, descriptor smoke passed 93 with 11 expected topology skips, CTest passed 3/3, and both Vulkan/X11 captures passed. The accumulator reset and resolve ping-pong remained local at this stage. |
| Graph-owned caustic accumulator-bootstrap clear follow-up: renderer build, task-graph unit, descriptor-buffer smoke, ECS graphics, CTest, and hardware/forced-software caustic captures | passed; when a fresh temporal accumulator is pending, both selected caustics routes append a typed unsigned zero `CopyDest` clear after irradiance and merge it into the same producer packet. Compiler and native-Vulkan probes prove the accumulator's `CopyDest`-to-`UnorderedAccess` handoff, packet identity, and accepted clear token; renderer validation rejects a split. The producer publishes the initialized mirror only after an accepted dispatch, leaving no-producer, failed-recording, and rejected-submission frames pending for retry. Graph tests passed 68/68, ECS graphics passed 18/18, descriptor smoke passed 93 with 11 expected topology skips, CTest passed 3/3, and both Vulkan/X11 captures passed. The non-temporal reset and resolve ping-pong remained local at this stage. |
| Graph-owned caustic accumulator warm-decay follow-up: renderer build, task-graph unit, descriptor-buffer smoke, ECS graphics, CTest, and GPU-validation hardware/forced-software caustic A/B | passed; once an initialized accumulator has a positive temporal-decay factor, the selected Software or Hardware Caustics route inserts an exact-range `UnorderedAccess` decay task immediately before and merged with its photon producer. The decay starts the existing photon timing interval, the producer receives the compiler-owned UAV-to-UAV fence and closes it, and renderer validation rejects a split packet. Graph tests passed 69/69, ECS graphics passed 18/18, descriptor smoke passed 94 with 11 expected topology skips, and CTest passed 3/3. The getter-only native Vulkan probe proves the `ShaderResource`-to-`UnorderedAccess` entry transition and same-UAV producer fence without a local bridge; the debug GPU-validation paired capture passed with no forbidden runtime or validation log (`.cozter/out/ab-results/bindless-parity/caustics/dbg-validation-investigation/`; max delta 46, mean 0.332155, 13.8806% changed pixels). The non-temporal reset remained local at this stage; resolve ping-pong remains local. |
| Graph-owned caustic accumulator non-temporal-reset follow-up: renderer build, task-graph unit, descriptor-buffer smoke, ECS graphics, CTest, and GPU-validation hardware/forced-software caustic A/B | passed; when temporal decay is disabled, both selected caustics routes append an exact-range typed unsigned zero `CopyDest` clear after irradiance and merge it into the same photon-producer packet. The Software clear locks the selected Compute transport (with Graphics fallback); the Hardware clear stays on Graphics. Compiler planning covers both routes, renderer validation rejects a split, and the producer publishes the reset CPU mirrors only after the containing packet accepts; direct callers retain the legacy native helper. Graph tests passed 70/70, ECS graphics passed 18/18, descriptor smoke passed 95 with 11 expected topology skips, and CTest passed 3/3. The real-Vulkan typed-clear probe proves the `ShaderResource`-to-`CopyDest` clear entry and compiler-owned `CopyDest`-to-`UnorderedAccess` producer handoff without a local bridge. The debug GPU-validation paired capture passed (`.cozter/out/ab-results/bindless-parity/caustics/20260813_050334/`; max delta 20, mean 0.112706, 9.8331% changed pixels). Resolve ping-pong remains local. |
| Graph-owned caustic geometry-downsample entry-state follow-up: renderer build, task-graph unit, descriptor-buffer smoke, ECS graphics, CTest, and GPU-validation hardware/forced-software caustic A/B | passed; normal Software and Hardware Caustics now consume the compiler-lowered world/depth `ShaderResource` and resolve-geometry `UnorderedAccess` states for their first geometry-downsample dispatch, so the shared resolve callback no longer reasserts those three states. Direct callers retain the native setup. Existing compiler and real-Vulkan getter-only coverage verifies the inherited inputs and cache UAV state; graph tests passed 70/70, ECS graphics passed 18/18, descriptor smoke passed 95 with 11 expected topology skips, and CTest passed 3/3. The debug GPU-validation paired capture reported no failures (`.cozter/out/ab-results/bindless-parity/caustics/20260813_051811/`; max delta 46, mean 0.343689, 15.5282% changed pixels). Later accumulator and resolve wavelet/ping-pong transitions remain native task-local ownership. |
| Graph-owned caustic wavelet input-state follow-up: renderer build, task-graph unit, descriptor-buffer smoke, ECS graphics, CTest, and GPU-validation hardware/forced-software caustic A/B | passed; after the graph-owned geometry-downsample entry, normal Software and Hardware Caustics retain its immutable world/depth `ShaderResource` inputs across all wavelet passes instead of reasserting them before each dynamic resolve-buffer handoff. Direct callers preserve the per-pass native transition. Graph tests passed 70/70, ECS graphics passed 18/18, descriptor smoke passed 95 with 11 expected topology skips, and CTest passed 3/3. The debug GPU-validation paired capture reported no failures (`.cozter/out/ab-results/bindless-parity/caustics/20260813_053037/`; max delta 48, mean 0.336892, 15.5470% changed pixels). Accumulator and dynamic geometry/history/half/irradiance resolve transitions, UAV ordering, and timing remain native task-local ownership. |
| Graph-owned caustic photon-resolve handoff follow-up: renderer build, task-graph unit, descriptor-buffer smoke, ECS graphics, CTest, and GPU-validation hardware/forced-software caustic A/B | passed; normal Software and Hardware Caustics now record a photon producer and shared wavelet resolver as distinct merged callbacks in the same selected caustics packet. The resolver declares the exact accumulator range as `ShaderResource`, so the compiler lowers the in-packet `UnorderedAccess`-to-`ShaderResource` barrier; renderer validation rejects a packet split, and a no-producer resolver records no dispatch so the typed black irradiance clear survives. Graph tests passed 71/71, ECS graphics passed 18/18, descriptor smoke passed 96 with 11 expected topology skips, and CTest passed 3/3. The debug GPU-validation paired capture reported no failures (`.cozter/out/ab-results/bindless-parity/caustics/20260813_060010/`; max delta 47, mean 0.323944, 13.1823% changed pixels). Dynamic geometry/history/half/irradiance resolve transitions, UAV ordering, and timing remain native task-local ownership. |
| Graph-owned caustic geometry-resolve handoff follow-up: renderer build, task-graph unit, descriptor-buffer smoke, ECS graphics, CTest, and GPU-validation hardware/forced-software caustic A/B | passed; normal Software and Hardware Caustics now record photon, geometry-downsample, and wavelet callbacks as one selected caustics packet. Geometry declares its cache `UnorderedAccess`, starts the retained full-resolve timing interval, and the wavelet consumer declares that cache plus the exact accumulator range as `ShaderResource`; the compiler lowers both in-packet `UnorderedAccess`-to-`ShaderResource` barriers before wavelet records and closes the interval. Renderer validation rejects a split, while no-producer geometry/wavelet callbacks issue no dispatch and preserve the black irradiance clear. Graph tests passed 71/71, ECS graphics passed 18/18, descriptor smoke passed 96 with 11 expected topology skips, and CTest passed 3/3. The debug GPU-validation paired capture reported no failures (`.cozter/out/ab-results/bindless-parity/caustics/20260813_062457/`; max delta 47, mean 0.321770, 12.3789% changed pixels). Dynamic history/half/irradiance resolve transitions and UAV ordering remain native task-local ownership. |
| Graph-owned caustic prepare-wavelet handoff follow-up: renderer build, task-graph unit, descriptor-buffer smoke, ECS graphics, CTest, and GPU-validation hardware/forced-software caustic A/B | passed; normal Software and Hardware Caustics now record photon, geometry-downsample, resolve-prepare, first-wavelet, and timed-tail callbacks as one selected caustics packet. Prepare declares the parity-selected ping-pong input/output pair and first-wavelet declares the opposite pair, so the compiler lowers the exact first ping-pong `UnorderedAccess`-to-`ShaderResource` barrier before bridge-free wavelet recording; the tail starts with both surfaces in `UnorderedAccess`, preserves the remaining alternating native loop, and closes the retained resolve timing interval. Renderer validation rejects a split, while no-producer prepare/wavelet/tail callbacks issue no dispatch and preserve the black irradiance clear. Graph tests passed 71/71, ECS graphics passed 18/18, descriptor smoke passed 96 with 11 expected topology skips, and CTest passed 3/3. The debug GPU-validation paired capture reported no failures (`.cozter/out/ab-results/bindless-parity/caustics/20260813_071119/`; max delta 47, mean 0.318389, 13.1354% changed pixels). |
| Graph-owned caustic second-wavelet handoff follow-up: renderer build, task-graph unit, descriptor-buffer smoke, ECS graphics, CTest, and GPU-validation hardware/forced-software caustic A/B | passed; normal Software and Hardware Caustics now retain a second graph-owned wavelet callback in the same selected caustics packet. It reads the first wavelet's ping-pong output as `ShaderResource` and writes the opposite parity-selected target as `UnorderedAccess`, so the compiler lowers the exact second in-packet `UnorderedAccess`-to-`ShaderResource` barrier before bridge-free recording. The timed tail begins after both graph-owned wavelets, retains the remaining three alternating native passes plus upsample, and closes the existing resolve interval. Renderer packet validation rejects a split, while no-producer prepare/wavelet/tail callbacks issue no dispatch and preserve the black irradiance clear. Graph tests passed 71/71, ECS graphics passed 18/18, descriptor smoke passed 96 with 11 expected topology skips, and CTest passed 3/3. The debug GPU-validation paired capture reported no failures (`.cozter/out/ab-results/bindless-parity/caustics/20260813_073108/`; max delta 20, mean 0.111340, 9.8191% changed pixels). |
| Graph-owned caustic third-wavelet handoff follow-up: renderer build, task-graph unit, descriptor-buffer smoke, ECS graphics, CTest, and GPU-validation hardware/forced-software caustic A/B | passed; normal Software and Hardware Caustics now retain a third graph-owned wavelet callback in the same selected caustics packet. It reads the second wavelet's ping-pong output as `ShaderResource` and writes the counterpart as `UnorderedAccess`, so the compiler lowers the exact third in-packet `UnorderedAccess`-to-`ShaderResource` barrier before bridge-free recording. The timed tail begins after all three graph-owned wavelets, retains the final two alternating native passes plus upsample, and closes the existing resolve interval. Renderer packet validation rejects a split, while no-producer prepare/wavelet/tail callbacks issue no dispatch and preserve the black irradiance clear. Graph tests passed 71/71, ECS graphics passed 18/18, descriptor smoke passed 96 with 11 expected topology skips, and CTest passed 3/3. The debug GPU-validation paired capture reported no failures (`.cozter/out/ab-results/bindless-parity/caustics/20260813_074829/`; max delta 22, mean 0.139727, 12.8664% changed pixels). |
| Graph-owned caustic fourth-wavelet handoff follow-up: renderer build, task-graph unit, descriptor-buffer smoke, ECS graphics, CTest, and GPU-validation hardware/forced-software caustic A/B | passed; normal Software and Hardware Caustics now retain a fourth graph-owned wavelet callback in the same selected caustics packet. It reads the third wavelet's ping-pong output as `ShaderResource` and writes the parity-selected surface as `UnorderedAccess`, so the compiler lowers the exact fourth in-packet `UnorderedAccess`-to-`ShaderResource` barrier before bridge-free recording. The timed tail begins after all four graph-owned wavelets, retains only the final native wavelet plus upsample, and closes the existing resolve interval. Renderer packet validation rejects a split, while no-producer prepare/wavelet/tail callbacks issue no dispatch and preserve the black irradiance clear. Graph tests passed 71/71, ECS graphics passed 18/18, descriptor smoke passed 96 with 11 expected topology skips, and CTest passed 3/3. The debug GPU-validation paired capture reported no failures (`.cozter/out/ab-results/bindless-parity/caustics/20260813_080319/`; max delta 46, mean 0.309171, 11.3173% changed pixels). |
| Graph-owned caustic fifth-wavelet and upsample handoff follow-up: renderer build, task-graph unit, descriptor-buffer smoke, ECS graphics, CTest, and GPU-validation hardware/forced-software caustic A/B | passed; normal Software and Hardware Caustics now retain the fixed fifth wavelet callback in the same selected caustics packet. It reads the fourth ping-pong output as `ShaderResource` and writes the fixed half-resolution upsample input as `UnorderedAccess`; the following timed tail declares that half-resolution input as `ShaderResource`, so the compiler lowers both the fifth wavelet and final wavelet-to-upsample in-packet `UnorderedAccess`-to-`ShaderResource` barriers before bridge-free recording. The tail now contains only the upsample dispatch and timing close. Renderer packet validation rejects a split, while no-producer wavelet/tail callbacks issue no dispatch and preserve the black irradiance clear. Graph tests passed 71/71, ECS graphics passed 18/18, descriptor smoke passed 96 with 11 expected topology skips, and CTest passed 3/3. The debug GPU-validation paired capture reported no failures (`.cozter/out/ab-results/bindless-parity/caustics/20260813_083102/`; max delta 47, mean 0.327890, 13.8906% changed pixels). |
| Graph-owned caustic resolve-upsample follow-up: renderer build, task-graph unit, descriptor-buffer smoke, ECS graphics, CTest, and GPU-validation hardware/forced-software caustic A/B | passed; normal Software and Hardware Caustics now record their final upsample as its own graph callback after the fixed fifth wavelet. The compiler lowers the exact fifth-wavelet `UnorderedAccess`-to-`ShaderResource` handoff before it records, and a following resource-free callback closes the retained resolve timing interval without a native resource-state bridge. Renderer packet validation preserves one selected caustics packet; no-producer upsample emits no dispatch and the timing callback discards its pending measure, preserving the black irradiance clear. Direct callers retain the full native compatibility path. Graph tests passed 71/71, ECS graphics passed 18/18, descriptor smoke passed 96 with 11 expected topology skips, and CTest passed 3/3. The debug GPU-validation paired capture reported no failures (`.cozter/out/ab-results/bindless-parity/caustics/20260813_084216/`; max delta 47, mean 0.307923, 11.6091% changed pixels). |
| Graph-owned Surfel GI entry-state follow-up: renderer build, task-graph unit, descriptor-buffer smoke, and CTest | passed; `nwb_ecs_render` rebuilt, graph tests passed 62/62, descriptor smoke passed 85 with 11 expected topology skips, and CTest passed 2/2. Normal deferred Surfel GI now receives descriptor-visible G-buffer, selector/context/scene/surfel constants, frozen software traversal/snapshot/light streams, persistent UAV buffers, and half-resolution output from graph declarations before its callback records. Focused compiler and real-Vulkan getter-only packet tests prove that an incompatible prefix state is lowered before the native bridge-free entry. Direct callers retain their native setup while output/cell clears and in-task UAV/indirect transitions stay local. |
| Graph-owned Surfel GI age/free and cell-head-clear follow-up: renderer build, task-graph unit, descriptor-buffer smoke, ECS graphics, CTest, and GPU-validation Surfel A/B | passed; the prepared normal GI route orders Age Free -> typed `NWB_SURFEL_CELL_INVALID` cell-head `CopyDest` clear -> remaining GI in the existing selected Compute packet (or Graphics fallback). The unit test proves the explicit edges, prefix state seeds, same-packet ordering, age/free `CopyDest`-to-`ConstantBuffer`/`UnorderedAccess` transitions, and cell-head `CopyDest`-to-`UnorderedAccess` handoff. The real-Vulkan descriptor-buffer test records the age/free and remaining callbacks without a native bridge, captures the built-in clear, and reads back the invalid cell-head value. Graph tests passed 71/71, descriptor smoke passed 96 with 11 expected topology skips, and ECS graphics passed 18/18. The debug GPU-validation paired capture passed with no forbidden runtime or validation log (`.cozter/out/ab-results/bindless-parity/surfel-gi/20260813_093442/`; report-only max delta 13, mean 0.083822, 17.4918% changed pixels). |
| Graph-owned Surfel GI hash-build handoff follow-up: renderer build, task-graph unit, descriptor-buffer smoke, ECS graphics, CTest, and GPU-validation Surfel A/B | passed; the prepared normal GI route now orders Age Free -> typed `NWB_SURFEL_CELL_INVALID` cell-head `CopyDest` clear -> Hash Build -> remaining GI in the existing selected Compute packet (or Graphics fallback). The focused compiler test proves the explicit edges, five-task packet order, clear-to-Hash-Build `CopyDest`-to-`UnorderedAccess` transition, and Hash-Build-to-Spawn `UnorderedAccess`-to-`UnorderedAccess` barrier. The real-Vulkan getter-only test records Hash Build and the remaining callback without a native bridge, preserves the typed clear capture, and reads back the invalid cell-head value. `nwb_ecs_render` rebuilt; graph tests passed 71/71, descriptor smoke passed 96 with 11 expected topology skips, ECS graphics passed 18/18, and CTest passed 3/3. The debug GPU-validation paired capture passed with no failures or forbidden runtime/validation log (`.cozter/out/ab-results/bindless-parity/surfel-gi/20260813_100530/`; report-only max delta 9, mean 0.094289, 19.9768% changed pixels). |
| Graph-owned Surfel GI Spawn handoff follow-up: renderer build, task-graph unit, descriptor-buffer smoke, ECS graphics, CTest, and GPU-validation Surfel A/B | passed; the prepared normal GI route now orders Age Free -> typed `NWB_SURFEL_CELL_INVALID` cell-head `CopyDest` clear -> Hash Build -> Spawn -> remaining GI in the existing selected Compute packet (or Graphics fallback). The focused compiler test proves the explicit edges, six-task packet order, Hash-Build-to-Spawn `UnorderedAccess`-to-`UnorderedAccess` barrier, and Spawn-to-remaining counter `UnorderedAccess`-to-`UnorderedAccess` handoff. The real-Vulkan getter-only test records Spawn and the remaining callback without a native bridge, preserves the typed clear capture, and reads back the invalid cell-head value. `nwb_ecs_render` rebuilt; graph tests passed 71/71, descriptor smoke passed 96 with 11 expected topology skips, ECS graphics passed 18/18, and CTest passed 3/3. The debug GPU-validation paired capture passed with no failures (`.cozter/out/ab-results/bindless-parity/surfel-gi/20260813_102811/`; report-only max delta 9, mean 0.131935, 24.3476% changed pixels). |
| Graph-owned Surfel GI Trace Build Args handoff follow-up: renderer build, task-graph unit, descriptor-buffer smoke, ECS graphics, CTest, and GPU-validation Surfel A/B | passed; the prepared normal GI route now orders Age Free -> typed `NWB_SURFEL_CELL_INVALID` cell-head `CopyDest` clear -> Hash Build -> Spawn -> Trace Build Args -> remaining GI in the existing selected Compute packet (or Graphics fallback). The focused compiler test proves the explicit edges, seven-task packet order, Spawn-to-Args counter `UnorderedAccess`-to-`UnorderedAccess` barrier, Args `CopyDest`-to-`UnorderedAccess` entry, and Args-to-remaining `UnorderedAccess`-to-`IndirectArgument` handoff. The real-Vulkan getter-only test records Args and the remaining callback without a native bridge, preserves the typed clear capture, and reads back the invalid cell-head value. `nwb_ecs_render` rebuilt; graph tests passed 71/71, descriptor smoke passed 96 with 11 expected topology skips, ECS graphics passed 18/18, and CTest passed 3/3. The debug GPU-validation paired capture passed with no failures (`.cozter/out/ab-results/bindless-parity/surfel-gi/20260813_105839/`; report-only max delta 14, mean 0.070172, 15.9552% changed pixels). |
| Graph-owned Surfel GI Trace handoff follow-up: renderer build, task-graph unit, descriptor-buffer smoke, ECS graphics, CTest, and GPU-validation Surfel A/B | passed; the prepared normal GI route now orders Age Free -> typed `NWB_SURFEL_CELL_INVALID` cell-head `CopyDest` clear -> Hash Build -> Spawn -> Trace Build Args -> Trace -> remaining Resolve/Upsample in the existing selected Compute packet (or Graphics fallback). The focused compiler test proves the explicit edges, eight-task packet order, Args-to-Trace `UnorderedAccess`-to-`IndirectArgument` transition, Trace live-pool `UnorderedAccess` fence, and compiler-owned live-pool/cell-head `UnorderedAccess`-to-`ShaderResource` handoffs to Resolve. The real-Vulkan getter-only test records Trace and the remaining callback without a native bridge, preserving the typed clear capture and invalid cell-head readback. `nwb_ecs_render` rebuilt; graph tests passed 71/71, descriptor smoke passed 96 with 11 expected topology skips, ECS graphics passed 18/18, and CTest passed 3/3. The debug GPU-validation paired capture passed with no failures (`.cozter/out/ab-results/bindless-parity/surfel-gi/20260813_113010/`; report-only max delta 14, mean 0.075144, 17.1809% changed pixels). |
| Graph-owned Surfel GI Resolve-to-Upsample handoff follow-up: renderer build, task-graph unit, descriptor-buffer smoke, ECS graphics, CTest, and GPU-validation Surfel A/B | passed; the prepared normal GI route now orders Age Free -> typed `NWB_SURFEL_CELL_INVALID` cell-head `CopyDest` clear -> Hash Build -> Spawn -> Trace Build Args -> Trace -> Resolve -> Upsample in the existing selected Compute packet (or Graphics fallback). The focused compiler test proves the explicit edges, nine-task packet order, Trace-to-Resolve live-pool and Spawn-to-Resolve cell-head `UnorderedAccess`-to-`ShaderResource` handoffs, Resolve half-irradiance `CopyDest`-to-`UnorderedAccess` entry, and Resolve-to-Upsample half-irradiance `UnorderedAccess`-to-`ShaderResource` handoff. The real-Vulkan getter-only test records Resolve and Upsample without a native bridge, preserving the typed clear capture and invalid cell-head readback. `nwb_ecs_render` rebuilt; graph tests passed 71/71, descriptor smoke passed 96 with 11 expected topology skips, ECS graphics passed 18/18, and CTest passed 3/3. The debug GPU-validation paired capture passed with no failures (`.cozter/out/ab-results/bindless-parity/surfel-gi/20260813_120112/`; report-only max delta 14, mean 0.076239, 16.8878% changed pixels). |
| Graph-owned Surfel GI output-consumer handoff follow-up: renderer build, task-graph unit, descriptor-buffer smoke, ECS graphics, CTest, and GPU-validation Surfel A/B | passed; the prepared normal GI route leaves full-resolution irradiance as `UnorderedAccess` after its unchanged nine-task packet. The compiler now lowers the precise return state in the downstream packet: live Lighting receives `UnorderedAccess`-to-`ShaderResource`, while active lagged-history copy receives the current output as `UnorderedAccess`-to-`CopySource` after Present. The focused compiler tests prove both paths and preserve the existing packet dependency shape; the real-Vulkan getter-only Lighting probe records the `ShaderResource` state without a native bridge. Direct and unprepared compatibility paths retain the native `ShaderResource` exit. The transfer of this barrier to the consumer prologue moves timing attribution but does not add GI work or a packet. `nwb_ecs_render` rebuilt; graph tests passed 71/71, descriptor smoke passed 96 with 11 expected topology skips, ECS graphics passed 18/18, and CTest passed 3/3. The debug GPU-validation paired capture passed with no failures (`.cozter/out/ab-results/bindless-parity/surfel-gi/20260813_122247/`; report-only max delta 9, mean 0.074342, 17.3545% changed pixels). |
| Graph-owned CSG receiver-surface image-state follow-up: 54 task-graph, 18 ECS graphics, descriptor-buffer smoke, plus opaque and early/mid/late transparent CSG captures | passed; opaque G-buffer and prepared-transparent AVBOIT declare all receiver-event-data layers and the receiver-event-count layer as `UnorderedAccess` before their native material thunks record. The normal graph no longer manually transitions that StorageImage pair; unprepared and direct compatibility paths retain their bridge while the remaining CSG image lifecycle stays explicitly out of scope. |
| Graph-owned CSG interval-peel state follow-up: 54 task-graph, 18 ECS graphics, descriptor-buffer smoke, plus opaque and early/mid/late transparent CSG captures | passed; opaque G-buffer and prepared-transparent AVBOIT now declare every cap-back-normal, interval-depth, and interval-ID peel layer as `UnorderedAccess` before their native interval dispatches. The direct first-peel state bridge was removed only for those graph callers; at that stage compatibility paths and the native combine-stage UAV barrier remained intact. |
| Graph-owned CSG receiver-span output state follow-up: 54 task-graph, 18 ECS graphics, descriptor-buffer smoke, plus opaque and early/mid/late transparent CSG captures | passed; opaque G-buffer and prepared-transparent AVBOIT now declare every receiver-span-data layer and the receiver-span-count layer as `UnorderedAccess` before their native span dispatches. The graph removed only output setup; at that stage native event-image and span-to-combine UAV fences remained intact, as did direct compatibility paths. |
| Graph-owned CSG removed-interval output state follow-up: 54 task-graph, 18 ECS graphics, descriptor-buffer smoke, plus opaque and early/mid/late transparent CSG captures | passed; opaque G-buffer and prepared-transparent AVBOIT now declare every removed-interval depth/cap/data layer and the removed-interval-count layer as `UnorderedAccess` before their native combine dispatches. The graph removes only output setup; at that stage native combine-input and post-combine sampling UAV fences remained intact, as did direct compatibility paths. |
| Graph-owned opaque CSG interval-sample state follow-up: 55 task-graph, 18 ECS graphics, descriptor-buffer smoke, plus opaque mesh/compute and early/mid/late transparent CSG captures | passed; opaque interval combine now ends at a mergeable graph boundary, and the following opaque material/cap task declares all removed-interval outputs as exact-range `UnorderedAccess` reads. The compiler lowers four same-packet UAV handoffs before the sample thunk records; AVBOIT and direct compatibility consumers retain their native post-combine bridge. |
| Graph-owned AVBOIT occupancy CSG interval-sample state follow-up: 56 task-graph, 18 ECS graphics, descriptor-buffer smoke, plus static/skinned early/mid/late transparent CSG captures | passed; when both prepared transparent interval production and prepared occupancy CSG sampling are present, the four removed-interval outputs cross the intervening AVBOIT clear through compiler-lowered same-packet UAV handoffs. Direct occupancy paths retain their native sample-state bridge. |
| Graph-owned AVBOIT extinction CSG interval-sample state follow-up: 57 task-graph, 18 ECS graphics, descriptor-buffer smoke, plus static/skinned early/mid/late transparent CSG captures | passed; prepared extinction declares the four removed-interval aliases as exact-range `UnorderedAccess` reads. The compiler preserves four state seeds across the split depth-warp packet and waits for both it and the interval producer; direct/unprepared extinction paths retain their native bridge. |
| Graph-owned AVBOIT accumulation CSG interval-sample state follow-up: 58 task-graph, 18 ECS graphics, descriptor-buffer smoke, plus static/skinned early/mid/late transparent CSG captures | passed; prepared accumulation declares the four removed-interval aliases as exact-range `UnorderedAccess` reads. The compiler preserves four source state seeds across split depth-warp, extinction, and integration packets; direct/unprepared accumulation paths retain their native bridge. |
| Graph-owned lagged deferred bindless-selector follow-up: FrontierSafe graph unit and descriptor-buffer smoke | passed; the immutable history-selector upload is pinned to and merged into Deferred Lighting's existing Compute packet, retains the external history completion wait, and hands off `Common` to `ConstantBuffer` without extending the Lighting-to-Composite range |
| Graph-owned ray-trace material-context selector follow-up: FrontierSafe graph unit, descriptor-buffer smoke, and opaque CSG capture | passed; a post-preflight immutable selector blob merges into Shadow Preparation, publishes `Common`, and is logically handed off there to the later Compute trace consumers |
| Graph-owned caustic emission-target follow-up: FrontierSafe graph unit, descriptor-buffer smoke, and caustic-sphere capture | passed; the preflight-frozen refractive AABB payload merges into Shadow Preparation, publishes `Common`, and is logically handed off there to software and hardware caustic consumers |
| Graph-owned Surfel frame-constants follow-up: FrontierSafe graph unit, descriptor-buffer smoke, and forced-software GI capture | passed; the immutable 80-byte payload merges into Shadow Preparation, publishes `Common`, and is logically handed off there to later asynchronous Surfel GI work |
| Graph-owned shadow material-context batch follow-up: FrontierSafe graph unit, descriptor-buffer smoke, and forced-software GI capture | passed; all three immutable streams merge into Shadow Preparation, publish `Common`, and are handed off there as `ShaderResource` to later asynchronous trace consumers |
| Graph-owned software scene-BVH pair follow-up: FrontierSafe graph unit, descriptor-buffer smoke, and forced-software GI capture | passed; the frozen node and leaf-instance uploads merge into Shadow Preparation, publish `Common`, and are handed off there as one `ShaderResource` topology payload to later asynchronous trace consumers |
| Graph-owned opaque hardware TLAS build follow-up: renderer build, FrontierSafe graph unit, and descriptor-buffer smoke | passed; `nwb_ecs_render` built, graph tests passed 50/50, and descriptor smoke passed 75 tests with 10 expected topology skips. The graph unit proves the backing-buffer `Common` to `AccelStructRead` handoff and that Shadow Preparation, rather than the frozen preflight data, owns later Compute consumers. |
| Graph-owned opaque hardware BLAS build/refit follow-up: renderer build, FrontierSafe graph unit, and descriptor-buffer smoke | passed; `nwb_ecs_render` built, graph tests passed 50/50, and descriptor smoke passed 75 tests with 10 expected topology skips. The graph unit covers both a frozen BLAS build and a state-only compatibility BLAS backing, proving their `Common` to `AccelStructRead` handoff remains owned by Shadow Preparation before later Compute consumers. |
| Graph-owned software-only per-mesh SW-BVH build/refit follow-up: renderer/runtime build, FrontierSafe graph unit, descriptor-buffer smoke, and forced-software transparent capture | passed; the graph unit proves `Common` to `UnorderedAccess` ownership for parent and shared scratch state, graph tests passed 50/50, descriptor smoke passed 75 tests with 10 expected topology skips, and the forced-software transparent capture completed. |
| Graph-owned hybrid per-mesh SW-BVH follow-up: renderer build, FrontierSafe graph unit, descriptor-buffer smoke, and hardware transparent-multi capture | passed; the frozen mesh build is accepted only after its commands record, while a failed optional software tail discards that plan and may still submit the valid hardware opaque result after the complete declared fallback triple restores successfully. An invalid restore rejects the merged packet. Graph tests passed 50/50, descriptor smoke passed 77 tests with 11 expected topology skips, and the Vulkan/X11 transparent-multi capture passed. |
| Graph-owned hybrid scene-BVH pair follow-up: renderer build, FrontierSafe graph unit, descriptor-buffer smoke, and hardware transparent-multi capture | passed; the immutable node/leaf pair is accepted after the retained traversal table restores its exact mesh descriptor arrays. A fresh-pair miss without declared blobs rejects the optional software tail rather than writing the scene buffers natively. Graph tests passed 50/50, descriptor smoke passed 77 tests with 11 expected topology skips, and the Vulkan/X11 transparent-multi capture passed. |
| Graph-owned hybrid shadow material-context follow-up: renderer build, FrontierSafe graph unit, descriptor-buffer smoke, transparent-multi, and skinned-caustic captures | passed and strengthened by the later blob-only closure; every healthy hybrid frame now freezes a fresh software triple and a complete hardware fallback triple before the software plan supersedes the hardware plan. A tail mismatch restores only the three declared hardware blobs; an invalid restore rejects and discards the merged packet. There is no direct material-context retry or consumer-disable escape path. |
| Graph-owned hybrid software scene-traversal follow-up: renderer build, FrontierSafe graph unit, descriptor-buffer smoke, transparent-multi, and skinned-caustic captures | passed and closed for fresh uploads plus accepted static-cache reuse; healthy hybrid preflight retains owning node/position/index/attribute buffers, exact descriptor slots, mesh versions, byte sizes, primitive counts, source mutation versions, and either immutable scene bytes or the accepted cache identity. Recording only validates that frozen plan. A stale or missing plan rejects the optional SW tail without a recording-time ECS/material gather or descriptor-table publication. |
| Historical hybrid software traversal fallback proof: renderer build, FrontierSafe graph unit, descriptor-buffer smoke, and Vulkan/X11 transparent-multi capture | passed as fault-injection evidence; the former retained-table fault proved that an optional software tail could fail while the complete declared hardware blob triple restored opaque hardware shadows. That production test hook is now retired. Current coverage verifies the immutable triple and reject-before-write boundary with a test-owned graph fixture instead of mutating renderer behavior. |
| Frozen hybrid hardware material-context fallback follow-up: renderer build, FrontierSafe graph unit, descriptor-buffer smoke, transparent-multi, skinned-caustic, and historical forced-fallback captures | passed and strengthened; preflight retains the hardware material table, instance stream, typed bytes, destination identities, descriptor handles, capacities, and source mutation versions before the software-compatible upload supersedes it. The optional-tail miss restores that immutable payload through declared graph writes only. Missing capture fails preflight and a stale restore rejects the packet for fresh planning. Forced-fallback captures are retained only as historical evidence; no production test control remains. |
| Retire stale hybrid hardware material-context retry seam: renderer build, ECS graph contract, graph unit, descriptor-buffer smoke, and source audit | passed; the stale-snapshot test proxy, one-shot flags, dedicated stale-smoke target, no-blob restore overload, and the fallback's direct `buildSceneTlas(..., false)` material-regather retry were removed. Separate TLAS/BLAS acceleration-structure build retries remain. The ordinary transparent smoke now pins a fixed angle for six rendered frames and rejects cache alternation, incomplete uploads, invalid blob restoration, hybrid-tail failure, and the retired direct-material-retry diagnostic. `rt_swbvh.cpp` contains zero `writeBuffer(` calls. Debug passed ECS graphics 93/93, graph 288/288, and descriptor smoke 306 with 27 capability skips; optimized passed 93/93, 288/288, and 305 with 28 skips; final passed 93/93, 286/286, and 264 with 30 skips. Asset graphics passed 72/72. Window capture was unavailable in this Windows session because the testbed exited before creating a window. |
| Historical forced-fallback target-scene paired A/B: renderer build, Vulkan/X11 stress capture, and GPU timing | passed on AMD BC-250 (RADV GFX1013) without validation-layer timing overhead. The paired fixed-yaw ten-character animated scene produced 26 `render.frame` intervals per arm: healthy hybrid median 18.2199 ms and forced opaque-HW fallback median 9.0301 ms. This result remains historical diagnostic evidence only; the forced renderer route and restore log are no longer acceptance controls. The `hybrid-shadow-boundary` workflow now compares the natural healthy scene with a test-owned transparency-free opaque scene using the same binary. |
| Retire hybrid traversal production test controls: renderer builds, ECS source contract, descriptor-buffer fixture, Python/CTest harnesses, and source audit | passed; all hybrid force methods, proxy methods, state flags, synthetic branches, and test-throttled production logs were removed from `impl/`. The same stress binary now selects either the natural transparent hybrid scene or a test-owned opaque scene before renderer input, requires positive post-warmup GPU timing samples for every mandatory route scope, rejects route leakage and every exact hybrid failure diagnostic, reports non-RayQuery hardware through one explicit test-owned skip marker without allowing runtime/validation errors to hide behind it, and measures hybrid overhead relative to the opaque baseline. The descriptor suite owns success and third-blob-mismatch restore fixtures that prove immutable triple validation, reject-before-transition/write behavior, discard semantics, final states, and acceptance-only publication without a renderer test API. Debug passed ECS graphics 95/95 and descriptor smoke 308 tests with 27 capability skips; the three registered Python harness suites passed. Optimized renderer/tests and focused contracts passed, and the final renderer built. The live Windows capture was attempted but the Vulkan loader failed to query Win32 surface capabilities before project startup, so it provided no route evidence and remained a failure rather than being reclassified as a capability skip. |
| Graph-owned hybrid TLAS build follow-up: renderer build, FrontierSafe graph unit, descriptor-buffer smoke, transparent-multi, and skinned-caustic captures | passed; healthy hybrid preflight retains the exact instance descriptors, BLAS handles, selected TLAS, and backing generation in Shadow Preparation. A capture or record mismatch clears only that plan and retries the direct TLAS build while retaining the graph-owned software material context. Graph tests passed 50/50, descriptor smoke passed 77 tests with 11 expected topology skips, and both Vulkan/X11 captures passed. |
| Graph-owned hybrid hardware-BLAS follow-up: renderer build, FrontierSafe graph unit, descriptor-buffer smoke, transparent-multi, and skinned-caustic captures | passed; independent frozen hardware mesh builds now record in Shadow Preparation for hybrid frames, while a mismatch discards the plan and retries the direct compatibility loop instead of rejecting the opaque-shadow fallback. Graph tests passed 50/50, descriptor smoke passed 77 tests with 11 expected topology skips, and both Vulkan/X11 captures passed. |
| Dedicated-Transfer recovery-frontier follow-up: graph unit and descriptor-buffer smoke | passed; graph tests passed 50/50 and descriptor smoke passed 77 tests with 11 expected topology skips. The native test accepts a graph-owned Transfer upload, injects a rejected dependent Graphics suffix, executes the semantic accepted-frontier recovery helper, then confirms the Device receives that exact physical Transfer token on the independent recovery-tail submission. |
| Graph-owned frame GPU-timing reset: graph unit, ECS graphics, and descriptor-buffer smoke | passed; the reset now compiles as one strict primary-Graphics graph packet, and only its accepted task callback publishes timer-query availability while failed work remains revoked. Graph tests passed 50/50, ECS graphics passed 18/18, and descriptor smoke passed 78 tests with 11 expected topology skips. The targeted rejection/retry smoke proves a rejected reset leaves no stale dynamic-rendering query availability and the following accepted graph preamble recovers it. |
| Graph-owned CSG interval rect-clear follow-up: renderer build, graph/ECS/descriptor tests, and Vulkan/X11 CSG captures | passed; opaque G-buffer and prepared-transparent AVBOIT now run an exact two-target frozen-rect clear task before their native CSG producers, with graph-declared `CopyDest` to `UnorderedAccess` handoff over all peel ID layers and the single receiver-event-count layer. Graph tests passed 51/51, ECS graphics passed 18/18, descriptor smoke passed 78 tests with 11 expected topology skips, and opaque plus transparent CSG early/mid/late captures passed. The direct CSG clear remains the compatibility fallback for unprepared transparent work. |
| Graph-owned CSG clip-buffer entry-state follow-up: renderer build, graph/ECS/descriptor tests, and Vulkan/X11 CSG captures | passed; opaque and prepared-transparent CSG callbacks now consume graph-declared receiver/cutter `ShaderResource` and clip-context/interval-sample `ConstantBuffer` states without restating the native heap-buffer bridge. Graph tests passed 60/60, ECS graphics passed 18/18, descriptor smoke passed 83 tests with 11 expected topology skips, and opaque plus early/mid/late transparent CSG captures passed. Direct, unprepared, and empty gathered-work paths retain the native helper; intra-task CSG UAV fences remain native. |
| Graph-owned soft-transparent Shadow fold handoff follow-up: renderer build, task-graph unit, descriptor-buffer smoke, ECS graphics, and CTest | passed; the prepared normal soft-transparent route now records opaque visibility and the terminal transparent fold as two mergeable callbacks in the existing Shadow effects packet. The compiler test proves their explicit dependency, same-packet order, tail `UnorderedAccess`-to-`UnorderedAccess` visibility fence, and the terminal Lighting `UnorderedAccess`-to-`ShaderResource` consumer transition. The real-Vulkan getter-only fold task observes both output images as `UnorderedAccess` without a native state bridge. The terminal fold retains the established timing, acceptance, temporal-history, recovery, and output-state owner; direct/minimal, preflight-clear, and adaptive/hybrid compatibility routes remain monolithic. `nwb_ecs_render` rebuilt; graph tests passed 72/72, descriptor smoke passed 97 with 11 expected topology skips, ECS graphics passed 18/18, and CTest passed 3/3. |
| Historical graph-owned soft-transparent Shadow fold target-scene A/B: paired stress build, graph/ECS/descriptor tests, CTest, and Vulkan/X11 timing capture | passed on AMD BC-250 (RADV GFX1013) without validation-layer timing overhead. The now-retired `soft-transparent-shadow-fold` workflow proved the graph and retained-monolithic path markers independently, captured both fixed-yaw ten-character hybrid-shadow frames, and rejected forbidden runtime/validation-log strings. Across 27 `render.frame` intervals per arm, retained monolithic median was 18.1724 ms and graph-owned fold median was 18.1798 ms (+0.041%); `render.shadow_visibility` was 12.0111 vs 12.0144 ms (+0.027%), opaque trace 1.6096 vs 1.6152 ms (+0.348%), and transparent trace 7.5743 vs 7.5800 ms (+0.075%). The independently rebuilt graph suite passed 72/72, ECS graphics 18/18, descriptor smoke 97 passed with 11 expected topology skips, and CTest passed 3/3. Historical local evidence was recorded under `.cozter/out/ab-results/soft-transparent-shadow-fold/20260815_130045/` on the capture host. |
| Graph-owned soft-transparent trace-to-resolve handoff follow-up: renderer build, task-graph unit, descriptor-buffer smoke, ECS graphics, CTest, and target-scene A/B | passed; the prepared route now records opaque visibility, transparent trace, and terminal resolve as three mergeable callbacks in the existing Shadow effects packet. The compiler test proves their exact order, the opaque-to-trace `UnorderedAccess` fence, and the trace output's `UnorderedAccess`-to-`ShaderResource` transition before terminal resolve. The real-Vulkan getter-only probes observe the trace target as `UnorderedAccess` in the trace callback and `ShaderResource` in resolve without a native bridge. The terminal resolve retains timing, acceptance, temporal-history, recovery, and output-state ownership; direct/minimal, preflight-clear, and adaptive/hybrid compatibility routes remain monolithic. `nwb_ecs_render` rebuilt; graph tests passed 72/72, descriptor smoke passed 97 with 11 expected topology skips, ECS graphics passed 18/18, and CTest passed 3/3. The fresh paired target-scene run passed with graph `render.frame` median 18.1536 ms (26 intervals) versus retained 18.1592 ms (27, -0.031%); `render.shadow_visibility` was 12.0011 vs 12.0145 ms (-0.111%) and transparent trace was 7.5686 vs 7.5753 ms (-0.088%). Local evidence: `.cozter/out/ab-results/soft-transparent-shadow-fold/20260815_133404/`. |
| Graph-owned soft-transparent temporal-merge entry states: renderer build, task-graph unit, descriptor-buffer smoke, ECS graphics, CTest, and target-scene A/B | passed; when the prepared transparent temporal path is active, the compiled frame freezes its A/B selector and declares the selected history/moment pair as `ShaderResource` inputs and the opposite pair as `UnorderedAccess` outputs. The terminal callback no longer restates that entry batch. The compiler test proves the selected history/moment `UnorderedAccess`-to-`ShaderResource` prologue transitions, and the real-Vulkan getter-only probe observes both selected inputs as `ShaderResource` and both outputs as `UnorderedAccess` without a native bridge. The merge-to-wavelet intra-callback transition remains local; inactive temporal, direct/minimal, preflight-clear, and adaptive/hybrid compatibility routes retain native setup. `nwb_ecs_render` rebuilt; graph tests passed 72/72, descriptor smoke passed 97 with 11 expected topology skips, ECS graphics passed 18/18, and CTest passed 3/3. The fresh paired target-scene run passed with graph `render.frame` median 18.1776 ms (26 intervals) versus retained 18.1972 ms (27, -0.108%); `render.shadow_visibility` was 12.0063 vs 12.0341 ms (-0.231%) and transparent trace was 7.5804 vs 7.5948 ms (-0.190%). Local evidence: `.cozter/out/ab-results/soft-transparent-shadow-fold/20260815_135623/`. |
| Graph-owned opaque soft temporal-merge entry states: renderer build, task-graph unit, descriptor-buffer smoke, ECS graphics, CTest, and target-scene A/B | passed; when the prepared opaque temporal path is active, the compiled frame freezes its shared A/B selector and declares the selected opaque history/moment pair as `ShaderResource` inputs and the opposite pair as `UnorderedAccess` outputs. The opaque callback no longer restates that temporal entry batch. The compiler proof covers B-as-input, and the real-Vulkan getter-only callback observes B history/moments as `ShaderResource` with A history/moments as `UnorderedAccess`, without a native bridge. The opaque geometry downsample-to-merge handoff remains local because both phases are inside that callback; inactive temporal, direct/minimal, preflight-clear, and adaptive/hybrid compatibility routes retain native setup. `nwb_ecs_render` rebuilt; graph tests passed 73/73, descriptor smoke passed 98 with 11 expected topology skips, ECS graphics passed 18/18, and CTest passed 3/3. The fresh paired target-scene run passed with graph `render.frame` median 18.1866 ms (27 intervals) versus retained 18.1779 ms (27, +0.048%); `render.shadow_visibility` was 12.0282 vs 12.0022 ms (+0.217%) and transparent trace was 7.5878 vs 7.5809 ms (+0.091%). Local evidence: `.cozter/out/ab-results/soft-transparent-shadow-fold/20260815_142137/`. |
| Graph-owned opaque soft temporal static reads: renderer build, task-graph unit, descriptor-buffer smoke, ECS graphics, CTest, and target-scene A/B | passed; the prepared opaque temporal route now declares stable previous soft geometry and world-position reads as `ShaderResource` at the task boundary alongside its selected history/moment inputs. The opaque callback no longer restates those entry states; its current geometry remains locally transitioned after the in-callback downsample. The compiler proof covers selected B history/moments plus previous geometry/world position as `ShaderResource`, and the real-Vulkan getter-only callback observes the same without a native bridge. Inactive temporal, direct/minimal, preflight-clear, and adaptive/hybrid compatibility routes retain native setup. `nwb_ecs_render` rebuilt; graph tests passed 73/73, descriptor smoke passed 98 with 11 expected topology skips, ECS graphics passed 18/18, and CTest passed 3/3. The fresh paired target-scene run passed with graph `render.frame` median 18.1706 ms (26 intervals) versus retained 18.1829 ms (27, -0.067%); `render.shadow_visibility` was 12.0036 vs 12.0307 ms (-0.226%) and transparent trace was 7.5802 vs 7.5960 ms (-0.207%). Local evidence: `.cozter/out/ab-results/soft-transparent-shadow-fold/20260815_143457/`. |
| Graph-owned soft resolve static entry states: renderer build, task-graph unit, descriptor-buffer smoke, ECS graphics, CTest, and target-scene A/B | passed; prepared opaque resolve now inherits graph-declared depth/world-position/normal `ShaderResource` reads and scene-shading `ConstantBuffer` state for upsample, while the split transparent fold also inherits its stable soft-geometry `ShaderResource` state for wavelets. Native resolve callbacks no longer restate that entry batch; opaque current geometry after its in-callback downsample and all ping-pong/UAV handoffs remain local. The compiler proof covers the trace and opaque-geometry `UnorderedAccess`-to-`ShaderResource` handoffs plus the terminal previous-geometry/G-buffer/scene prologue, and the real-Vulkan getter-only fold callback observes every state without a native bridge. Direct/minimal, preflight-clear, and adaptive/hybrid compatibility routes retain native setup. `nwb_ecs_render` rebuilt; graph tests passed 73/73, descriptor smoke passed 98 with 11 expected topology skips, ECS graphics passed 18/18, and CTest passed 3/3. The fresh paired target-scene run passed with graph `render.frame` median 18.1763 ms (26 intervals) versus retained 18.1894 ms (27, -0.072%); `render.shadow_visibility` was 11.9954 vs 12.0225 ms (-0.226%) and transparent trace was 7.5776 vs 7.5809 ms (-0.044%). Local evidence: `.cozter/out/ab-results/soft-transparent-shadow-fold/20260815_145347/`. |
| Graph-owned opaque soft geometry-to-resolve handoff: renderer build, task-graph unit, descriptor-buffer smoke, ECS graphics, CTest, and target-scene A/B | passed; the prepared route now records opaque trace/geometry production, opaque soft resolve, transparent trace, and terminal transparent resolve as four mergeable callbacks in the existing Shadow effects packet. The compiler lowers current soft geometry from `UnorderedAccess` to `ShaderResource` before opaque temporal merge and wavelet resolve, while the trace image's local UAV fence remains native. Getter-only compiler and real-Vulkan probes prove the new resolve callback receives its geometry read without a native bridge; resolve failure falls back to all-lit visibility and discards the split timing scopes before downstream no-op callbacks. The terminal fold retains timing completion, temporal-history publication, recovery, and output-state ownership; direct/minimal, preflight-clear, adaptive, and hybrid compatibility routes remain monolithic. `nwb_ecs_render` rebuilt; graph tests passed 73/73, descriptor smoke passed 98 with 11 expected topology skips, ECS graphics passed 18/18, and CTest passed 3/3. The fresh paired target-scene run passed with graph `render.frame` median 18.1734 ms (27 intervals) versus retained 18.1701 ms (27, +0.018%); `render.shadow_visibility` was 12.0104 vs 12.0115 ms (-0.009%) and transparent trace was 7.5854 vs 7.5889 ms (-0.046%). Local evidence: `.cozter/out/ab-results/soft-transparent-shadow-fold/20260815_151737/`. |
| Graph-owned opaque trace-to-resolve same-UAV fence: renderer build, task-graph unit, descriptor-buffer smoke, ECS graphics, CTest, and target-scene A/B | passed; the prepared four-callback route now declares opaque `shadowSoftHalfA` as `UnorderedAccess` read/write in both opaque production and opaque resolve, so the compiler emits the exact same-UAV fence in the resolver prologue. The hardware and software-BVH renderers no longer submit a duplicate local post-trace fence on that split route; the resolver still owns its native sampled-merge transition, while direct/minimal, unsplit, preflight-clear, adaptive, and hybrid compatibility routes retain their local fence. Compiler and real-Vulkan getter-only probes prove the opaque trace `UnorderedAccess`-to-`UnorderedAccess` barrier together with the existing geometry `UnorderedAccess`-to-`ShaderResource` handoff. `nwb_ecs_render` rebuilt; graph tests passed 73/73, descriptor smoke passed 98 with 11 expected topology skips, ECS graphics passed 18/18, and CTest passed 3/3. The paired target-scene run passed with graph `render.frame` 18.1825 ms (26 intervals) versus retained 18.1711 ms (27, +0.063%); `render.shadow_visibility` was 12.0135 vs 12.0306 ms (-0.142%) and transparent trace was 7.5986 vs 7.5819 ms (+0.220%). Local evidence: `.cozter/out/ab-results/soft-transparent-shadow-fold/20260815_154207/`. |
| Graph-owned opaque first-wavelet and resolve-tail handoffs: renderer build, task-graph unit, descriptor-buffer smoke, ECS graphics, CTest, and target-scene A/B | passed; the prepared route now records opaque trace/geometry production, opaque first wavelet, opaque resolve tail, transparent trace, and terminal transparent resolve as five mergeable callbacks in the existing Shadow effects packet. The compiler lowers opaque trace and geometry from `UnorderedAccess` to `ShaderResource` before the first-wavelet/temporal work, then lowers the first-wavelet half result from `UnorderedAccess` to `ShaderResource` before the tail upsample. Those split callbacks no longer restate their graph-owned output/input/visibility states; only temporal and dynamic compatibility transitions remain local. Getter-only compiler and real-Vulkan probes observe every supplied state without a native bridge. The retained opaque resolve timing begins in the first wavelet and ends in the tail; failure of either clears all-lit visibility and discards it while the terminal fold remains output, acceptance, and recovery owner. Direct/minimal, preflight-clear, adaptive, and hybrid compatibility routes remain monolithic. `nwb_ecs_render` rebuilt; graph tests passed 73/73, descriptor smoke passed 98 with 11 expected topology skips, ECS graphics passed 18/18, and CTest passed 3/3. The fresh paired target-scene run passed with graph `render.frame` median 18.1686 ms (26 intervals) versus retained 18.1415 ms (27, +0.149%); `render.shadow_visibility` was 12.0267 vs 11.9909 ms (+0.298%), opaque trace was 1.6171 vs 1.6109 ms (+0.385%), and transparent trace was 7.5878 vs 7.5578 ms (+0.398%). Local evidence: `.cozter/out/ab-results/soft-transparent-shadow-fold/20260815_163936/`. |
| Graph-owned transparent first-wavelet and resolve-tail handoffs: renderer build, task-graph unit, descriptor-buffer smoke, ECS graphics, CTest, and target-scene A/B | passed; the prepared route now records opaque production, opaque first wavelet, opaque resolve tail, transparent trace, transparent first wavelet, and terminal transparent resolve tail as six mergeable callbacks in the existing Shadow effects packet. The compiler lowers transparent trace output from `UnorderedAccess` to `ShaderResource` before its first wavelet, preserves the exact trace-to-first same-UAV fence, then lowers that wavelet's half result from `UnorderedAccess` to `ShaderResource` before the terminal tail upsample. Getter-only compiler and real-Vulkan probes observe every supplied state without a native bridge. The transparent resolve timing begins in the first wavelet and ends in the terminal tail; failure discards it while preserving the completed opaque visibility. The terminal tail remains the acceptance, recovery, temporal-history, and output-state owner. Direct/minimal, preflight-clear, adaptive, and hybrid compatibility routes remain monolithic. `nwb_ecs_render` rebuilt; graph tests passed 73/73, descriptor smoke passed 98 with 11 expected topology skips, ECS graphics passed 18/18, and CTest passed 3/3. The fresh paired target-scene run passed with graph `render.frame` median 18.1623 ms (26 intervals) versus retained 18.1931 ms (27, -0.170%); `render.shadow_visibility` was 12.0084 vs 12.0448 ms (-0.303%), opaque trace was 1.6131 vs 1.6160 ms (-0.179%), and transparent trace was 7.5787 vs 7.6089 ms (-0.396%). Local evidence: `.cozter/out/ab-results/soft-transparent-shadow-fold/20260815_171128/`. |
| Graph-owned transparent temporal-merge-to-wavelet handoff: renderer build, task-graph unit, descriptor-buffer smoke, ECS graphics, CTest, and target-scene A/B | passed; on active prepared transparent-temporal frames the Shadow effects packet now contains seven mergeable callbacks: opaque production, opaque first wavelet, opaque resolve tail, transparent trace, transparent temporal merge, transparent first wavelet, and terminal resolve tail. The compiler lowers transparent trace output from `UnorderedAccess` to `ShaderResource` for temporal merge, then lowers the selected next history/moment pair from `UnorderedAccess` to `ShaderResource` for first wavelet. Getter-only compiler and real-Vulkan merge/wavelet probes observe those inputs and outputs without a native bridge. Transparent resolve timing starts and closes its marker in temporal merge; the terminal tail alone finishes the timestamp and retains acceptance, recovery, temporal-history, and output-state ownership. Merge or wavelet failure discards only transparent resolve and preserves opaque visibility; inactive temporal, direct/minimal, preflight-clear, adaptive, and hybrid compatibility routes retain their existing paths. `nwb_ecs_render` rebuilt; graph tests passed 73/73, descriptor smoke passed 98 with 11 expected topology skips, ECS graphics passed 18/18, and CTest passed 3/3. The paired target-scene run passed with graph `render.frame` median 18.1814 ms (26 intervals) versus retained 18.1517 ms (27, +0.163%); `render.shadow_visibility` was 12.0232 vs 11.9829 ms (+0.336%), opaque trace was 1.6083 vs 1.6139 ms (-0.347%), and transparent trace was 7.5867 vs 7.5461 ms (+0.538%). Local evidence: `.cozter/out/ab-results/soft-transparent-shadow-fold/20260815_174054/`. |
| Graph-owned opaque CSG interval-combine handoff follow-up: renderer build, task-graph unit, descriptor-buffer smoke, ECS graphics, CTest, and Vulkan/X11 opaque captures | passed; at that stage opaque G-buffer ended after receiver-span build, and a mergeable Combine callback declared the five peel/span `UnorderedAccess` reads and four removed-output writes before the Sample callback consumed those outputs. The common compiler path proved 5/9/4 barriers in one Graphics packet, while a forced FrontierSafe split proved all five Combine state seeds and same-UAV fences. A real Vulkan getter-only packet test recorded both callbacks without native bridges. Prepared-transparent AVBOIT and direct compatibility callers retained their aggregate combine fences. `nwb_ecs_render` rebuilt; graph tests passed 74/74, descriptor smoke passed 99 with 11 expected topology skips, ECS graphics passed 18/18, CTest passed 3/3, and opaque plus compute-emulation CSG captures passed 2/2. |
| Graph-owned opaque CSG receiver-span handoff follow-up: renderer build, task-graph unit, descriptor-buffer smoke, ECS graphics, CTest, and Vulkan/X11 opaque captures | passed; opaque G-buffer now ends after receiver-surface raster work, and a mergeable Span callback declares receiver-event data/count `UnorderedAccess` reads plus receiver-span data/count writes before Combine consumes the span outputs. The common compiler path proves 5/4/9/4 barriers across G-buffer, Span, Combine, and Sample in one Graphics packet; a forced FrontierSafe split proves the two Span seeds, the three G-buffer plus two Span Combine seeds, and all resulting same-UAV fences. A real Vulkan getter-only packet test observes event slices 0/31 and span slices 0/15 without native bridges. Prepared-transparent AVBOIT and direct compatibility callers retain their aggregate receiver-surface-to-span and span-to-combine fences. `nwb_ecs_render` rebuilt; graph tests passed 74/74, descriptor smoke passed 99 with 11 expected topology skips, ECS graphics passed 18/18, CTest passed 3/3, and opaque plus compute-emulation CSG captures passed 2/2. |
| Graph-owned prepared TLAS state-finalize handoff: renderer build, FrontierSafe graph unit, descriptor-buffer smoke, ECS graphics, CTest, and transparent-multi capture | passed; frozen Shadow Preparation now declares both the typed TLAS and its backing buffer `Common` to `AccelStructWrite`, records the build without a native bridge, then uses an adjacent state-only finalizer for `AccelStructWrite` to `AccelStructRead`. A successor-only FrontierSafe opt-in retains that finalizer in the same accepting first Graphics packet despite direct Compute consumers, preserving packet acceptance and cross-frame backing-state atomicity. Direct and hybrid retry paths retain their native write-to-read bridge. The compiler unit and real Vulkan getter-only smoke prove both typed aliases at both boundaries. `nwb_ecs_render` rebuilt; graph tests passed 75/75, descriptor smoke passed 100 with 11 expected topology skips, ECS graphics passed 18/18, focused CTest passed 3/3, and the hardware transparent-multi capture passed. |
| Graph-owned prepared transparent AVBOIT interval-combine handoff: renderer build, graph unit, descriptor-buffer smoke, ECS graphics, and CSG capture matrix | passed; prepared transparent AVBOIT now records its existing receiver-surface/span producer followed by a mergeable Combine callback. The graph lowers the five exact peel/span `UnorderedAccess` input fences and four removed-output writes before phase-local occupancy uploads, while direct, unprepared, and empty-work callers retain native aggregate bridges. The frozen interval timer spans both callbacks and is discarded on either readiness mismatch; the first AVBOIT upload begins a distinct semantic packet so Hardware Caustics and AVBOIT retain independent timing/acceptance submissions on Graphics-only topology. The compiler unit proves the common 5/9/1/5 barriers plus forced-split source seeds, and the real Vulkan getter-only probe records every Combine alias without a native bridge. `nwb_ecs_render` rebuilt; graph tests passed 77/77, descriptor smoke passed 100 with 11 expected topology skips, ECS graphics passed 18/18, and the clean CSG capture matrix passed 14/14. |
| Graph-owned prepared transparent AVBOIT receiver-span handoff: renderer build, graph unit, descriptor-buffer smoke, ECS graphics, CTest, and CSG capture matrix | passed; the prepared transparent route now records Pre (peel plus receiver-surface events), a mergeable Span callback, then Combine before phase-local occupancy uploads. The graph lowers two receiver-event-to-span, three peel-to-Combine, two span-to-Combine, and four Combine-to-occupancy exact-range `UnorderedAccess` handoffs. Pre closes its raster pass before Span's graph prologue; the retained AVBOIT interval timer spans Pre/Span/Combine, and a Span readiness mismatch discards it so Combine no-ops rather than consuming stale spans. Renderer acceptance requires the timed Pre/Span/Combine/Clear/Occupancy chain to remain one packet; direct, unprepared, and empty-work callers retain their native aggregate span/combine bridges. The compiler tests prove common 5/4/9/1/5 barriers and forced-split state seeds from both Pre and Span; the real Vulkan getter-only smoke observes event and span slices without native bridges. `nwb_ecs_render` rebuilt; graph tests passed 77/77, descriptor smoke passed 100 with 11 expected topology skips, ECS graphics passed 18/18, focused CTest passed 3/3, and the clean CSG capture matrix passed 14/14. |
| Graph-owned prepared BLAS typed-state-finalize handoff: renderer build, FrontierSafe graph unit, descriptor-buffer smoke, ECS graphics, and hardware transparent/skinned captures | passed; frozen Shadow Preparation now declares each prepared typed BLAS and its backing buffer `Common` to `AccelStructWrite`, records the build without its native typed-AS bridge, then uses the same adjacent accelerator-structure finalizer to lower both aliases from `AccelStructWrite` to `AccelStructRead` in the accepting first Graphics packet. Position/index `AccelStructBuildInput` retains its native bridge whenever the aggregate callback may immediately build the software BVH from those streams; the later tail-free follow-up graph-owns it only when that consumer is absent. The accepted cross-frame source retains every live BLAS backing and position/index pair, so a direct record-time replacement fallback cannot later be re-imported as `Common`; direct and hybrid retry builds retain their native typed write-to-read bridge. Compiler and real-Vulkan getter-only coverage prove typed TLAS/BLAS plus backing aliases at both boundaries. `nwb_ecs_render` and both scene targets rebuilt; graph tests passed 77/77, descriptor smoke passed 100 with 11 expected topology skips, ECS graphics passed 18/18, and fresh transparent-multi plus late skinned-caustic captures passed. |
| Graph-owned prepared pure-software SW-BVH input handoff: renderer build, graph unit, descriptor-buffer smoke, ECS graphics, and forced-software/hardware transparent captures | passed; frozen pure-software Shadow Preparation now supplies each selected position/index input as `ShaderResource` before its prepared SW-BVH recorder runs, so it no longer restates the native entry transition or commits an empty barrier batch. The compiler handles both fresh `Common` and retained `AccelStructBuildInput` seeds; the real-Vulkan getter-only packet test observes both entry states and the accepted final `ShaderResource` handoff without native state calls. Direct and unfrozen callers retain native setup; the later packet-local hybrid-tail handoff applies the same lowering only when both frozen plans and imports verify. Rejection and acceptance-only topology/refit publication are unchanged. The split opaque software path also preserves the established one-shot traversal diagnostic used by the forced-software smoke. `nwb_ecs_render` and both transparent targets rebuilt; graph tests passed 78/78, descriptor smoke passed 101 with 11 expected topology skips, ECS graphics passed 18/18, and both Vulkan/X11 transparent-multi captures passed. |
| Graph-owned prepared tail-free BLAS input handoff: renderer build, graph unit, descriptor-buffer smoke, ECS graphics, and opaque/hybrid/software captures | passed; a frozen hardware BLAS plan with no prepared software trace tail now verifies each position/index buffer reuses an ID from the shared trace-geometry import set, deduplicates physical aliases, and declares `AccelStructBuildInput` in Shadow Preparation. The frozen recorder therefore skips only those redundant native position/index setters; the generic `ShaderResource` uses exclude the same IDs, and Post-G-Buffer Normalize returns them to `ShaderResource` for the accepted cross-frame handoff. A missing import falls back to the native bridge rather than rejecting the packet. Direct/retry and incomplete prepared software-tail routes retain the native `AccelStructBuildInput` setup; the later verified hybrid-tail follow-up lowers its immediate HW-to-SW handoff. Compiler coverage proves both `Common` and retained `ShaderResource` entries lower to `AccelStructBuildInput` and then normalize to `ShaderResource`; the real-Vulkan getter-only packet test observes both callbacks and its final handoff without native state calls. `nwb_ecs_render`, graph/descriptor/ECS tests, and opaque GI target rebuilt; graph tests passed 79/79, descriptor smoke passed 102 with 11 expected topology skips, ECS graphics passed 18/18, and fresh Vulkan/X11 opaque GI plus hardware-hybrid and forced-software transparent captures passed. |
| Packet-local hybrid HW-to-SW Shadow Preparation tail: renderer build, FrontierSafe graph unit, descriptor-buffer smoke, ECS graphics, and hybrid/software capture matrix | passed; healthy hybrid preparation now records its frozen SW-BVH build, software scene traversal restoration, and opaque-HW fallback transaction in an explicit serial callback immediately after hardware Shadow Preparation and before the accelerator-structure finalizer. The immediate-successor opt-in keeps that callback, the finalizer, and their timing ticket in the exact first accepting Graphics packet despite direct Compute consumers; the compiler proof verifies Compute waits for the complete packet. This row establishes the packet boundary later used to lower the native `AccelStructBuildInput`-to-`ShaderResource` reassertion. Historical captures covered healthy hybrid, frozen-context fallback, stale-context retry, and forced software; the stale production seam and direct retry were subsequently retired by the blob-only closure. |
| Graph-owned prepared hybrid BLAS-to-SW-BVH input handoff: renderer build, FrontierSafe graph unit, descriptor-buffer smoke, ECS graphics, hybrid/software capture matrix, and target-scene A/B | passed; when both frozen mesh plans and all required trace-geometry imports are available, Shadow Preparation declares the prepared BLAS position/index pairs as `AccelStructBuildInput` and the immediate hybrid software tail declares every prepared SW-BVH position/index input as `ShaderResource`. The compiler lowers the exact callback-boundary handoff while the explicit same-packet contract still makes Compute wait for the complete fallback transaction. The prepared BLAS and SW recorders skip only their verified duplicate native setters; missing imports, incomplete frozen plans, direct/retry work, and optional-tail failure retain the established native compatibility bridge. The extended FrontierSafe unit proves `Common`/retained-SRV to build-input plus build-input-to-SRV barriers in the packet-local tail, while the existing real-Vulkan getter-only SW-BVH probe verifies the same native packet-state lowering and accepted SRV handoff. `nwb_ecs_render` and capture targets rebuilt; graph tests passed 79/79, descriptor smoke passed 102 with 11 expected topology skips, ECS graphics passed 18/18, and the four-arm Vulkan/X11 transparent-multi capture matrix passed. The fresh paired target-scene boundary run passed its healthy-hybrid and forced persistent opaque-HW-fallback markers, clean runtime/validation log gate, and 26 `render.frame` timestamp intervals per arm; the healthy median was 18.6151 ms and the deliberately transparency-free fallback was 6.5108 ms (a diagnostic boundary comparison, not pixel parity). Local evidence: `.cozter/out/ab-results/hybrid-shadow-boundary/20260816_010000/`. |
| Imported texture/buffer external-final-state contract: task-graph unit, descriptor-buffer smoke, and ECS graphics | passed; imported texture/buffer metadata may now require a terminal external state. The compiler appends a `TextureStateExport` or `BufferStateExport` epilogue for every terminal declared range, and native lowering reasserts that state after task-local work while always retaining it in the accepted packet snapshot. Unsupported resource kinds and untouched requirements are rejected instead of leaving a renderer-side final-state guess. The real Vulkan smoke deliberately changes a task's texture state locally, then proves the packet handoff returns it to the declared `ShaderResource` state. Graph tests passed 83/83, descriptor smoke passed 106 with 11 expected topology skips, and ECS graphics passed 18/18. Initial exclusive ownership has its own explicit contract below. |
| Graph-to-external exclusive ownership export and later-graph multi-producer texture import: task-graph unit and descriptor-buffer smoke | passed; an imported exclusive texture/buffer/AS may name an `externalFinalReleaseDestinationQueue` alongside its required external final state. The compiler emits each terminal state export before its exact physical ownership release. For a texture, it retains every disjoint terminal subresource source instead of rejecting a split packet: `GpuCompiledExternalResourceExport` exposes immutable source task/queue/range provenance, while its scalar producer fields remain populated only for a one-packet export. The graph-aware `GpuGraphSubmissionTransaction::externalResourceHandoff(graph, …)` stays invalid until *all* terminal packets accept, then returns semantic producers, one latest wait token per physical source queue, immutable terminal-range provenance with each source token, and a merged exact-range native state snapshot without exposing compiler packet IDs. A later graph may import those texture ranges through immutable `initialOwnerHandoffSources`: every source names its exact range, physical owner/destination, graph-local completion, minimum acceptable token, and copied state snapshot. The compiler rejects overlap, mixed legacy/multi declarations, non-texture or concurrent sharing, and a first use that is broad/ambiguous or targets the wrong destination; recording imports the exact source range before its compiler acquire, and submission rejects a same-queue token whose timeline value predates that source. The legacy one-owner path and source-compatible one-producer query remain intact. Buffers and AS remain one-packet externally because native state/ownership tracking is whole-allocation. The real Vulkan smoke proves a same-Graphics two-packet/multi-mip source graph into a later graph, including stale-token rejection; the paired Graphics-plus-dedicated-Compute source into a Graphics graph is topology-gated on this adapter. Graph tests passed 133/133 and descriptor smoke passed 153 with 22 expected topology skips. |
| Imported initial exclusive-owner contract: task-graph unit, descriptor-buffer smoke, and ECS graphics | passed; imported texture/buffer metadata now retains an optional physical owner for an exclusive resource. The compiler requires the owner to be in the current topology and requires every first declared range use to resolve to that exact queue; stale owners, concurrent sharing, and a different first queue fail compilation rather than inventing a Vulkan acquire without an external release/completion pair. The real Vulkan smoke imports a Graphics-owned buffer and proves the first accepted packet uses the same Device-owned physical queue. Graph tests passed 84/84, descriptor smoke passed 107 with 11 expected topology skips, and ECS graphics passed 18/18. Owner-only imports remain exact-queue-only; explicit cross-queue handoffs are covered below. |
| Imported cross-queue initial ownership handoff: task-graph unit, descriptor-buffer smoke, and ECS graphics | passed; imported exclusive texture/buffer metadata may carry a fixed external release destination, an imported completion node, and the producer's native state snapshot. For a different first physical consumer, the compiler emits an initial ownership-acquire marker, attaches that completion to the exact first packet, and recording imports the released state before prologue lowering so Vulkan emits the paired acquire before the graph transition. Owner-only imports, retargeted destinations, stale or unknown queues, concurrent sharing, incomplete handoffs, missing released ranges, and completion tokens that do not match the declared source physical queue are rejected. The real Compute-to-Graphics Vulkan probe also checks wrong-token rejection, then wait/acquire/transition on a dedicated Compute family; it skips on this adapter. Graph tests passed 85/85, descriptor smoke passed 107 tests with 12 expected topology skips, and ECS graphics passed 18/18. |
| Graph-runtime accepted skinning-state cache: task-graph unit, descriptor-buffer smoke, and ECS graphics | passed; `GpuPersistentResourceStateCache` now owns the reusable accepted cross-frame native snapshot, typed resource retention, live-resource filtering, and base-plus-final fan-in that runtime skinning previously implemented itself. Skinning continues to declare the cache source on its semantic graph tasks, but no longer retains raw handoff pointers or builds renderer-local subsets/fan-ins. The real Vulkan probe drops the caller’s retired buffer handle, opens the next command list from the retained raw snapshot, then proves the cache filters that retired generation and imports the later accepted `ShaderResource` final state. Graph tests passed 85/85, descriptor smoke passed 108 tests with 12 expected topology skips, and ECS graphics passed 18/18. |
| Graph-runtime Shadow Prepare accepted-state cache: renderer build, task-graph unit, descriptor-buffer smoke, and ECS graphics | passed; Shadow Preparation now obtains a typed, filtered recording candidate from the last accepted state without mutating that cache, then builds a separate final-state candidate from the recorded packet and commits it only from the accepting packet callback. This preserves prior state and resource lifetime across a rejected packet while removing renderer-local raw subset/fan-in/backing-vector management; the prepared native fallback and semantic task binding remain unchanged. The real Vulkan cache probe proves both the pre-acceptance state remains visible and the accepted final `ShaderResource` state is adopted only after commit. Graph tests passed 85/85, descriptor smoke passed 108 tests with 12 expected topology skips, and ECS graphics passed 18/18. |
| Graph-runtime Surfel-GI counter accepted-state cache: renderer build, task-graph unit, descriptor-buffer smoke, and ECS graphics | passed; the Compute Surfel-GI packet now publishes its counter subset through the typed accepted cache, and the optional late Transfer-preferred readback builds a private candidate before submission then commits it only after that tail accepts. The next semantic Surfel-GI task imports the cache source, so a rejected tail retains the last accepted counter state and its buffer backing without a renderer-local raw handoff. The existing real Vulkan cache probe covers filtered typed sources, pre-acceptance immutability, acceptance-only commit, and retired-buffer filtering. Graph tests passed 85/85, descriptor smoke passed 108 tests with 12 expected topology skips, and ECS graphics passed 18/18. |
| Graph-runtime Surfel-GI Compute scratch cache: renderer build, task-graph unit, descriptor-buffer smoke, and ECS graphics | passed; the Compute route now retains its accepted half-resolution irradiance image plus the six private pool/hash/indirect/snapshot buffers through `GpuPersistentResourceStateCache`, then imports its source via the semantic Surfel-GI task. This retires the raw renderer handoff while keeping the existing Graphics fallback and temporal scheduling untouched. The real Vulkan cache probe drops the caller-owned texture and buffer handles, then opens the next command list from the cache and verifies both typed retained resources still resolve to their accepted `UnorderedAccess` states. Graph tests passed 85/85, descriptor smoke passed 109 tests with 12 expected topology skips, and ECS graphics passed 18/18. |
| Graph-runtime Shadow Visibility Compute scratch cache: renderer build, task-graph unit, descriptor-buffer smoke, and ECS graphics | passed; the accepted Compute Shadow Visibility packet now retains its private coarse/opaque/transparent temporal images and edge-statistics/indirect buffers through `GpuPersistentResourceStateCache`, then declares the cache source on the semantic Shadow Visibility task. This retires the raw renderer handoff without changing Graphics fallback routing or cache ownership; the later blob-only closure retires the hybrid material/scene native-writer records. The real Vulkan cache probe covers typed texture-and-buffer retention after caller handles are released. Graph tests passed 85/85, descriptor smoke passed 109 tests with 12 expected topology skips, and ECS graphics passed 18/18. |
| Graph-runtime Software Caustics Compute scratch cache: renderer build, task-graph unit, descriptor-buffer smoke, and ECS graphics | passed; Software Caustics now retains its accepted accumulator/history/resolve image set through `GpuPersistentResourceStateCache` and imports the source on its semantic Compute task. This retires the raw renderer handoff while retaining both the Hardware Caustics Graphics path and the existing HW→SW compatibility selection. The real Vulkan cache probe covers typed texture retention after caller handles are released. Graph tests passed 85/85, descriptor smoke passed 109 tests with 12 expected topology skips, and ECS graphics passed 18/18. |
| Graph-runtime deferred output-return caches: renderer build, task-graph unit, descriptor-buffer smoke, and ECS graphics | passed; Shadow Visibility, Caustic Irradiance, and Surfel Irradiance retain only the accepted cross-frame snapshots still needed by later first-use declarations. Each normal producer or consumer packet replaces its applicable cache only after acceptance; declaration-owned sources import the next-frame state without renderer-local raw handoffs. The optional lagged-history Transfer tail instead receives its current-frame producer state from normal graph planning. Graphics/Compute routing, hardware/software effect selection, and lagged-history scheduling are unchanged. The real Vulkan cache probe covers typed texture retention after caller handles are released. Graph tests passed 85/85, descriptor smoke passed 109 tests with 12 expected topology skips, and ECS graphics passed 18/18. |
| Declaration-owned lagged-lighting Caustic Irradiance source: renderer build, task-graph unit, descriptor-buffer smoke, and ECS graphics | passed; the optional late history-copy task declares the same irradiance image as its normal Hardware or Software Caustics producer, so compiler planning seeds its late packet directly from the accepted producer packet. The former frame-scoped lighting cache, candidate/filter/commit lifecycle, and task-anchored runtime binding are removed. A real Vulkan late-history proof records and submits the tail with null/zero runtime bindings while observing the producer packet as its compiler-owned state seed. The late-recorded acceptance contract and effect routing remain unchanged. |
| Native task recordability and debug command-capability validation: task-graph unit, descriptor-buffer smoke, and ECS graphics | passed; native compilation now requires every task to retain both a payload and record thunk by default, while metadata-only tooling must opt in explicitly. Debug packet recording accumulates graphics, compute, and transfer command capabilities per task, then rejects a task whose recorded commands exceed either its declared capability mask or the assigned physical queue. The real Vulkan smoke records a valid compute dispatch from a Transfer-declared task and proves the packet is rejected before submission. Graph tests passed 86/86, descriptor smoke passed 110 tests with 12 expected topology skips, and ECS graphics passed 18/18. |
| Immutable graph resource-set expansion: task-graph unit, descriptor-buffer smoke, and ECS graphics | passed; the graph now owns named immutable resource sets for dynamic enumerable bindless collections. A task may declare one state/access contract for a set; task creation validates its live members and freezes them, in declaration order, into ordinary exact-resource uses before hazard analysis and barrier planning. Duplicate members and stale set handles are rejected, so sets never become opaque synchronization domains. The unit proof expands a texture/buffer set and observes each concrete RAW hazard; graph tests passed 87/87, descriptor smoke passed 110 tests with 12 expected topology skips, and ECS graphics passed 18/18. |
| Opaque G-buffer material-geometry resource-set adoption: renderer build, task-graph unit, descriptor-buffer smoke, and ECS graphics | passed; the frozen opaque regular/receiver-surface draw snapshot now imports and names its exact selected mesh-source buffers as one immutable `ShaderResource` resource set. The G-buffer declares that set directly rather than grafting a renderer-local use list; task creation still expands it to ordinary concrete members, preserving exact hazards, barriers, and preflight import reuse. Gather failure or unprepared work retains the existing native material-geometry compatibility path. Graph tests passed 87/87, descriptor smoke passed 110 tests with 12 expected topology skips, and ECS graphics passed 18/18. |
| Opaque CSG interval-sample material-geometry resource-set adoption: renderer build, task-graph unit, descriptor-buffer smoke, and ECS graphics | passed; the frozen opaque CSG draw snapshot now declares its selected mesh-source buffers as one immutable `ShaderResource` resource set. Expansion preserves the existing mergeable Graphics packet and all CSG interval-image UAV handoffs, while an unprepared/gather-failure route retains the native material-geometry compatibility behavior. The compiler and native Vulkan CSG probes verify the set member transitions from `Common` to `ShaderResource` before the sample callback, alongside its four interval-image `UnorderedAccess` reads. Graph tests passed 87/87, descriptor smoke passed 110 tests with 12 expected topology skips, and ECS graphics passed 18/18. |
| Opaque material sampled-texture resource-set adoption: renderer build, task-graph unit, descriptor-buffer smoke, and textured Vulkan capture | passed; the frozen opaque regular/receiver-surface and CSG interval-sample draw sets now resolve each prepared `SampledImage2D` asset through the material cache, deduplicate its typed texture handle, reuse any preflight typed import, and declare the exact `ShaderResource` members as immutable resource sets. Incomplete preparation leaves the existing compatibility route intact. A real Vulkan probe verifies preflight-import reuse and its `Common` to `ShaderResource` transition; graph tests passed 87/87, descriptor smoke passed 116 tests with 12 expected topology skips, and the texture smoke captured the authored texture with red=14619, green=1225, blue=2355 samples. |
| Transparent material sampled-texture resource-set adoption: renderer build, task-graph unit, descriptor-buffer smoke, and transparent Vulkan captures | passed; the frozen transparent CSG interval producer plus AVBOIT occupancy, extinction, and accumulation plans each resolve their prepared `SampledImage2D` assets through the material cache, deduplicate typed texture handles, reuse preflight imports, and declare exact phase-local immutable `ShaderResource` sets alongside their existing material geometry and CSG image uses. Incomplete preparation retains the native compatibility route. The ordinary transparent capture passed with left/center/right regions 32143/37187/59980, and the transparent-CSG capture passed with regions 34109/48610/59173 and cut/remaining-center 16526/35420 and 9027/29376; graph tests passed 87/87 and descriptor smoke passed 116 tests with 12 expected topology skips. |
| Trace material sampled-texture resource-set adoption: renderer build, ECS graphics contract, task-graph unit, descriptor-buffer smoke, CTest, and textured Vulkan capture | passed; the exact resolved `SampledImage2D` assets selected by the preflighted hardware/software shadow material contexts are deduplicated and frozen with their trace geometry. Shadow Transparent Trace and monolithic Shadow Visibility, Software and Hardware Caustic Photon, and both split/monolithic Surfel GI trace routes now declare the shared immutable `ShaderResource` set, reusing any typed material import already owned by G-buffer or AVBOIT. Explicit-stage materials without a generated surface dispatcher are excluded. A failed frozen hybrid hardware-context restore now rejects and discards the merged packet for fresh preflight, so no post-declaration material upload can expose an untracked texture. Graph tests passed 122/122, ECS graphics passed 19/19, descriptor smoke passed 144 with 16 expected topology skips, focused CTest passed 3/3, and the textured Vulkan capture passed. |
| Prepared material resource-set fail-closed audit: renderer build, ECS graphics contract, task-graph unit, and descriptor-buffer smoke | passed; opaque G-buffer, opaque CSG interval sampling, transparent CSG interval production, and AVBOIT Occupancy, Extinction, and Accumulation now abandon graph construction if either their frozen material-geometry set or bindless sampled-texture set cannot be declared. This prevents a compiled callback from taking a native bridge to dynamic resources absent from its task declaration; the existing direct compatibility renderer remains the fallback for that frame. Graph tests passed 122/122, ECS graphics passed 20/20, and descriptor smoke passed 144 with 16 expected topology skips. |
| Debug command-capability follow-up for textured opaque and transparent recording: renderer build and textured Vulkan captures | passed; opaque and transparent material raster callbacks declare Compute where mesh-shader emulation dispatches before their primary-Graphics raster work, transparent CSG Span/Combine use the same primary-Graphics Compute contract, and its typed clear declares Transfer. The hybrid shadow tail declares its Compute-plus-Transfer fallback work, while Hardware Caustics decay, photon, and geometry/resolve dispatches declare primary-Graphics Compute. Surfel initialization and AVBOIT clear continue to declare their Transfer clears. The textured opaque and both transparent Vulkan captures completed with no command-capability rejection. |
| Transparent CSG interval-producer material-geometry resource-set adoption: renderer build, task-graph unit, descriptor-buffer smoke, and ECS graphics | passed; the frozen transparent receiver-surface snapshot now declares its selected mesh-source buffers as one immutable `ShaderResource` resource set on the AVBOIT interval producer. The prepared route remains in its existing mergeable Graphics packet and preserves the Pre-to-Span-to-Combine interval-image UAV handoffs; unavailable preparation retains the existing native compatibility path. Compiler and real Vulkan producer probes verify the set member reaches `ShaderResource` before the producer callback, alongside its five `UnorderedAccess` interval writes. Graph tests passed 87/87, descriptor smoke passed 110 tests with 12 expected topology skips, and ECS graphics passed 18/18. |
| AVBOIT occupancy material-geometry resource-set adoption: renderer build, task-graph unit, descriptor-buffer smoke, and ECS graphics | passed; the frozen AVBOIT occupancy regular/CSG snapshot now declares its selected mesh-source buffers as one immutable `ShaderResource` resource set. Expansion preserves the existing coverage and interval-image `UnorderedAccess` handoffs plus the mergeable Graphics packet, while incomplete preparation retains the native compatibility behavior. Compiler and real Vulkan probes verify a distinct occupancy set member transitions from `Common` to `ShaderResource` before the occupancy callback, independently of the preceding transparent CSG producer set. Graph tests passed 87/87, descriptor smoke passed 110 tests with 12 expected topology skips, and ECS graphics passed 18/18. |
| AVBOIT extinction material-geometry resource-set adoption: renderer build, task-graph unit, descriptor-buffer smoke, and ECS graphics | passed; the frozen AVBOIT extinction regular/CSG snapshot now declares its selected mesh-source buffers as one immutable `ShaderResource` resource set. The optional depth-warp gap and the four transparent CSG interval-image `UnorderedAccess` handoffs remain graph-owned, while incomplete preparation retains the native compatibility behavior. Compiler and real Vulkan probes verify the set member transitions from `Common` to `ShaderResource` before the extinction callback alongside those four interval aliases. Graph tests passed 87/87, descriptor smoke passed 111 tests with 12 expected topology skips, and ECS graphics passed 18/18. |
| AVBOIT accumulation material-geometry resource-set adoption: renderer build, task-graph unit, descriptor-buffer smoke, and ECS graphics | passed; the frozen AVBOIT accumulation regular/CSG snapshot now declares its selected mesh-source buffers as one immutable `ShaderResource` resource set. The four transparent CSG interval-image `UnorderedAccess` handoffs remain graph-owned across the intervening extinction/integration work, while incomplete preparation retains the native compatibility behavior. The compiler probe preserves the direct producer wait and state seeds through that gap; the real Vulkan probe verifies the set member reaches `ShaderResource` before the accumulation callback alongside the four interval aliases. Graph tests passed 87/87, descriptor smoke passed 112 tests with 12 expected topology skips, and ECS graphics passed 18/18. |
| Software Caustics trace-geometry resource-set adoption: renderer build, task-graph unit, descriptor-buffer smoke, and ECS graphics | passed; the frozen preflighted Software BVH node/position/index/attribute IDs now form one immutable `ShaderResource` resource set for the photon producer. Task creation still expands that set into the same exact resource uses before hazard analysis and barrier planning, while an unavailable set retains the established individual-declaration compatibility route. Focused compiler and real-Vulkan entry-state probes passed; graph tests passed 87/87, descriptor smoke passed 112 tests with 12 expected topology skips, and ECS graphics passed 18/18. |
| Shadow Visibility trace-geometry resource-set adoption: renderer build, task-graph unit, descriptor-buffer smoke, and ECS graphics | passed; the normal monolithic/opaque Shadow Visibility callback and prepared transparent trace now consume the shared frozen software node/position/index/attribute set as `ShaderResource`. Expansion preserves the exact producer waits, state seeds, and single-packet transparent-fold behavior; an unavailable set retains individual declarations while direct/hybrid callback behavior is unchanged. Focused monolithic and transparent compiler/Vulkan probes passed; graph tests passed 87/87, descriptor smoke passed 112 tests with 12 expected topology skips, and ECS graphics passed 18/18. |
| Surfel GI trace-geometry resource-set adoption: renderer build, task-graph unit, descriptor-buffer smoke, and ECS graphics | passed; the selected frozen software or hardware traversal geometry list now forms an immutable `ShaderResource` set for the prepared Surfel Trace callback. The graph-owned trace task expands the selected set into the same exact members before hazard analysis and barrier planning; the monolithic/direct compatibility route and an unavailable set retain individual declarations. Focused compiler and real-Vulkan Surfel entry-state probes passed; graph tests passed 87/87, descriptor smoke passed 112 tests with 12 expected topology skips, and ECS graphics passed 18/18. |
| Hardware Caustics attribute resource-set adoption: renderer build, task-graph unit, descriptor-buffer smoke, and ECS graphics | passed; the frozen selected hardware closest-hit attribute streams now form one immutable `ShaderResource` set on the Hardware Caustic Photon task. Expansion preserves the existing Prefix-to-photon handoff and leaves TLAS, fixed producer inputs, accumulator transitions, and unavailable-set individual-declaration fallback unchanged. Focused compiler and real-Vulkan entry-state probes passed; graph tests passed 87/87, descriptor smoke passed 112 tests with 12 expected topology skips, and ECS graphics passed 18/18. |
| Post-G-Buffer Normalize trace-geometry resource-set adoption: renderer build, task-graph unit, descriptor-buffer smoke, and ECS graphics | passed; the frozen all-route prepared shadow trace-geometry list now forms one immutable `ReadWrite ShaderResource` set on Post-G-Buffer Normalize. Exact expansion preserves the normalizer's outgoing Prefix state seed for every BLAS/SW-BVH member and its packet order, while an unavailable set retains the individual-declaration compatibility route. Focused compiler and real-Vulkan entry-state probes passed; graph tests passed 87/87, descriptor smoke passed 112 tests with 12 expected topology skips, and ECS graphics passed 18/18. |
| Shadow Prepare trace-geometry resource-set adoption: renderer build, task-graph unit, descriptor-buffer smoke, and ECS graphics | passed; Shadow Preparation now freezes its exact `ReadWrite ShaderResource` trace-geometry remainder into one immutable set. Prepared BLAS position/index members that require `AccelStructBuildInput` stay as explicit uses, preserving the verified hybrid tail boundary; unavailable sets retain the individual-declaration route. Focused compiler and real-Vulkan mixed-predecessor probes passed; graph tests passed 87/87, descriptor smoke passed 112 tests with 12 expected topology skips, and ECS graphics passed 18/18. |
| Shadow Prepare software-BVH build-state resource-set adoption: renderer build, task-graph unit, descriptor-buffer smoke, and ECS graphics | passed; Shadow Preparation now freezes its retained parent/sort-key/sort-payload/visit-counter `ReadWrite UnorderedAccess` list into a second immutable set. Exact expansion preserves the native SW-BVH build's UAV close state and leaves trace geometry, BLAS build inputs, and hybrid-tail state contracts separate. Focused compiler and real-Vulkan getter-only probes passed; graph tests passed 87/87, descriptor smoke passed 113 tests with 12 expected topology skips, and ECS graphics passed 18/18. |
| Shadow Prepare hybrid software-tail input resource-set adoption: renderer build, task-graph unit, descriptor-buffer smoke, and ECS graphics | passed; the frozen HW-to-SW continuation position/index pairs now form one immutable `Read ShaderResource` set. Expansion preserves the same accepting Graphics packet and compiler-lowered `AccelStructBuildInput` to `ShaderResource` boundary, while unavailable sets retain the individual-declaration fallback and direct/retry routes remain native. Focused compiler and real-Vulkan probes passed; graph tests passed 87/87, descriptor smoke passed 114 tests with 12 expected topology skips, and ECS graphics passed 18/18. |
| Shadow Prepare acceleration-structure finalizer resource-set adoption: renderer build, task-graph unit, descriptor-buffer smoke, and ECS graphics | passed; the frozen TLAS/BLAS typed resources form one immutable `Read AccelStructRead` set. Expansion preserves the accepting Graphics packet and each compiler-lowered `AccelStructWrite` to `AccelStructRead` handoff; an unavailable set retains the individual-declaration route. Focused compiler and real-Vulkan typed acceleration-structure probes passed; graph tests passed 87/87, descriptor smoke passed 114 tests with 12 expected topology skips, and ECS graphics passed 18/18. |
| Shadow Prepare BLAS geometry build-input resource-set adoption: renderer build, task-graph unit, descriptor-buffer smoke, and ECS graphics | passed; each frozen prepared BLAS position/index pair now forms one immutable `ReadWrite AccelStructBuildInput` set on Shadow Preparation. Expansion preserves the compiler-lowered source-to-build-input boundary, stays in the accepting Graphics packet, and feeds the verified later hybrid-tail SRV set; an unavailable set retains individual declarations. Focused compiler and real-Vulkan position/index plus typed-finalizer probes passed; graph tests passed 87/87, descriptor smoke passed 114 tests with 12 expected topology skips, and ECS graphics passed 18/18. |
| Graph-owned imported initial-owner state snapshots: task-graph unit and descriptor-buffer smoke | passed; typed imported texture/buffer ownership declarations now deep-copy their native release-state handoff into graph-owned arena storage. The original producer handoff can be reset before late packet recording, while repeated typed imports still require the same declaration source identity and malformed invalid sources retain their established record-time diagnostic. The real Vulkan cross-queue smoke verifies the graph snapshot remains valid after its producer handoff is released; its execution is topology-gated when no distinct Compute family is available. |
| Graph-owned task-declared state snapshots: task-graph unit and descriptor-buffer smoke | passed; task declarations deep-copy each external native state source into graph-owned arena storage. The producer-side handoff can be reset immediately after task creation while late recording still filters the immutable snapshot through the task's declared resources. The real Vulkan late-history-tail proof also observes a compiler-owned state seed from its normal producer and records without a recording-time state-binding bridge. Graph tests passed 87/87, descriptor smoke passed 114 tests with 12 expected topology skips, and ECS graphics passed 18/18. |
| Opt-in worker recording for graph-owned task state snapshots: descriptor-buffer smoke | passed; an explicitly opt-in ready frontier may now record a task with declaration-captured immutable state on a worker, using the ordinary per-packet scratch and worker-affined command arena. Command-IR capture remains serial to preserve one deterministic graph-generation artifact; graph declarations and compiler-produced seeds supply packet initial state in both modes. The real Vulkan probe submits two source producers, resets both caller-side handoffs after declaration, then proves both independent worker-recorded tasks receive their copied `CopyDest` state and distinct nonzero worker leases. Graph tests passed 87/87, descriptor smoke passed 114 tests with 12 expected topology skips, and ECS graphics passed 18/18. |
| Semantic task timing bindings: runtime skinning and descriptor-buffer smoke | passed; `GpuTaskGraphSubmitter` now resolves timing tickets from declared semantic task IDs only after compilation, retaining packet IDs as an internal submission detail. Runtime skinning binds its terminal timing scope to its terminal task rather than mirroring compiler packetization. The resolver rejects invalid, duplicate-task, out-of-range, and one-ticket-across-packets bindings before native submission; independent tickets may share one merged packet. The real Vulkan smoke forces separate begin/end Graphics packets, verifies duplicate-task rejection leaves the transaction untouched, then collects a valid timed frame through task-bound submission. Graph tests passed 87/87 and descriptor smoke passed 115 with 12 expected topology skips. |
| Semantic task timing bindings: deferred Lighting, Composite, and Present | passed; the normal deferred terminal chain now anchors each timing ticket to its declared Lighting, Composite, or Present task. Compiler packet IDs remain only for accepted-token lookup, the Lighting acceptance callback, and the exact terminal-packet swap-chain signal; none are mirrored merely to select a timing submit. `nwb_ecs_render` rebuilt; graph tests passed 87/87, descriptor smoke passed 115 with 12 expected topology skips, and ECS graphics passed 18/18. |
| Semantic task timing binding: Surfel GI | passed; the normal Surfel GI scope now binds directly to its semantic task even when its compiler-derived range also contains preparation and snapshot-copy packets. The main packet ID remains only for acceptance-state publication and completion-token lookup; the separate late counter-readback/recovery submission is unchanged. `nwb_ecs_render` rebuilt; graph tests passed 87/87, descriptor smoke passed 115 with 12 expected topology skips, and ECS graphics passed 18/18. |
| Semantic task timing binding: Hardware Caustics | passed; Hardware Caustics now binds its timing ticket to the declared producer task instead of its compiler packet. The packet ID continues to identify its acceptance-state publication and optional lagged-history completion, so existing hardware/software routing and history behavior are unchanged. `nwb_ecs_render` rebuilt; graph tests passed 87/87, descriptor smoke passed 115 with 12 expected topology skips, and ECS graphics passed 18/18. |
| Semantic task timing bindings: Shadow Visibility and Software Caustics | passed; the shared Shadow Effects submission resolves its first ticket from Shadow Visibility and, on the software route, its second from Software Caustics. The branch-dependent ticket count and exact packet-token lookups are unchanged; the later blob-only hybrid closure changes material/scene fallback ownership without changing these timing bindings. Compiler packet IDs no longer select those timing submits. `nwb_ecs_render` rebuilt; graph tests passed 87/87, descriptor smoke passed 115 with 12 expected topology skips, and ECS graphics passed 18/18. |
| Semantic task timing bindings: AVBOIT chain | passed; AVBOIT now binds its Pre, Depth-Warp, Extinction, Integration, and Accumulation timing scopes to the corresponding declared tasks. The non-async route still submits only Pre, while the async route retains all five task anchors and existing packet-token/finalizer handling; no compiler packet ID is used merely to select timing submission. `nwb_ecs_render` rebuilt; graph tests passed 87/87, descriptor smoke passed 115 with 12 expected topology skips, and ECS graphics passed 18/18. |
| Semantic task timing bindings: Shadow Prepare and dynamic Graphics Prefix | passed; Shadow Prepare and every Prefix timing anchor now submit by task ID. The established compiler-derived merge-alias check remains only to coalesce several Prefix callbacks into one shared ticket for one native command list; optional CSG anchors fall back to the G-buffer task, exactly matching the prior packet contract. The dynamic submission array itself contains no packet IDs. `nwb_ecs_render` rebuilt; graph tests passed 87/87, descriptor smoke passed 115 with 12 expected topology skips, and ECS graphics passed 18/18. |
| Semantic task-range routing: shared deferred record/submit spans | passed; `GpuCompiledGraph` now resolves an inclusive compiler-order range from declared task endpoints. The normal deferred renderer uses that path for every normal and late recovery/readback/history record/submit span, so packet splitting and merging no longer require it to assemble range endpoints from packet IDs. Packet identities remain only for exact queue/merge validation and generic packet-local compatibility APIs. `nwb_ecs_render` rebuilt; graph tests passed 87/87, descriptor smoke passed 115 with 12 expected topology skips, and ECS graphics passed 18/18. |
| Semantic task-range runtime routing: shared deferred recorder/submitter | passed; `GpuNativePacketRecorder` and `GpuTaskGraphSubmitter` now resolve record and submit spans directly from declared task endpoints. Every normal and late recovery/readback/history renderer invocation uses those APIs; packet ranges remain only for validation/count guards, while packet identities remain only for exact queue/merge validation and generic packet-local compatibility APIs. The real-Vulkan timing smoke now exercises task-range recording and task-range task-timing submission. `nwb_ecs_render` rebuilt; graph tests passed 87/87, descriptor smoke passed 115 with 12 expected topology skips, and ECS graphics passed 18/18. |
| Semantic task accepted-token routing: shared deferred lifecycle | passed; `GpuGraphSubmissionTransaction::taskToken` validates the current compiled graph and resolves its current packet internally. Every normal and late renderer accepted-token lookup now uses the producing task. The real-Vulkan timing smoke forces separate begin/end packets, proves their task tokens are distinct and match the internal packet tokens, and rejects invalid tasks and an unrelated compiled graph. Packet identity remains only for exact queue/merge validation and generic packet-local compatibility APIs. `nwb_ecs_render` rebuilt; graph tests passed 87/87, descriptor smoke passed 115 with 12 expected topology skips, and ECS graphics passed 18/18. |
| Semantic task acceptance callbacks: shared deferred lifecycle | passed; `GpuTaskGraphSubmitter` now resolves acceptance callbacks from declared tasks and invokes them in compiler task order after the containing packet accepts. Lighting, Surfel GI, Shadow Prepare/Graphics Prefix, and Hardware Caustics no longer pass packet-wide callbacks that filter compiler packet IDs. The real-Vulkan smoke rejects duplicate task bindings before submission, proves each split task receives its accepted token, and verifies two callbacks execute in task order in one merged Graphics packet. The later packet-anchored submission-binding retirement supersedes this historical packet-wide compatibility allowance. `nwb_ecs_render` rebuilt; graph tests passed 87/87, descriptor smoke passed 115 with 12 expected topology skips, and ECS graphics passed 18/18. |
| Semantic task pre-submit hook: deferred presentation | passed; the task-based submitter resolves a one-shot native hook from the declared terminal presentation task and attaches it to that task's exact compiler packet. The renderer still validates the resolved terminal packet's physical queue before claiming the swap-chain binary signal, while the hook itself no longer mirrors packet IDs. The real-Vulkan smoke rejects out-of-range and duplicate task hook bindings before native submission, then proves an in-range task hook resolves to the exact packet by rejecting its conflict with a packet compatibility binding. The later packet-anchored submission-binding retirement supersedes that historical conflict case and the generic packet-hook compatibility API. `nwb_ecs_render` rebuilt; graph tests passed 87/87, descriptor smoke passed 115 with 12 expected topology skips, and ECS graphics passed 18/18. |
| Semantic task final-state snapshots: shared deferred lifecycle | passed; `GpuRecordedGraph` now validates the current compiler output and resolves final native state snapshots from declared task IDs. Shadow Prepare, Graphics Prefix, Shadow Visibility, Software/Hardware Caustics, Surfel GI, Lighting, the late counter readback/history tails, and runtime skinning no longer mirror compiler packet IDs merely to retain recorded final state. The real-Vulkan timing smoke verifies split task lookups resolve their containing packets' snapshots and rejects invalid tasks and an unrelated compiled graph. The later task-only final-state closure below retires the packet compatibility/testing accessor. `nwb_ecs_render` rebuilt; graph tests passed 87/87, descriptor smoke passed 115 with 12 expected topology skips, and ECS graphics passed 18/18. |
| Semantic task topology validation: deferred renderer and recovery | passed; `GpuCompiledGraph` now resolves same-packet, compiler-order, queue-routing, and accepted-frontier checks from declared task IDs, while `GpuGraphSubmissionTransaction` can reject unaccepted work by task. Dynamic Prefix timing coalescing, Shadow/AVBOIT merge guards, skinning’s primary-Graphics check, terminal queue checks, and deferred recovery/readback revocation no longer rebuild packet topology in renderer code. Graph tests passed 87/87, descriptor smoke passed 115 with 12 expected topology skips, and ECS graphics passed 18/18. |
| Semantic task packet-sequence validation: opaque and AVBOIT producer/raster chains | passed; `GpuCompiledGraph` now answers strict same-packet ordering and contiguous task-sequence queries from declared task IDs. The renderer uses those APIs for the opaque shared-output and CSG sample paths plus AVBOIT Occupancy, Extinction, Integration, and Accumulation producer/raster/finalizer sequences, removing every direct `packetTasks()` inspection from its normal frame validation. Unit coverage proves strict order, non-contiguity, reverse order, and cross-packet rejection; graph tests passed 122/122, ECS graphics passed 20/20, and descriptor smoke passed 144 with 16 expected topology skips. |
| Semantic task effect-topology validation: Caustics and Surfel GI | passed; the selected Hardware-or-Software Caustics producer is now represented by one semantic task anchor, and photon/geometry/resolve/clear stages validate their shared packet through task relationships rather than mirrored compiler packet IDs. Surfel GI’s output clear, prepared age/free-through-resolve prefix, initialization lifecycle, snapshot/readback presence, and Hardware Caustics submission gates likewise resolve through task anchors. The renderer no longer mirrors Caustics or Surfel compiler packet handles for normal topology validation; ECS graphics contract, graph tests, and descriptor smoke passed 21/21, 122/122, and 144 with 16 expected topology skips. |
| Semantic task prefix and shadow presence validation | passed; Shadow Preparation, every Graphics Prefix setup/clear/G-buffer task, the Shadow Visibility endpoint, and Software Caustics now verify graph presence through semantic task anchors. Packet identity is no longer mirrored merely to prove record readiness or prefix/shadow submission eligibility; direct task relationships retain all same-packet merge guarantees. ECS graphics contracts passed 22/22, graph tests passed 122/122, and descriptor smoke passed 144 with 16 expected topology skips. |
| Semantic task AVBOIT presence validation | passed; AVBOIT Pre, Depth Warp, Extinction, Integration, and Accumulation now verify normal and async graph readiness through their declared task anchors. The existing one-packet and five-packet range assertions, timing tickets, and exact producer/raster sequence checks remain unchanged, while no AVBOIT compiler packet ID is mirrored solely for presence validation. ECS graphics contracts passed 23/23, graph tests passed 122/122, and descriptor smoke passed 144 with 16 expected topology skips. |
| Semantic task deferred-presence validation and terminal presentation boundary | passed; Lighting, Composite, Present, optional overlay/history, and recovery now use task anchors for record/submit readiness. `RendererSystem` retains exactly one compiler packet lookup: the accepted terminal presentation task’s packet, used solely to attach and verify the swap-chain binary signal. Renderer contract tests prove the single lookup boundary; ECS graphics contracts passed 24/24, graph tests passed 122/122, and descriptor smoke passed 144 with 16 expected topology skips. |
| Graph-owned ImGui late-record snapshot and bindless declaration | passed; the terminal overlay now freezes vertex/index bytes, visible draw offsets and scissors, pipeline/buffer/sampler bindings, and every selected sampled-texture handle before graph declaration. Its task declares the exact frozen texture handles rather than rereading ImGui command arrays during recording, so a later frame cannot introduce an untracked bindless access. An arbitrary callback cannot use that immutable task; it takes the separately named opaque synchronous standalone graph route, whose rejection requests recreation instead of direct rasterization. `nwb_ecs_ui` rebuilt; the ECS graphics contract passed 25/25, graph tests passed 122/122, and descriptor smoke passed 144 with 16 expected topology skips. |
| Dynamic bindless sampled-image declaration audit | passed; the renderer’s supported runtime-selected sampled-image domains are explicitly bounded and contract-tested: material `Texture2D` assets freeze into immutable sets for opaque, CSG, AVBOIT, and every generated ray-trace surface consumer; ImGui textures freeze their exact handles and descriptor slots into terminal draw commands. Material validation rejects any unrecognized resource kind, samplers carry no resource state, and opaque custom ImGui callbacks make their intentionally undiscoverable user-owned accesses explicit through a serial hazard domain rather than claiming an immutable declaration. Static target-generation bindless images remain individually declared by their effect tasks. Current validation: ECS graphics contracts 23/23, graph tests 122/122, descriptor smoke 144 passed with 16 expected topology skips. |
| Graph-owned frozen hybrid hardware-context restore | passed and strengthened; when the optional hybrid SW traversal tail misses, its retained opaque-HW material context is copied into three immutable graph blobs at declaration and recovered from those blobs by the declared tail task. The tail always declares the exact instance-material, instance, and typed-material buffers as final `ShaderResource` writes and validates the blob bytes against the frozen snapshot before recording. A missing capture fails declaration and a stale restore rejects the merged packet; discard handling requests fresh planning, with no direct material retry or consumer-disable escape. Historical Vulkan-validation evidence remains under `.cozter/out/ab-results/hybrid-shadow-boundary/20260817_135800_graph_restore/`. |
| Graph-owned adaptive software-shadow primitive chain: renderer build, task-graph unit, descriptor-buffer smoke, and ECS graphics | passed; every eligible prepared adaptive frame freezes an enabled lifecycle plan during declaration, including cadence frames where neither primitive is due. A snapshot plan declares a typed stats `CopyDest` clear before the monolithic Shadow Visibility callback and a stats `CopySource` to readback `CopyDest` tail, while a compact plan independently declares the typed counter clear before visibility. The full chain shares the selected Compute/Graphics packet, so the callback consumes compiler-lowered UAV states; only acceptance advances the frozen tick and publishes any readback handoff. Rejected and no-producer routes retain no-publication behavior, completed accepted readbacks are polled outside packet recording before the next graph setup, and direct or unprepared callers disable diagnostics rather than issuing native primitives. The compiler proof asserts exact chain order and `CopyDest`-to-UAV-to-`CopySource` barriers under FrontierSafe; the real-Vulkan smoke records two clears and the copy in IR order, observes both UAV inputs in a getter-only callback, verifies the accepted token/final states, and maps the zeroed readback. `nwb_ecs_render` rebuilt; graph tests passed 90/90, descriptor smoke passed 117 with 13 expected topology skips, and ECS graphics passed 18/18. |
| Graph-owned AVBOIT typed-clear primitives: renderer build, task-graph unit, descriptor-buffer smoke, and ECS graphics | passed; normal AVBOIT setup records its original nine values as ordered built-in clears—black low-raster/accumulation textures, zeroed coverage/depth-warp/control/extinction buffers, the overflow sentinel, then white transmittance—rather than a custom native clear thunk. First/last texture hooks preserve the `AvboitClear` timing scope, and runtime validation keeps the complete chain in AVBOIT Pre's accepted Graphics packet before occupancy. The compiler proof asserts all nine tasks' order, one-packet coalescing, and every `CopyDest`-to-`UnorderedAccess` occupancy handoff; the real-Vulkan probe captures the typed clear IR record and observes the unchanged occupancy/tail state chain. The direct helper remains for compatibility callers. `nwb_ecs_render` rebuilt; graph tests passed 91/91, descriptor smoke passed 117 with 13 expected topology skips, and ECS graphics passed 18/18. |
| Graph-owned Surfel persistent-initialization clear primitives: renderer build, task-graph unit, descriptor-buffer smoke, and ECS graphics | passed; first-use normal Surfel GI now chains four typed buffer clears before a resource-free acceptance/discard lifecycle tail, retaining the direct native helper only for compatibility callers. The FrontierSafe compiler proof forces distinct Graphics, Compute, and Transfer queues with the later pool/cell snapshot as a cross-queue RAW consumer, then proves all four clears plus lifecycle remain one Compute packet while the snapshot remains separate. The real-Vulkan smoke captures the four exact clear IR records and values, verifies each `UnorderedAccess`-to-`CopyDest` prologue, maps all cleared buffers, and proves accepted publication plus rejected retry-state restoration. `nwb_ecs_render` rebuilt; graph tests passed 91/91, descriptor smoke passed 117 with 13 expected topology skips, and ECS graphics passed 18/18. |
| Graph-owned CSG interval rectangular clear primitives: renderer build, task-graph unit, descriptor-buffer smoke, and ECS graphics | passed; normal opaque and prepared transparent CSG interval production now records its interval-ID and receiver-event-count work regions as serial typed `UInt` rectangular-clear primitives rather than a custom native clear thunk. First/last hooks preserve the `CsgIntervalClear` timing scope, while runtime checks retain the two clears in the accepting G-buffer or AVBOIT-pre Graphics packet. The direct helper remains compatibility-only. Command IR v2 keeps v1 decoding compatible, captures and generic-replays the rectangle record, and the real-Vulkan probe maps the exact changed 2x2 subregion. `nwb_ecs_render` rebuilt; graph tests passed 92/92, descriptor smoke passed 117 with 13 expected topology skips, and ECS graphics passed 18/18. |
| Graph-owned pure-software SW-BVH sentinel primitives: renderer build, FrontierSafe task-graph unit, descriptor-buffer smoke, and ECS graphics | passed; every frozen pure-software rebuild now records sort-key-invalid, parent-invalid, and shared-counter-zero typed clears immediately before its own native compute callback, while a refit records only its counter-zero clear. The compiler owns each `CopyDest`-to-`UnorderedAccess` handoff and keeps the complete serial chain in Shadow Preparation's existing accepted Graphics packet; a late snapshot mismatch rejects that one packet, preserving the established discard and acceptance-only topology publication. The real-Vulkan smoke captures all four clear records/values in rebuild-then-refit order, verifies final state seeds and the accepted physical completion token. Healthy hybrid retains its conditional native sentinel setup so a failed optional software tail can still accept the valid opaque hardware fallback. `nwb_ecs_render` rebuilt; graph tests passed 93/93, descriptor smoke passed 118 with 13 expected topology skips, and ECS graphics passed 18/18. |
| Graph-owned pure-software SW-BVH callback boundaries: renderer build, FrontierSafe task-graph unit, descriptor-buffer smoke, and ECS graphics | passed; the pure-software per-mesh callback now relies on its exact declared SRV/UAV entry states and Shadow Preparation successor states rather than reissuing native entry/exit state batches. The compiler supplies rebuild `CopyDest`/`Common`-to-UAV transitions, rebuild-to-refit UAV dependencies for node/parent/keys/payload, and final node UAV-to-SRV plus retained scratch UAV handoffs. Internal Morton/sort/topology/Fit synchronization remains native, and direct plus healthy-hybrid compatibility recorders retain their full boundary fences. `nwb_ecs_render` rebuilt; graph tests passed 93/93, descriptor smoke passed 118 with 13 expected topology skips, and ECS graphics passed 18/18. |
| Graph-owned frozen software scene traversal: renderer build, FrontierSafe task-graph unit, descriptor-buffer smoke, and ECS graphics | passed; pure-SW preflight retains its exact distinct-mesh descriptor table with immutable scene-BVH/material uploads, while healthy hybrid preflight now freezes the same traversal for both fresh scene bytes and accepted static-cache reuse. Shadow Preparation verifies those live tables rather than regathering ECS/material data, rebuilding the CPU scene BVH, or growing/repopulating descriptor arrays while the accepted packet records. A hybrid cache hit retains exact scene storage/hash metadata, emits no upload, and a stale snapshot restores the declared HW context. The graph-owned scene/material `ShaderResource` handoff remains in the existing first Graphics packet after the serial per-mesh SW-BVH chain. The expanded source contract fixes the no-regather/no-republication boundary; the FrontierSafe unit and real-Vulkan getter-only smoke retain the upload/state-transition proof. |
| Graph-owned monolithic Shadow Visibility all-lit clear: renderer build, FrontierSafe task-graph unit, descriptor-buffer smoke, and ECS graphics | passed; the retained monolithic route now records an unconditional typed float-white visibility clear immediately before its callback in the same selected Compute/Graphics packet. A producer overwrites that value, while a missing producer keeps the established all-lit fallback; the compiler owns the `CopyDest`-to-`UnorderedAccess` entry handoff and the later Lighting `UnorderedAccess`-to-`ShaderResource` transition. Split-soft and direct compatibility clears remain native. The compiler proof verifies exact clear/callback ordering and one-packet effects routing under FrontierSafe; the real-Vulkan smoke captures the typed clear in IR, observes the getter-only UAV/SRV callback states, validates final state snapshots and accepted packet tokens. `nwb_ecs_render` rebuilt; graph tests passed 94/94, descriptor smoke passed 119 with 13 expected topology skips, and ECS graphics passed 18/18. |
| Graph-owned alias-free opaque compute-emulation handoff: renderer build, task-graph unit, descriptor-buffer smoke, and ECS graphics | passed; regular opaque compute-emulation draws whose frozen generated-vertex outputs are pairwise distinct now record a graph-owned compute producer before G-buffer rasterization. The producer and G-buffer remain in their existing accepting primary-Graphics packet, so the compiler lowers the output `UnorderedAccess`-to-`VertexBuffer` handoff while the original G-buffer timing/range remains the semantic endpoint. The immutable plan retains exact output handles and rejects a late mesh-resource replacement before recording. Shared-output, CSG, transparent, direct, and unprepared material paths retain their established per-item native handoff. The compiler and real-Vulkan probes verify the merged packet, entry-state callbacks, final `VertexBuffer` state, accepted token, and no extra submission. `nwb_ecs_render` rebuilt; graph tests passed 95/95, descriptor smoke passed 120 with 13 expected topology skips, and ECS graphics passed 18/18. |
| Graph-owned alias-free opaque CSG receiver compute-emulation handoff: renderer build, task-graph unit, descriptor-buffer smoke, and ECS graphics | passed; opaque CSG receiver-surface compute draws with pairwise-distinct generated-vertex outputs now record a separate graph-owned producer before G-buffer rasterization. Its frozen plan rejects both receiver self-aliases and every regular opaque compute output that can execute between producer and receiver raster, then revalidates those exact handles at record time. Receiver-event images remain raster-only G-buffer outputs; graph declarations own the CSG clip inputs and the output `UnorderedAccess`-to-`VertexBuffer` handoff in the same accepting primary-Graphics packet. Shared-output, other CSG, transparent, direct, and unprepared material paths retain their established local interleaving. The compiler and real-Vulkan probes verify CSG setup to producer to G-buffer ordering, state callbacks, final `VertexBuffer` state, and accepted packet token without another submission. `nwb_ecs_render` rebuilt; graph tests passed 96/96, descriptor smoke passed 121 with 13 expected topology skips, and ECS graphics passed 18/18. |
| Graph-owned small opaque shared-output compute-emulation handoff: renderer build, FrontierSafe task-graph unit, descriptor-buffer smoke, and ECS graphics | passed; when a non-CSG opaque regular stream contains exactly two or three frozen compute-emulation draws selecting the same retained generated-vertex buffer and heap slot, G-buffer now records its mesh-only prelude followed by the original alternating graph callbacks: `Dispatch(A) → Raster(A) → Dispatch(B) → Raster(B)` and, for a triple, `→ Dispatch(C) → Raster(C)`. The compiler owns `Common → UnorderedAccess → VertexBuffer → ... → VertexBuffer`, while the runtime requires the active four- or six-phase sequence to be contiguous and ordered in G-buffer's same primary-Graphics packet before record or submit. The opaque-regular timer spans the prelude through the terminal raster and is discarded on any rejected/no-op phase. Larger or mixed shared-output groups, opaque CSG, transparent, direct, and unprepared paths retain local interleaving. The compiler and real-Vulkan probes verify exact order, hazards, lowered barriers, final `VertexBuffer` state, timing/acceptance tokens, and one submission. `nwb_ecs_render` rebuilt; graph tests passed 98/98, descriptor smoke passed 123 with 13 expected topology skips, and ECS graphics passed 18/18. |
| Graph-owned exact-four shared-output regular opaque compute-emulation handoff: renderer build, FrontierSafe task-graph unit, descriptor-buffer smoke, and ECS graphics | passed; the same non-CSG opaque regular path now also accepts exactly four frozen compute-emulation draws selecting one generated-vertex buffer and descriptor-heap slot. G-buffer retains its mesh-only prelude, then records the original strict `Dispatch(A) → Raster(A) → Dispatch(B) → Raster(B) → Dispatch(C) → Raster(C) → Dispatch(D) → Raster(D)` order in one primary-Graphics packet. The compiler owns the full `Common → UnorderedAccess → VertexBuffer → UnorderedAccess → VertexBuffer → UnorderedAccess → VertexBuffer → UnorderedAccess → VertexBuffer` chain, and runtime validation permits only contiguous four-, six-, or eight-phase sequences while rejecting inactive phase-ID leakage. The opaque-regular timing interval spans through Raster(D); larger or mixed shared-output groups, opaque CSG, transparent, direct, and unprepared paths retain local interleaving. The compiler and real-Vulkan probes verify all eight callbacks, D’s barriers, final `VertexBuffer` state, timing/acceptance tokens, and one submission. `nwb_ecs_render` rebuilt; graph tests passed 122/122, descriptor smoke passed 144 with 16 expected topology skips, and ECS graphics passed 18/18. |
| Graph-owned alias-free opaque CSG interval-sample compute-emulation handoff: renderer build, FrontierSafe task-graph unit, descriptor-buffer smoke, and ECS graphics | passed; frozen opaque CSG interval-sample compute draws with pairwise-distinct retained generated-vertex buffers and descriptor-heap slots now run a graph-owned producer immediately after Interval Combine, while the existing Sample callback consumes the outputs as `VertexBuffer` raster inputs before interval caps. Combine retains its independent semantic timing/routing freedom; the producer and Sample must be adjacent in one primary-Graphics packet before recording or submitting their shared opaque-CSG timing interval. The immutable plan revalidates the complete CSG stream, output handles, and slots, and a late readiness mismatch safely discards the timing reservation and re-pends the graph. Shared-output CSG, transparent, direct, and unprepared paths retain local interleaving. The compiler and real-Vulkan probes verify Combine-to-producer ordering, `Common → UnorderedAccess → VertexBuffer`, getter-only entry states, the final `VertexBuffer` state, and the shared accepted token. `nwb_ecs_render` rebuilt; graph tests passed 99/99, descriptor smoke passed 124 with 13 expected topology skips, and ECS graphics passed 18/18. |
| Graph-owned alias-free split-AVBOIT Extinction compute-emulation handoff: renderer build, FrontierSafe task-graph unit, descriptor-buffer smoke, and ECS graphics | passed; on the opt-in split AVBOIT route, prepared regular Extinction compute-emulation draws with pairwise-distinct retained generated-vertex buffers and descriptor-heap slots now run in a graph-owned producer after the final immutable Extinction upload. The existing Extinction callback consumes those outputs as `VertexBuffer` inputs in the same primary-Graphics packet, so the compiler lowers the `UnorderedAccess`-to-`VertexBuffer` handoff without changing the route's five-packet topology. The shared Extinction timing interval starts only after producer validation, spans the producer and raster callbacks through the supported shared timing ticket, and is discarded on either rejected callback. CSG compute, shared outputs, direct, and unprepared paths retain local interleaving; the separately verified unsplit regular handoff is recorded below. The compiler proof forces Compute → merged Graphics producer/raster → Compute routing; the real-Vulkan probe verifies the direct handoff, final `VertexBuffer` state, and accepted token, while the dedicated-Compute timing lifecycle probe cleanly topology-skips on this adapter. `nwb_ecs_render` rebuilt; graph tests passed 101/101, descriptor smoke passed 125 with 14 expected topology skips, and ECS graphics passed 18/18. |
| Graph-owned alias-free split-AVBOIT Occupancy compute-emulation handoff: renderer build, FrontierSafe task-graph unit, descriptor-buffer smoke, and ECS graphics | passed; on the opt-in split AVBOIT route, prepared regular Occupancy compute-emulation draws with pairwise-distinct retained generated-vertex buffers and descriptor-heap slots now run after the final immutable Occupancy upload and typed AVBOIT target clears. The existing Occupancy callback consumes those outputs as `VertexBuffer` inputs in the same primary-Graphics AVBOIT-Pre packet, so the compiler lowers the `UnorderedAccess`-to-`VertexBuffer` handoff without changing the route's five-packet topology. The shared Occupancy timing interval starts only after producer validation, spans the producer and raster callbacks through the existing AVBOIT-Pre timing ticket, and is discarded on either rejected callback. CSG compute, shared outputs, direct, and unprepared paths retain local interleaving; the separately verified unsplit regular handoff is recorded below. The compiler proof forces clear/upload → merged Graphics producer/raster → Compute Depth-Warp routing; the real-Vulkan probe verifies the direct handoff, final `VertexBuffer` state, and accepted token, while the dedicated-Compute timing lifecycle probe cleanly topology-skips on this adapter. `nwb_ecs_render` rebuilt; graph tests passed 103/103, descriptor smoke passed 126 with 15 expected topology skips, and ECS graphics passed 18/18. |
| Graph-owned alias-free split-AVBOIT Accumulation compute-emulation handoff: renderer build, FrontierSafe task-graph unit, descriptor-buffer smoke, and ECS graphics | passed; on the opt-in split AVBOIT route, prepared regular Accumulation compute-emulation draws with pairwise-distinct retained generated-vertex buffers and descriptor-heap slots now run after Integration and the final immutable Accumulation upload. The existing Accumulation callback consumes those outputs as `VertexBuffer` inputs in the terminal primary-Graphics packet before the graph-owned attachment/depth finalizer, so the compiler lowers the `UnorderedAccess`-to-`VertexBuffer` handoff without changing the route's five-packet topology. The shared Accumulation timing interval starts only after producer validation, spans the producer and raster callbacks through the shared Accumulation timing ticket, and is discarded on either rejected callback. CSG compute, shared outputs, direct, and unprepared paths retain local interleaving; the separately verified unsplit regular handoff is recorded below. The compiler proof forces Compute Integration → merged Graphics upload/producer/raster/finalizer → Compute Composite routing; the real-Vulkan probe verifies the direct handoff, final `VertexBuffer` plus attachment/depth `ShaderResource` states, accepted tokens, and the shared timing lifecycle, while the dedicated-Compute probe cleanly topology-skips on this adapter. `nwb_ecs_render` rebuilt; graph tests passed 105/105, descriptor smoke passed 127 with 16 expected topology skips, and ECS graphics passed 18/18. |
| Graph-owned alias-free unsplit-AVBOIT Accumulation compute-emulation handoff: renderer build, FrontierSafe task-graph unit, descriptor-buffer smoke, and ECS graphics | passed; on the one-packet Graphics AVBOIT route, prepared regular Accumulation compute-emulation draws with pairwise-distinct retained generated-vertex buffers and descriptor-heap slots now run after the final immutable Accumulation upload and immediately before the existing Accumulation raster/finalizer. The producer, raster, and finalizer remain in AVBOIT Pre's one accepting primary-Graphics packet, so the compiler lowers `UnorderedAccess` to `VertexBuffer` and preserves the final attachment/depth `ShaderResource` handoff without another submission. The shared AVBOIT-Pre timing ticket spans the validated producer/raster interval; a late mismatch rejects the packet before stale vertices can be consumed. CSG compute, shared outputs, direct, and unprepared routes retain local interleaving. The FrontierSafe unit and real-Vulkan smoke prove exact Pre → upload → producer → raster → finalizer order, state lowering, one packet/token, final states, and one timing sample. `nwb_ecs_render` rebuilt; graph tests passed 106/106, descriptor smoke passed 128 with 16 expected topology skips, and ECS graphics passed 18/18. |
| Graph-owned alias-free unsplit-AVBOIT Occupancy compute-emulation handoff: renderer build, FrontierSafe task-graph unit, descriptor-buffer smoke, and ECS graphics | passed; on the one-packet Graphics AVBOIT route, prepared regular Occupancy compute-emulation draws with pairwise-distinct retained generated-vertex buffers and descriptor-heap slots now run after the final immutable Occupancy upload and typed AVBOIT target clears. The producer and Occupancy raster remain in AVBOIT Pre's one accepting primary-Graphics packet, so the compiler lowers `UnorderedAccess` to `VertexBuffer` without another submission. The shared AVBOIT-Pre timing ticket spans the validated producer/raster interval, while the normal unsplit tail remains ordered in the same packet; a late mismatch rejects the packet before stale vertices can be consumed. CSG compute, shared outputs, direct, and unprepared routes retain local interleaving. The FrontierSafe unit and real-Vulkan smoke prove exact Pre → upload → clear → producer → raster order, state lowering, one packet/token, final states, and one timing sample. `nwb_ecs_render` rebuilt; graph tests passed 107/107, descriptor smoke passed 129 with 16 expected topology skips, and ECS graphics passed 18/18. |
| Graph-owned alias-free CSG-only AVBOIT Occupancy compute-emulation handoff: renderer build, FrontierSafe task-graph unit, descriptor-buffer smoke, and ECS graphics | passed; prepared CSG-only Occupancy compute draws with pairwise-distinct frozen generated-vertex buffers and descriptor-heap slots now run after the immutable CSG stream uploads, interval Combine, and typed AVBOIT clears. The producer declares its receiver/cutter and clip/interval inputs plus all four removed-interval StorageImage aliases, then the existing Occupancy callback consumes those exact generated outputs as `VertexBuffer` inputs in the same accepting AVBOIT-Pre Graphics packet. The shared Pre timing scope spans the validated CSG producer and raster, and any late CSG-resource or output-handle mismatch rejects the packet rather than rasterizing stale vertices. Regular+CSG mixtures, shared-output CSG, direct, and unprepared paths retain local interleaving. The compiler and real-Vulkan tests prove interval UAV handoffs, output `Common → UnorderedAccess → VertexBuffer`, exact packet order/token, final states, and one timing sample. `nwb_ecs_render` rebuilt; graph tests passed 109/109, descriptor smoke passed 131 with 16 expected topology skips, and ECS graphics passed 18/18. |
| Graph-owned alias-free CSG-only AVBOIT Extinction compute-emulation handoff: renderer build, FrontierSafe task-graph unit, descriptor-buffer smoke, and ECS graphics | passed; prepared CSG-only Extinction compute draws with pairwise-distinct frozen generated-vertex buffers and descriptor-heap slots now run after their immutable CSG stream uploads and the preceding Depth Warp handoff. The producer declares its receiver/cutter and clip/interval inputs plus all four removed-interval StorageImage aliases, then the existing Extinction callback consumes those exact generated outputs as `VertexBuffer` inputs in the same accepted Graphics packet. The existing Extinction timing scope remains shared by producer and raster: AVBOIT-Pre owns it on the unsplit route and the Extinction packet owns it on the split route. Regular+CSG mixtures, shared-output CSG, direct, and unprepared paths retain local interleaving; a late CSG-resource or output-handle mismatch rejects the packet rather than rasterizing stale vertices. The compiler and real-Vulkan tests prove interval UAV handoffs across the prior phase, output `Common → UnorderedAccess → VertexBuffer`, exact packet order/token, final states, and one timing sample. `nwb_ecs_render` rebuilt; graph tests passed 110/110, descriptor smoke passed 132 with 16 expected topology skips, and ECS graphics passed 18/18. |
| Graph-owned alias-free CSG-only AVBOIT Accumulation compute-emulation handoff: renderer build, FrontierSafe task-graph unit, descriptor-buffer smoke, and ECS graphics | passed; prepared CSG-only Accumulation compute draws with pairwise-distinct frozen generated-vertex buffers and descriptor-heap slots now run after their immutable CSG stream uploads and final Accumulation upload. The producer declares receiver/cutter and clip/interval inputs plus all four removed-interval StorageImage aliases, and the existing Accumulation callback consumes those exact generated outputs as `VertexBuffer` inputs before the retained attachment/depth finalizer. The shared Accumulation timing scope spans the validated producer and raster: AVBOIT-Pre owns it on the unsplit route and the terminal Accumulation packet owns it on the split route. Regular+CSG mixtures, shared-output CSG, direct, and unprepared paths retain local interleaving; a late CSG-resource or output-handle mismatch rejects the packet rather than rasterizing stale vertices. The compiler and real-Vulkan tests prove the exact unsplit Pre → CSG stream → producer → raster → finalizer packet, four interval UAV handoffs, output `Common → UnorderedAccess → VertexBuffer`, attachment/depth `ShaderResource` finalization, a shared timing sample, and one accepted token. `nwb_ecs_render` rebuilt; graph tests passed 111/111, descriptor smoke passed 133 with 16 expected topology skips, and ECS graphics passed 18/18. |
| Graph-owned alias-free unsplit-AVBOIT Extinction compute-emulation handoff: renderer build, FrontierSafe task-graph unit, descriptor-buffer smoke, and ECS graphics | passed; on the one-packet Graphics AVBOIT route, prepared regular Extinction compute-emulation draws with pairwise-distinct retained generated-vertex buffers and descriptor-heap slots now run after the final immutable Extinction upload in a graph-owned producer. The normal route now records a packet-local graph-owned Depth Warp task immediately after Occupancy, preserving the coverage `UnorderedAccess` ordering fence before the immutable stream and leaving the producer directly adjacent to Extinction rasterization. A typed graph Integration tail then preserves the native Depth-Warp → Extinction → Integration sequence while the compiler owns the output `UnorderedAccess`-to-`VertexBuffer` handoff. The shared AVBOIT-Pre ticket measures only the validated producer/raster interval; CSG compute, shared outputs, direct, and unprepared routes retain local interleaving. The FrontierSafe unit and real-Vulkan smoke prove exact Pre → Occupancy → Depth Warp → stream → producer → raster → Integration order, the coverage same-UAV fence, final states, one packet/token, and one timing sample. `nwb_ecs_render` rebuilt; graph tests passed 108/108, descriptor smoke passed 130 with 16 expected topology skips, and ECS graphics passed 18/18. |
| Graph-owned unsplit AVBOIT Integration tail: renderer build, FrontierSafe task-graph unit, descriptor-buffer smoke, and ECS graphics | passed; the normal one-packet Graphics route now records a typed `Extinction → Integration → Accumulation` tail instead of dispatching Integration inside the Extinction callback. Extinction retains the packed Extinction/Overflow UAV writes, while Integration graph-declares their `ShaderResource` reads and the Transmittance UAV write; Accumulation’s immutable stream waits on Integration, so the compiler lowers both state handoffs before record or submission. The runtime requires the exact adjacent Extinction → Integration order in AVBOIT Pre’s accepted Graphics packet and verifies that Accumulation follows it; split AVBOIT retains its existing dedicated-Compute Integration packet and five-packet contract. The Pre timing ticket covers the unsplit tail without a second submission binding. The compiler and real-Vulkan probes verify one packet/token, exact order, packed-output `UnorderedAccess → ShaderResource`, Transmittance `UnorderedAccess → ShaderResource`, and a timing sample. `nwb_ecs_render` rebuilt; graph tests passed 114/114, descriptor smoke passed 136 with 16 expected topology skips, and ECS graphics passed 18/18. |

| Graph-owned exact-two shared-output unsplit-AVBOIT Accumulation compute-emulation handoff: renderer build, FrontierSafe task-graph unit, descriptor-buffer smoke, and ECS graphics | passed; when prepared Accumulation contains no mesh or CSG work and exactly two regular compute draws select the same frozen generated-vertex buffer and descriptor-heap slot, the normal one-packet Graphics route now records `Dispatch(A) → Raster(A) → Dispatch(B) → Raster(B)` after its immutable stream and before the existing attachment/depth finalizer. Each phase is an explicit adjacent graph task, so the compiler lowers `Common → UnorderedAccess → VertexBuffer → UnorderedAccess → VertexBuffer`; the runtime requires all four phases to be contiguous in AVBOIT Pre's accepted primary-Graphics packet before recording or submission. The shared Pre timing interval spans the validated pair stream, and a late output/resource mismatch rejects the packet rather than consuming stale vertices. Other shared-output sizes, mixed streams, CSG, direct, and unprepared routes retain local interleaving. The compiler and real-Vulkan probes verify exact Pre → stream → D(A) → R(A) → D(B) → R(B) → finalizer order, barriers, final attachment/depth `ShaderResource` handoff, one timing sample, and one accepted token. `nwb_ecs_render` rebuilt; graph tests passed 112/112, descriptor smoke passed 134 with 16 expected topology skips, and ECS graphics passed 18/18. |
| Graph-owned exact-three shared-output unsplit-AVBOIT regular Accumulation compute-emulation handoff: renderer build, FrontierSafe task-graph unit, descriptor-buffer smoke, and ECS graphics | passed; the same strictly regular, mesh-free, CSG-free unsplit Accumulation path now also accepts exactly three frozen compute draws selecting one generated-vertex buffer and descriptor-heap slot. It preserves the native order as `Dispatch(A) → Raster(A) → Dispatch(B) → Raster(B) → Dispatch(C) → Raster(C)`, then requires the terminal raster's immediate attachment/depth finalizer in AVBOIT Pre’s one primary-Graphics packet. The compiler owns the complete `Common → UnorderedAccess → VertexBuffer → UnorderedAccess → VertexBuffer → UnorderedAccess → VertexBuffer` chain; runtime validation permits only a contiguous four- or six-phase route, rejects inactive phase-ID leakage, and rejects a detached finalizer. Larger or mixed shared-output groups, CSG, direct, and unprepared routes retain local interleaving. The FrontierSafe compiler and real-Vulkan smoke tests verify all six callbacks, C’s barriers, the terminal attachment/depth `ShaderResource` handoff, one Pre timing ticket/token, and one timing sample. `nwb_ecs_render` rebuilt; graph tests passed 117/117, descriptor smoke passed 139 with 16 expected topology skips, and ECS graphics passed 18/18. |
| Graph-owned exact-four shared-output unsplit-AVBOIT regular Accumulation compute-emulation handoff: renderer build, FrontierSafe task-graph unit, descriptor-buffer smoke, and ECS graphics | passed; the same strictly regular, mesh-free, CSG-free unsplit Accumulation path now also accepts exactly four frozen compute draws selecting one generated-vertex buffer and descriptor-heap slot. It preserves the native order as `Dispatch(A) → Raster(A) → Dispatch(B) → Raster(B) → Dispatch(C) → Raster(C) → Dispatch(D) → Raster(D)`, then requires the terminal raster's immediate attachment/depth finalizer in AVBOIT Pre’s one primary-Graphics packet. The reusable frozen-plan storage supports four entries, and all regular shared-output AVBOIT consumers now opt into the same maximum. The compiler owns the complete `Common → UnorderedAccess → VertexBuffer → UnorderedAccess → VertexBuffer → UnorderedAccess → VertexBuffer → UnorderedAccess → VertexBuffer` chain; runtime validation permits only a contiguous four-, six-, or eight-phase route, rejects inactive phase-ID leakage, and rejects a detached finalizer. Larger or mixed shared-output groups, CSG, direct, and unprepared routes retain local interleaving. The FrontierSafe compiler and real-Vulkan smoke tests verify all eight callbacks, D’s barriers, the terminal attachment/depth `ShaderResource` handoff, one Pre timing ticket/token, and one timing sample. `nwb_ecs_render` rebuilt; graph tests passed 121/121, descriptor smoke passed 143 with 16 expected topology skips, and ECS graphics passed 18/18. |
| Graph-owned exact-two shared-output unsplit-AVBOIT regular Occupancy compute-emulation handoff: renderer build, FrontierSafe task-graph unit, descriptor-buffer smoke, and ECS graphics | passed; when prepared Occupancy contains no mesh or CSG work and exactly two regular compute draws select the same frozen generated-vertex buffer and descriptor-heap slot, the normal one-packet Graphics route now records `Dispatch(A) → Raster(A) → Dispatch(B) → Raster(B)` after its immutable stream and typed AVBOIT clears, and before the retained packet-local Depth Warp. Each phase is an explicit adjacent graph task, so the compiler lowers `Common → UnorderedAccess → VertexBuffer → UnorderedAccess → VertexBuffer`; the runtime requires AVBOIT Pre, stream, clear, and all four phases to be ordered in its accepted primary-Graphics packet before recording or submission. The shared Pre timing interval spans the validated pair stream, and a late output/resource mismatch rejects the packet rather than consuming stale vertices. Other shared-output sizes, mesh or mixed streams, CSG, direct, and unprepared routes retain local interleaving. The compiler and real-Vulkan probes verify exact Pre → stream → clear → D(A) → R(A) → D(B) → R(B) → Depth Warp order, generated-output and coverage barriers, final states, one timing sample, and one accepted token. `nwb_ecs_render` rebuilt; graph tests passed 113/113, descriptor smoke passed 135 with 16 expected topology skips, and ECS graphics passed 18/18. |
| Graph-owned exact-three shared-output unsplit-AVBOIT regular Occupancy compute-emulation handoff: renderer build, FrontierSafe task-graph unit, descriptor-buffer smoke, and ECS graphics | passed; the same strictly regular, mesh-free, CSG-free unsplit Occupancy path now also accepts exactly three frozen compute draws selecting one generated-vertex buffer and descriptor-heap slot. It preserves the native order as `Dispatch(A) → Raster(A) → Dispatch(B) → Raster(B) → Dispatch(C) → Raster(C)`, then hands the terminal raster directly to the retained packet-local Depth Warp in AVBOIT Pre’s one primary-Graphics packet. The compiler owns the complete `Common → UnorderedAccess → VertexBuffer → UnorderedAccess → VertexBuffer → UnorderedAccess → VertexBuffer` chain; runtime validation permits only a contiguous four- or six-phase route and rejects inactive phase-ID leakage. Larger or mixed shared-output groups, CSG, direct, and unprepared routes retain local interleaving. The FrontierSafe compiler and real-Vulkan smoke tests verify all six callbacks, C’s barriers, the Coverage same-UAV handoff into Depth Warp, one Pre timing ticket/token, and one timing sample. `nwb_ecs_render` rebuilt; graph tests passed 118/118, descriptor smoke passed 140 with 16 expected topology skips, and ECS graphics passed 18/18. |
| Graph-owned exact-four shared-output unsplit-AVBOIT regular Occupancy compute-emulation handoff: renderer build, FrontierSafe task-graph unit, descriptor-buffer smoke, and ECS graphics | passed; the same strictly regular, mesh-free, CSG-free unsplit Occupancy path now also accepts exactly four frozen compute draws selecting one generated-vertex buffer and descriptor-heap slot. It preserves the native order as `Dispatch(A) → Raster(A) → Dispatch(B) → Raster(B) → Dispatch(C) → Raster(C) → Dispatch(D) → Raster(D)`, then hands the terminal raster directly to the retained packet-local Depth Warp in AVBOIT Pre’s one primary-Graphics packet. The reusable frozen-plan storage now supports four entries, and all regular shared-output consumers opt into that same explicit maximum. The compiler owns the complete `Common → UnorderedAccess → VertexBuffer → UnorderedAccess → VertexBuffer → UnorderedAccess → VertexBuffer → UnorderedAccess → VertexBuffer` chain; runtime validation permits only a contiguous four-, six-, or eight-phase route and rejects inactive phase-ID leakage. Larger or mixed shared-output groups, CSG, direct, and unprepared routes retain local interleaving. The FrontierSafe compiler and real-Vulkan smoke tests verify all eight callbacks, D’s barriers, the Coverage same-UAV handoff into Depth Warp, one Pre timing ticket/token, and one timing sample. `nwb_ecs_render` rebuilt; graph tests passed 119/119, descriptor smoke passed 141 with 16 expected topology skips, and ECS graphics passed 18/18. |

| Graph-owned exact-two shared-output unsplit-AVBOIT regular Extinction compute-emulation handoff: renderer build, FrontierSafe task-graph unit, descriptor-buffer smoke, and ECS graphics | passed; when prepared Extinction contains no mesh or CSG work and exactly two regular compute draws select the same frozen generated-vertex buffer and descriptor-heap slot, the normal one-packet Graphics route now records `Dispatch(A) → Raster(A) → Dispatch(B) → Raster(B)` after its immutable Extinction stream and before the typed Integration tail. Each phase is an explicit adjacent graph task, so the compiler lowers `Common → UnorderedAccess → VertexBuffer → UnorderedAccess → VertexBuffer`; the runtime requires all four phases and their immediate Integration successor to be contiguous and ordered in AVBOIT Pre's accepted primary-Graphics packet before recording or submission. The shared Pre timing interval spans the validated pair stream; typed Integration then owns the Extinction/Overflow `UnorderedAccess → ShaderResource` handoff before Accumulation. Other shared-output sizes, mesh or mixed streams, CSG, direct, and unprepared routes retain local interleaving. The compiler and real-Vulkan probes verify exact Pre → Occupancy → Depth Warp → stream → D(A) → R(A) → D(B) → R(B) → Integration → Accumulation order, generated-output and typed-tail barriers, final states, one timing sample per Extinction and Integration scope, and one accepted token. `nwb_ecs_render` rebuilt; graph tests passed 115/115, descriptor smoke passed 137 with 16 expected topology skips, and ECS graphics passed 18/18. |
| Graph-owned exact-three shared-output unsplit-AVBOIT regular Extinction compute-emulation handoff: renderer build, FrontierSafe task-graph unit, descriptor-buffer smoke, and ECS graphics | passed; the same strictly regular, mesh-free, CSG-free unsplit Extinction path now also accepts exactly three frozen compute draws selecting one generated-vertex buffer and descriptor-heap slot. It preserves the native order as `Dispatch(A) → Raster(A) → Dispatch(B) → Raster(B) → Dispatch(C) → Raster(C)`, then hands the terminal raster directly to typed Integration in AVBOIT Pre’s one primary-Graphics packet. The compiler owns the complete `Common → UnorderedAccess → VertexBuffer → UnorderedAccess → VertexBuffer → UnorderedAccess → VertexBuffer` chain; runtime validation permits only a contiguous four- or six-phase route and rejects inactive phase-ID leakage. Larger or mixed shared-output groups, CSG, direct, and unprepared routes retain local interleaving. The FrontierSafe compiler and real-Vulkan smoke tests verify all six callbacks, C’s barriers, terminal `Extinction/Overflow` UAV-to-SRV and Transmittance UAV-to-SRV handoffs, one Pre timing ticket/token, and one timing sample per Extinction and Integration scope. `nwb_ecs_render` rebuilt; graph tests passed 116/116, descriptor smoke passed 138 with 16 expected topology skips, and ECS graphics passed 18/18. |
| Graph-owned exact-four shared-output unsplit-AVBOIT regular Extinction compute-emulation handoff: renderer build, FrontierSafe task-graph unit, descriptor-buffer smoke, and ECS graphics | passed; the same strictly regular, mesh-free, CSG-free unsplit Extinction path now also accepts exactly four frozen compute draws selecting one generated-vertex buffer and descriptor-heap slot. It preserves the native order as `Dispatch(A) → Raster(A) → Dispatch(B) → Raster(B) → Dispatch(C) → Raster(C) → Dispatch(D) → Raster(D)`, then hands the terminal raster directly to typed Integration in AVBOIT Pre’s one primary-Graphics packet. The reusable frozen-plan storage supports four entries, and all regular shared-output consumers opt into that same explicit maximum. The compiler owns the complete `Common → UnorderedAccess → VertexBuffer → UnorderedAccess → VertexBuffer → UnorderedAccess → VertexBuffer → UnorderedAccess → VertexBuffer` chain; runtime validation permits only a contiguous four-, six-, or eight-phase route, rejects inactive phase-ID leakage, and requires typed Integration immediately after the terminal raster. Larger or mixed shared-output groups, CSG, direct, and unprepared routes retain local interleaving. The FrontierSafe compiler and real-Vulkan smoke tests verify all eight callbacks, D’s barriers, terminal `Extinction/Overflow` UAV-to-SRV and Transmittance UAV-to-SRV handoffs, one Pre timing ticket/token, and one timing sample per Extinction and Integration scope. `nwb_ecs_render` rebuilt; graph tests passed 120/120, descriptor smoke passed 142 with 16 expected topology skips, and ECS graphics passed 18/18. |

| Graph-owned opaque ImGui callback fallback: UI build, graph/ECS unit, and descriptor-buffer smoke | passed; the ordinary ImGui fallback uses a standalone graph even when an arbitrary `ImDrawCmd` callback prevents the immutable overlay snapshot. The task declares its live UI buffers and NWB-owned sampled textures, keeps the callback itself in an explicit serial opaque hazard domain, and verifies the live `ImDrawData` pointer plus frame generation while the synchronous standalone recorder is active. Texture-status publication remains terminal-acceptance-only. Rejection after an opaque callback requests device recreation because the callback cannot safely be replayed; callback-free work remains pending for a later graph retry. `nwb_ecs_ui` rebuilt; graph tests passed 122/122, ECS graphics passed 26/26, and descriptor-buffer smoke passed 147 with 16 expected topology skips. |
| Independent timing tickets on one merged native packet: runtime, deferred renderer, and descriptor-buffer smoke | passed; one compiler packet now prepares and resolves its graph-owned automatic ticket plus any number of distinct external task-timing tickets against the same exact native token. Same-ticket semantic aliases coalesce, one ticket spanning different packets and repeated direct bindings fail before submission, partial preparation and packet-lease failures roll back every prepared ticket, and native rejection discards every transaction together. Deferred Shadow Prepare/Prefix, software Shadow Visibility/Caustics, and Lighting/Composite no longer force packet separation only to route independent timings; the Surfel snapshot lifecycle boundary remains enforced. Real-Vulkan coverage proves graph-owned plus three external tickets on accepted and rejected one-packet submissions, retryable partial-preparation rollback, query-slot reuse, and one native submission. Debug passed 275 graph, 83 ECS graphics, and 321 descriptor tests (294 passed, 27 capability skips); Optimize passed 275, 83, and 321 (293 passed, 28 skips); Final passed 274, 83, and 288 (258 passed, 30 skips). |
| Native renderer submission-rejection matrix follow-up: descriptor-buffer smoke | passed; a deterministic Graphics fallback graph now rejects the first packet, first Compute-shaped packet, Graphics-overlap packet, Lighting, the deferred Present writer, the later presentation/timing endpoint, History, and Recovery. Every row proves that accepted physical tokens remain unchanged, every unaccepted typed lifecycle discards exactly once, native rejection is distinguished from cleanup-only rejection, split frame timing retires through an accepted recovery or explicit terminal discard, and accepted-frontier/recovery telemetry matches the outcome. A final clean graph reruns the merged built-in upload after all rejection rows. A second topology-gated graph resolved its strict first Compute packet to this host's dedicated Compute queue, rejected it natively, and retired the accepted Graphics prefix through Graphics recovery. Debug and Optimize each passed all 330 descriptor tests; Debug reported 303 passes with 27 capability skips, and Optimize reported 302 passes with 28 skips. Literal ray-tracing build-scratch pool reuse remains a separately audited storage gap and is not claimed by this row. |
| Rejection-aware ray-tracing build-scratch pool follow-up: Vulkan backend and descriptor-buffer smoke | passed; BLAS, TLAS, opacity-micromap, and NV-cluster builds now suballocate GPU-local StorageBuffer/device-address chunks against the exact physical queue, command-buffer owner, and native recording ID. Absolute device addresses are aligned inside padded reservations. Accepted chunks remain timeline-gated, rejected recordings reset their allocations for immediate exact reuse, and completed over-budget pools trim during idle or periodic garbage collection. A fresh-device BLAS probe rejects a native Graphics submission and proves that its retry reuses the same chunk identity and suballocation offset without another allocation. Debug passed 304 of 331 tests with 27 capability skips; Optimize passed 303 of 331 with 28 skips; Final passed 263 of 293 with 30 skips. The OMM and NV-cluster live paths compiled in every configuration but remained capability-skipped on this adapter. |
| Rejection-aware native upload-pool reuse follow-up: Vulkan backend and descriptor-buffer smoke | passed; a fresh-device GPU-local buffer upload rejects its native Graphics submission, then records its retry before any intervening submission can advance the completed timeline. Test-owned Vulkan call capture proves that the retry uses the exact same native source buffer and byte offset without another device-address query. Independent GPU-local readback proves that the rejected destination remains unchanged and that the accepted retry publishes the expected payload. No allocator identity, diagnostic counter, or production friend is required. Debug passed 306 of 333 tests with 27 capability skips; Optimize passed 305 of 333 with 28 skips; Final passed 265 of 295 with 30 skips. |
| Forced physical-queue override Vulkan integration follow-up: descriptor-buffer smoke | passed; the public timing-route override changes one built-in clear task from its deterministic primary Graphics assignment to a same-family auxiliary Graphics queue, and the compiler packet, native recorder, accepted token, and CPU readback all preserve that exact forced identity. No production test hook or test-only function is involved. Debug passed 306 of 333 tests with 27 capability skips; Optimize passed 305 of 333 with 28 skips; Final passed 264 of 294 with 30 skips. |
| Acceptance-owned adaptive shadow diagnostics closeout: renderer build, ECS/graph contracts, and descriptor-buffer smoke | passed; every eligible prepared adaptive frame freezes its lifecycle even when no primitive is due, both clears and the optional copy are graph primitives, and only the accepting Shadow Visibility callback advances the cadence or publishes the exact accepted readback token. Completed readback mapping/logging runs before graph setup rather than during packet recording. The legacy submission confirmer, rollback mirrors, native diagnostic clear/copy path, and unfrozen-plan escape hatch are removed without adding a test-only production API. Debug passed 93 ECS tests, 288 graph tests, and 306 of 333 descriptor tests with 27 capability skips; Optimize passed 93, 288, and 305 of 333 with 28 skips; Final passed 93, 286, and 264 of 294 with 30 skips. |
| Retire the soft-transparent-fold production test control and forced A/B workflow | passed; the prepared soft-transparent split now derives solely from the complete production readiness predicate. The renderer setter, three behavior flags, benchmark-only branch/logs, paired smoke targets, launcher, and registrations are removed; naturally unprepared, degraded, direct, and adaptive routes retain the monolithic compatibility path. Historical target-scene measurements above remain evidence, not a runnable selector workflow. Debug passed 96 ECS contracts and 288 graph tests, plus both focused real-Vulkan descriptor handoff probes, 22 launcher tests, and 3 remaining workflow tests; Optimize passed all 96 ECS contracts; Final rebuilt the renderer and stress smoke target successfully. |
| Retire mutable device-feature test controls and synthetic backend smoke variants | passed; Graphics feature support now delegates directly to the selected Device, and eight duplicate forced-route executables plus the dependent bindless-parity launcher are removed. Transparent-shadow and CSG capture pairs now run their ordinary native executable, assert the route selected by real adapter features, and capability-skip only the unavailable counterpart while treating validation and shader failures as blockers. Debug, Optimize, and Final compiled the graphics contracts, descriptor suite, Testbed, and all affected smoke scenes; Debug passed 97 ECS contracts and 308 of 335 descriptor tests with 27 capability skips, while the focused new contract passed in Optimize and Final. The policy and launcher suites passed. Runtime window capture was attempted for all four route arms, but this session's AMD adapter failed Win32 surface-capability discovery before project startup, so no route-specific runtime claim is made from that attempt. |
| Retire the production host-readback barrier test counter | passed; host-readback synchronization no longer carries a non-Final thread-local counter or reset/query functions in `core/`. The narrow unit contract still inspects the exact generated barriers directly, while the validation-backed placed-resource smoke proves host-visible buffer and staging-texture readback behavior and the descriptor smoke retains command-list marker recovery coverage. All three affected targets compiled in Debug, Optimize, and Final. Debug passed all 289 graph tests, 91 of 94 placed-resource tests with 3 capability skips, and 308 of 335 descriptor tests with 27 capability skips; the focused 9 graph, 3 placed-resource, and 1 descriptor tests passed in Optimize and Final. |
| Retire staging-texture queue-sharing test getters | passed; the Vulkan staging-texture implementation no longer exposes its native sharing mode or admitted queue-family list solely for non-Final assertions. The production admission state remains private and continues to enforce immutable queue-family and queue-class provenance. Validation-backed smoke coverage uses the same staging allocation across distinct Graphics and Transfer families with real upload/readback, accepts an exact same-family sibling queue, and rejects a queue class omitted from the immutable sharing mask. The placed-resource target compiled in Debug, Optimize, and Final; Debug and Optimize each passed 91 of 94 tests with 3 capability skips, and Final passed 82 of 85 with the same 3 capability skips. The source-format policy scan and self-test passed. |
| Remove the obsolete lane-policy RHI compatibility facade | passed; production command-list creation, ownership handoffs, submission, and timing now select `CommandQueue` or an exact `GpuPhysicalQueueId`, while compiled graph packets continue to carry exact physical queue identity. The asynchronous-Compute topology option remains only for real queue discovery and resource-sharing policy. A test-owned source policy rejects reintroduction of the retired facade identifiers without adding a test-only production API. Graphics, descriptor-buffer, and placed-resource targets compiled in Debug, Optimize, and Final. Graph tests passed 289/289, 289/289, and 287/287; descriptor smoke passed 307 of 334 with 27 capability skips, 306 of 334 with 28 skips, and 265 of 295 with 30 skips; placed-resource smoke passed 91 of 94, 91 of 94, and 82 of 85 with 3 capability skips in each configuration. The source policy scan and self-test passed. |
| Retire the manual packet-transaction production test facade | passed; an accepted transaction packet can now be published only by the graph submitter after `Device::executeCommandLists()` returns a valid exact-physical-queue token. The synthetic Recorded state, manual record/accept functions, non-Final diagnostic friend, borrowed packet/frontier inspectors, waiter counter, and non-native acceptance marker are removed from production. Native submission telemetry is consequently unconditional for every accepted packet. Unit tests no longer forge acceptance, while the real-Vulkan suite retains accepted callbacks, rejection, retry, recovery, exact wait-frontier, and per-queue telemetry coverage through public behavior. A test-owned source policy prevents the retired seams from returning. Graphics, graph-test, and descriptor-buffer targets compiled in Debug, Optimize, and Final. Graph tests passed 285/285 in every configuration; descriptor smoke passed 306 of 333 with 27 capability skips, 305 of 333 with 28 skips, and 265 of 295 with 30 skips. The source policy, source-format scan, and their self-tests passed. |
| Retire the Vulkan upload/build-scratch production diagnostic facade | passed; the two diagnostic peers, upload-chunk synthetic identity, six testing counters, and their production friend declarations are removed. Test-owned Vulkan boundary capture retains the exact behavioral proofs: a rejected upload immediately reuses the same native source buffer and offset without a new address query, rejected GPU-local bytes remain unchanged, the accepted retry publishes the expected payload, and rejected build scratch retries with the exact same padded device address and acceleration-structure destination before the resulting BLAS is consumed by a TLAS build. A source policy prevents every retired identifier from returning to production. The descriptor-buffer target compiled in Debug, Optimize, and Final; its full suites passed 306 of 333 with 27 capability skips, 305 of 333 with 28 skips, and 265 of 295 with 30 skips. The source policy, source-format scan, and their self-tests passed. |
| Retire the remaining renderer packet-derived semantic lookups | passed; standalone graph completion now resolves its accepted token through the declared terminal task, renderer record callbacks resolve physical queues through `queueInfoForTask`, and AVBOIT validates inclusive stage-anchor packet order through `taskPrecedesOrSharesPacket`. Generated packet identities remain compiler/runtime submission and telemetry identities, not renderer semantic-routing inputs. Narrow source contracts prevent each local regression. Graphics, ECS renderer, ECS graphics, descriptor-buffer, and graph-test targets compiled in Debug, Optimize, and Final. ECS graphics passed 97/97 and graph tests passed 285/285 in every configuration; the focused standalone smoke passed 3/3 in Debug and Optimize, while Final passed the production case and recorded the two expected configuration skips for unavailable rejection hooks. The source-format scan passed. |
| Retire the grouped Vulkan `DeviceDesc` queue fallback | passed; device creation now requires the synchronously consumed enumerated physical-queue registry and rejects a null or empty registry before allocating a wrapper. The sole creator continues to enumerate primary, dedicated, and same-class queues with their exact capabilities, family/index identity, timestamp width, and class-primary metadata; construction copies those descriptors into device-owned queue state. The active registry-derived compatibility/sharing context remains unchanged. A narrow source policy prevents the eight grouped fields, their assignments, and the synthesized fallback from returning. Graphics, descriptor-buffer, and placed-resource targets compiled in Debug, Optimize, and Final. Descriptor smoke passed 306 of 333 with 27 capability skips, 305 of 333 with 28 skips, and 265 of 295 with 30 skips; placed-resource smoke passed 91 of 94, 91 of 94, and 82 of 85 with 3 capability skips in each configuration. The source policy, source-format scan, and their self-tests passed. |
| Retire packet-anchored timing, acceptance-callback, and submission-hook bindings | passed; `GpuTaskGraphPacketTimingTicket`, `GpuTaskGraphPacketAcceptedCallback`, and `GpuTaskGraphPacketSubmissionHook` are removed together with `GpuTaskGraphNormalExecutionDesc::acceptedCallback`, `submissionHooks`, and `submissionHookCount`, the duplicate `submitPacketRangeInCompileOrderFromTasks`/`submitTaskRangeInCompileOrderFromTasks` tails, and the single-packet `submitPacket` timing-ticket/pre-submit-hook tail. The canonical range APIs accept semantic task bindings directly; the then-existing single-packet wrapper was reduced to completion bindings only and is retired entirely by the later public-facade closure below. The submitter resolves each current native packet internally after compilation while preserving timing-ticket coalescing and rollback, compiler-order callback publication, one-hook-per-native-submission validation, and exact terminal presentation signaling. Descriptor-buffer coverage exercises acceptance, rejection, retry, recovery, publication gating, timing aliases, binding-order-independent callback order, merged-hook rejection, and a successful task-anchored hook with a test-owned native signal, without selecting compiler packet IDs for those bindings or adding a production test seam. The `nwb_no_packet_anchored_submission_bindings` source policy rejects the retired types and overloads, rejects forbidden low-level timing/hook parameters on any reintroduced exact `submitPacket(...)` shape, and checks only top-level retired fields in balanced final/inherited `GpuTaskGraphNormalExecutionDesc` bodies; its self-test protects supported task bindings and unrelated nested/local names from false positives. Graphics, descriptor-buffer, ECS graphics, and graph-test targets compiled in Debug, Optimize, and Final. Descriptor smoke passed 307 of 334 with 27 capability skips, 306 of 334 with 28 skips, and 266 of 296 with 30 skips; ECS graphics and graph tests passed 97/97 and 285/285 in every configuration. The source policy, source-format scan, and their self-tests passed. |
| Retire the Vulkan submission-synchronization production test facade | passed; the five `Device` binary/timeline semaphore lifecycle helpers and their native-handle encoder are removed from production. A shared test-owned Vulkan RAII fixture captures the real device and matching allocation callbacks at the API boundary, creates genuine binary or timeline semaphores, waits before binary destruction, and owns timeline wait arming, host release, queue draining, and idle-safe destruction even on early test exits. The migrated queue-lease, retained-resource identity, command-list provenance, descriptor-timeline, and heap-shutdown tests exercise the same native submission paths without a `core/` or `impl/` test entry point; two formerly non-Final cases now run in Final as well. A production-wide source policy prevents the six retired identifiers from returning in another module. The descriptor-buffer and placed-resource targets compiled in Debug, Optimize, and Final. Descriptor smoke passed 307 of 334 with 27 capability skips, 306 of 334 with 28 skips, and 268 of 297 with 29 skips; placed-resource smoke passed 91 of 94, 91 of 94, and 83 of 86 with 3 capability skips in each configuration. The policy, source-format scan, CTest registrations, and both self-tests passed. |
| Retire recording-time packet state injection | passed; `GpuExternalPacketStateSource`, `GpuTaskPacketStateBinding`, sparse packet record overrides, and their recorder/submitter parameters are removed from production. Packet initial state now comes only from graph-declared immutable sources, initial-owner handoffs, accepted graph state, and compiler-produced packet seeds; recorded and accepted semantic callbacks remain available without carrying mutable state into recording. Descriptor smoke passed 303 of 330 with 27 capability skips, 302 of 330 with 28 skips, and 264 of 293 with 29 skips; graph tests passed 285/285 and ECS graphics passed 97/97 in Debug, while the affected targets compiled in Debug, Optimize, and Final. The recording-time-override policy, source-format scan, CTest registrations, and their self-tests passed. |
| Retire the public single-packet recording facade | passed; `GpuNativePacketRecordDesc`, its caller-controlled worker-domain/index fields, the public `recordPacket` wrapper, and the unused task-ready-frontier forwarding wrapper are removed. The native packet primitive is private and receives worker identity only from the ready-frontier recorder. All 56 smoke callers now enter the supported singleton compile-order range path, preserving generation, preflight, state-seed, command-IR rollback, retry, and provenance coverage without a test-only core hook, friend, or counter. The descriptor-buffer, placed-resource, and command-IR profile targets compiled in Debug, Optimize, and Final. Descriptor smoke passed 303 of 330 with 27 capability skips, 302 of 330 with 28 skips, and 264 of 293 with 29 skips; placed-resource smoke passed 91 of 94, 91 of 94, and 83 of 86 with 3 capability skips in each configuration. The public-single-packet policy, source-format scan, CTest registrations, and their self-tests passed. |
| Retire the public single-packet submission facade | passed; the public `GpuTaskGraphSubmitter::submitPacket` wrapper is removed, while the native submit-and-accept primitive remains private and range-owned. All 39 smoke callers now enter the supported singleton compile-order range path with their exact external completion bindings and no synthetic timing, callback, hook, friend, counter, or test-only core entry point. Device-generation rejection, imported handoff waits, invalid completion retry, graph-owned timing, native rejection, accepted-token publication, and queue-frontier recovery continue through the same private primitive. The descriptor-buffer target compiled in Debug, Optimize, and Final; its full suites passed 303 of 330 with 27 capability skips, 302 of 330 with 28 skips, and 264 of 293 with 29 skips. The dedicated public-single-packet-submission policy, the pre-existing packet-binding policy, source-format scan, CTest registrations, and their self-tests passed. |
| Retire the public packet-selected combined executors | passed; `recordAndSubmitPacketRangeInCompileOrder` and `recordAndSubmitPacketRangeInReadyFrontiers` are removed instead of being retained as unused public or private forwarding layers. Their semantic task-range counterparts now resolve the compiler packet range and directly own the existing recovery-tail preflight, recording, submission, failure reporting, and caller-owned cleanup contract. The retired APIs had no external or test callers, so this closure required no test-only core hook, friend, counter, or packet-ID migration. Graphics task-graph, descriptor-buffer, and ECS graphics targets compiled in Debug, Optimize, and Final. Graph tests and ECS graphics passed 285/285 and 97/97 in every configuration; descriptor smoke passed 303 of 330 with 27 capability skips, 302 of 330 with 28 skips, and 264 of 293 with 29 skips. The packet-binding source policy, source-format scan, CTest registrations, and the policy self-test passed. |
| Retire the public packet final-state query | passed; `GpuRecordedGraph::packetFinalStateSeed` is removed rather than retained as a public or private test helper. The semantic `taskFinalStateSeed` query now owns the unchanged graph, task, packet-publication, and device-generation validation before resolving the declared task's containing packet. All 62 smoke probes are task-anchored: 60 use their existing semantic task, the two obsolete packet/task pointer-alias checks are replaced by a distinct-packet state check, and merged first/last tasks explicitly prove that one packet-close snapshot is shared. No production test hook, friend, counter, or packet-ID query remains. Graphics task-graph, descriptor-buffer, and ECS graphics targets compiled in Debug, Optimize, and Final. Graph tests and ECS graphics passed 285/285 and 97/97 in every configuration; descriptor smoke passed 303 of 330 with 27 capability skips, 302 of 330 with 28 skips, and 264 of 293 with 29 skips. The final-state-query policy, source-format scan, CTest registrations, and their self-tests passed. |
| Privatize the packet-recording lifecycle | passed; the globally nameable `GpuTaskPacketRecordingLease` is replaced by the private nested `GpuTaskGraph::PacketRecordingLease`, graph recording-attempt/claim/complete/abort/readiness controls and `GpuRecordedGraph::resetForRecording` are private, and only the recorder, recorded-graph, transaction, compiler, and submitter production owners receive the required friendship. White-box tests no longer call those controls: a real two-recorder Vulkan smoke now proves one merged-packet claim, rejection safety while recording is active, abort cleanup, retry, and exactly-once merged-payload discard through the public compile-order recorder; the device-lifetime test recreates attempt state through real recorder failure instead of a reset hook. The concurrency case passed 100 consecutive runs without a production test hook, friend, counter, or `core/`/`impl/` test-only entry point. Graphics task-graph, descriptor-buffer, and ECS graphics targets compiled in Debug, Optimize, and Final. Graph tests and ECS graphics passed 280/280 and 97/97 in every configuration; descriptor smoke passed 304 of 331 with 27 capability skips, 303 of 331 with 28 skips, and 265 of 294 with 29 skips. The public-recording-lifecycle policy now guards the nested lease and private recording primitives; its scan/self-test and the source-format scan/self-test passed 4/4. |
| Retire the declaration-only task-discard seam | passed; `GpuTaskGraph::discardTask` is removed instead of being moved into `core/` or `impl/` as a test hook. Graph abandonment now has one canonical path through `reset()` or destruction, which discards every unresolved typed payload exactly once and clears the declaration rather than leaving a terminal zombie task in its dependency graph. The retry setup no longer retains the unreachable pre-plan-discard exception, while packet rejection and record-failure retries retain their compiler-owned lifecycle transactions. Unit coverage uses repeated public reset plus destructor abandonment, and a compile-time API guard plus the production-source policy prevent the retired selective discard from returning. The graphics task-graph target compiled in Debug, Optimize, and Final, and all 280 graph tests passed in every configuration. The manual-task-graph policy scan/self-test and source-format scan/self-test passed 4/4 without a production friend, counter, or test-only entry point. |
| Privatize the packet-submission and rejection lifecycle | passed; the globally nameable `GpuTaskPacketSubmissionLease` is replaced by the private nested `GpuTaskGraph::PacketSubmissionLease`, the graph's begin/complete/abort/discard packet controls are private, and `GpuGraphSubmissionTransaction::rejectPacket` is a private explicit-attempt primitive with its unused generation-inferred overload removed. Public recovery remains semantic through `rejectTask` or whole-transaction `discardUnaccepted`. The command-IR profile reuses a real submission transaction and resolves the exact recorded attempt before releasing its native artifact, rather than selecting a packet through a public graph lifecycle method. Compile-time access guards and the expanded source policy protect the private boundary without a production test hook, friend, counter, or `core/`/`impl/` test-only entry point. Graphics task-graph, descriptor-buffer, command-IR profile, and ECS graphics targets compiled in Debug, Optimize, and Final. Graph tests and ECS graphics passed 280/280 and 97/97 in every configuration; descriptor smoke passed 304 of 331 with 27 capability skips, 303 of 331 with 28 skips, and 265 of 294 with 29 skips; the command-IR profile completed successfully in every configuration. The submission-lifecycle policy scan/self-test and source-format scan/self-test passed 4/4. |
| Privatize graph and transaction lifecycle storage | passed; the globally nameable `GpuTaskLifecycleState`, `GpuPacketRuntimeState`, and `GpuPacketRuntime` are replaced by private `GpuTaskGraph::TaskLifecycleState` and `GpuGraphSubmissionTransaction::{PacketRuntimeState, PacketRuntime}` storage. This implements the redesign's graph/transaction ownership rule while retaining the task stages and packet acceptance/rejection semantics illustrated by the original API sketch: object layout, exact-once callbacks, submission synchronization, rejection/acceptance accounting, and telemetry are unchanged. Compile-time access checks plus recording, submission, and manual-transaction source policies protect both the retired global names and the private nested boundary without a production test hook, friend, counter, or `core/`/`impl/` test-only entry point. Graphics task-graph, descriptor-buffer, placed-resource, command-IR profile, ECS renderer, and mesh-skinning/ECS graphics targets compiled in Debug, Optimize, and Final. Graph tests and ECS graphics passed 280/280 and 97/97 in every configuration; descriptor smoke passed 304 of 331 with 27 capability skips, 303 of 331 with 28 skips, and 265 of 294 with 29 skips; the command-IR profile completed with matching native/replay hashes and zero logger errors in every configuration. The three lifecycle policy scans/self-tests and source-format scan/self-test passed. |
| Retire generation-inferred transaction cancellation | passed; the unused `discardUnaccepted(graph, compiledGraph)` wrapper and the `rejectTask(graph, compiledGraph, task)` wrapper are removed so semantic cancellation always carries an explicit owning recording-attempt generation. The sole inferred renderer call now supplies its deferred-lighting recorded-graph attempt, while every other production and smoke caller already used the explicit contract. Compile-time arity checks prove the inferred overloads remain absent and the explicit-attempt overloads remain available; the submission source policy independently rejects public/protected inferred overloads, inherited exposure, and default-public struct exposure without banning the supported forms. No production test hook, friend, counter, or `core/`/`impl/` test-only entry point was added. Graphics task-graph, descriptor-buffer, placed-resource, command-IR profile, ECS renderer, and mesh-skinning/ECS graphics targets compiled in Debug, Optimize, and Final. Graph tests and ECS graphics passed 280/280 and 97/97 in every configuration; descriptor smoke passed 304 of 331 with 27 capability skips, 303 of 331 with 28 skips after one non-reproducible depth-readback failure passed three isolated retries and the full rerun, and 265 of 294 with 29 skips. Debug placed-resource smoke passed 91 of 94 with 3 capability skips, and the command-IR profile completed with matching native/replay hashes and zero logger errors in every configuration. The focused policy/source-format CTest group passed 8/8. |
| Retire public accepted-frontier wait assembly | passed; `GpuGraphSubmissionTransaction::appendAcceptedQueueFrontierWaitTokens` is private submitter-owned transaction machinery. Its sole external caller was a redundant white-box smoke assertion, now removed; the dedicated-Transfer recovery smoke still accepts the exact Transfer token, rejects the dependent Graphics suffix, executes the semantic accepted-frontier recovery helper, and proves through a test-owned `vkQueueSubmit2` observer that the accepted Transfer submission's timeline semaphore and value become the recovery Graphics submission's exact native wait, with accepted-frontier/recovery telemetry intact. A compile-time access guard and the submission source policy prevent public, protected, default-public, or inherited exposure while retaining the existing submitter friendship. No new production test hook was added; the former Device wait-capture seam used by this and six other native-boundary tests is retired by the later closure below. Graphics task-graph and descriptor-buffer targets compiled in Debug, Optimize, and Final. Graph tests passed 280/280 in every configuration; descriptor smoke passed 304 of 331 with 27 capability skips, 303 of 331 with 28 skips, and 265 of 294 with 29 skips. The dedicated frontier-recovery test passed in Debug and Optimize and is intentionally absent from Final; policy/source-format scans and self-tests passed. |
| Privatize native packet barrier lowering and retained-state seeding | passed; `GpuTaskGraph::applyCompiledBarrier` and `seedTaskRetainedResourceStates` are private recorder-owned primitives. Their four production calls remain inside the already-friended `GpuNativePacketRecorder`; declarations, definitions, execution order, compiler barrier lowering, and retained-state materialization are otherwise unchanged. Compile-time access guards and the recording source policy prevent public, protected, default-public, or inherited exposure without adding a production test hook, friend, counter, or `core/`/`impl/` test-only entry point. Graphics task-graph and descriptor-buffer targets compiled in Debug, Optimize, and Final. Graph tests passed 280/280 in every configuration, and three focused native packet/barrier/state-seed tests passed in every configuration. The recording policy scan/self-test and source-format scan/self-test passed 4/4. |
| Retire the staging-texture mapping test facade and enforce persistent allocation | passed; `StagingTexture` no longer exposes `hasMappedMemoryForTesting`, `isPersistentlyMappedForTesting`, or `rejectNextInvalidateForTesting`, nor carries their non-Final rejection state. Supported Device-created Read/Write staging allocations require host-visible memory, the matching VMA host-access mode, and `VMA_ALLOCATION_CREATE_MAPPED_BIT`; successful creation now also requires a non-null mapped pointer and cleans up otherwise. These allocations are therefore persistently mapped by construction, so the mutable staging persistence flag and transient staging allocator map/unmap paths are removed, VMA's automatic mapping remains owned until destruction, and the public unmap operation is a validated no-op. Real non-coherent invalidation and its error handling remain unchanged; the retired hook substituted an error only after forging an otherwise unreachable transient wrapper state and did not exercise a real VMA invalidation failure. Public tests now prove both upload and readback preserve pointer and row pitch across repeated map, unmap, and remap operations without any production test entry point. Placed-resource targets compiled in Debug, Optimize, and Final; their suites passed 91 of 94 with 3 capability skips in Debug and Optimize and 83 of 86 with 3 skips in Final, while all five staging-texture contract tests passed in every configuration. A production-wide policy prevents the four retired test-only identifiers from returning, and its scan/self-test plus source-format scan/self-test passed 4/4. |
| Retire the ShaderTable dispatch-buffer capture test facade | passed; `ShaderTable::captureDispatchBuffersForTesting` is removed from the Vulkan backend instead of remaining as a non-Final production inspection API. The former exact-refcount test now owns a scoped `vkCmdTraceRaysKHR` observer, copies both native commands and all four strided device-address regions, closes the original command list before mutating every table region, and proves that the replacement ranges are nonzero, shape-compatible, and pairwise disjoint from the still-retained original ranges. After restoring the Volk entry point, it submits only the original closed command list and waits for idle under validation. The no-op ray-generation shader consumes the original ray-generation record; the test intentionally claims native encoding, retention, and non-overlap for miss, hit, and callable regions rather than claiming that this shader dereferences those three records. The semantic case now runs in Final as well. The descriptor-buffer target compiled in Debug, Optimize, and Final; focused ray-dispatch/shader-table coverage passed 17/17, 17/17, and 16/16, while the full suites passed 304 of 331 with 27 capability skips, 303 of 331 with 28 skips, and 266 of 295 with 29 skips. A production-wide exact-name policy prevents the retired capture API from returning; the then-separate allocation/map injectors are retired by the later closure below. Its scan/self-test and the source-format scan/self-test passed. |
| Retire the Device submission-wait capture facade | passed; the four public wait-capture methods, private capture helper, four backing fields, constructor allocation, and per-submit atomic/copy path are removed from the Vulkan backend. A fixed-capacity, cross-thread-safe test-owned `vkQueueSubmit2` observer deep-copies each native submit packet before forwarding it exactly once and records the real driver result afterward. Four cross-queue cases correlate the producer tracking-timeline signal with the consumer wait semaphore, value, all-command stage mask, and device index; two same-queue cases instead prove one planned dependency is elided into zero native waits while invalid preflight/fallback attempts make no driver call; the seam-liveness-only same-queue block is deleted. The external-final-state, graph-owned same-queue, and future-token cases now run in Final. The descriptor-buffer target compiled in Debug, Optimize, and Final; six focused migrated cases passed in Debug and Optimize, while three passed and the rejection-dependent standalone case skipped in Final. Full suites passed 304 of 331 with 27 capability skips, 303 of 331 with 28 skips, and 267 of 296 with 29 skips. A production-wide exact-name policy prevents the retired API and state from returning while leaving the separately audited submission-rejection hook available; its scan/self-test and the source-format scan/self-test passed 4/4. |

| Retire the Device submission-rejection facade | passed; the two public arm/clear methods, private consume helper, per-queue rejection counters, and pre-driver `Queue::submit` branch are removed from the Vulkan backend. A fixed-capacity, cross-thread-safe test-owned `vkQueueSubmit2` observer targets one exact native queue and returns `VK_ERROR_OUT_OF_HOST_MEMORY` without forwarding only the armed call, so every migrated case traverses the production `VkResult` rollback path while the rejected packet remains unseen by the driver. Legitimate host/device out-of-memory rejections now retain an always-on critical warning without contradicting that handled rollback contract in Debug or Optimize; unexpected submit results retain the error break. Thirty-one injected attempts across 22 descriptor tests, three ray-tracing cases, and one placed-resource lease case preserve lifecycle, timing, synchronization, retry, recovery, upload, build-scratch, descriptor-heap, and resource-retention coverage without a production test hook. Rejection-only guards were removed where no other diagnostic seam was required, raising Final descriptor coverage from 296 to 322 tests. Descriptor suites passed 304 of 331 with 27 capability skips, 303 of 331 with 28 skips, and 295 of 322 with 27 skips; placed-resource suites passed 91 of 94, 91 of 94, and 83 of 86 with 3 skips in every configuration. The dedicated-Transfer recovery case passed 100 consecutive Debug repetitions. A production-wide exact-name policy prevents the retired API and state from returning; its scan/self-test and the source-format scan/self-test passed 4/4. |
| Retire the ShaderTable allocation/map failure facade | passed; the two public non-Final rejection methods, two backing booleans, and three pre-operation early-return branches are removed from the Vulkan backend. The switches returned before `Device::createBuffer` or `Device::mapBuffer`, so they never tested a real external failure; the map switch also forged a state outside the supported writable-SBT construction path, whose VMA allocation is persistently mapped. The sole synthetic non-Final smoke is removed rather than replaced with another `core/`/`impl/` hook or a process-global Vulkan interceptor. The always-on typed-ingress smoke now proves wrong-kind and unknown ray-generation attempts preserve the prior native buffer and retry successfully, and that miss, hit, and callable rejections do not consume record indices before successful retry, including Final. Real allocation, readiness, and map error handling remains unchanged. The descriptor target compiled in Debug, Optimize, and Final; the focused replacement passed 1/1 in each configuration, while the full suites passed 303 of 330 with 27 capability skips, 302 of 330 with 28 skips, and 295 of 322 with 27 skips. The expanded production policy protects all retired method and field names; its scan/self-test and the source-format scan/self-test passed 4/4. |

The latest local command-IR evidence is under
`.cozter/out/ab-results/command-ir/20260811_050635/`. It is intentionally local/ignored A/B evidence rather than a
checked-in performance claim. The profile is a CPU probe for `CopyBuffer` only; it does not justify persistent
runtime IR templates or general direct-Vulkan IR replay.

## Version-controlled scope and evidence waivers

### W-ASYNC-001 — frame-lagged async-lighting target-hardware evidence

- **Deferred requirement:** collect a target-scene, dedicated-Compute-only Vulkan A/B for the opt-in one-frame-lagged
  lighting path: accepted lifecycle logs, validation-clean pixel comparison against the current-frame path, and
  GPU timing/critical-path evidence sufficient to make a rollout/default-policy decision.
- **Reason:** the available AMD BC-250 (RADV GFX1013) adapter has no dedicated Compute-only family. The live
  `frame-lagged-async-lighting` runner therefore returns its intentional `77` topology skip after the renderer
  accepts the Graphics fallback; its launcher/parser/lifecycle self-tests pass. This is evidence that the harness
  rejects a non-async route, not evidence that the dedicated-Compute path is performant or pixel-equivalent.
- **Affected behavior and safe fallback:** `RendererSystem::frameLaggedAsyncLightingEnabled` remains disabled by
  default. When the option is requested but no dedicated Compute queue is available, the renderer retains the
  current-frame Graphics route and the harness reports a skip; no Graphics-route result may be presented as async
  evidence.
- **Crash policy:** a process crash, including a pre-topology `SIGSEGV`, is a test failure and must be diagnosed
  before the target-hardware run is retried. It is not a valid skip and does not expand this waiver. The current
  recorded run reaches the intentional topology skip rather than preserving such a crash as evidence.
- **Removal condition:** run `python launcher.py frame-lagged-async-lighting` on a dedicated-Compute-only target
  adapter; retain the lifecycle, validation, pixel-comparison, and profiler/timestamp artifacts under
  `.cozter/out/ab-results/`; then update this audit with the measurements and explicitly decide whether the feature
  remains default-disabled, is promoted behind availability gating, or is retired.

### W-TRANSFER-001 — dedicated-Transfer setup-upload evidence

- **Deferred requirement:** collect the Phase 10 target-hardware A/B for large graph-owned setup uploads: the
  explicit-Graphics baseline and automatic Transfer-preferred arm must run on the same adapter with a distinct
  Transfer-only family, verify the exact producer/readiness topology, and include an external GPU-profiler trace
  covering copy-engine overlap and bandwidth.
- **Reason:** the available AMD BC-250 (RADV GFX1013) adapter exposes no dedicated Transfer-only family. The live
  `python launcher.py transfer-queue` run on 2026-08-18 returned its intentional `77`
  `dedicated_transfer_family_unavailable` skip after the Graphics control completed correctly. Its ignored result
  bundle is `.cozter/out/ab-results/transfer-queue/20260818_020109/`; host completion throughput is explicitly not
  GPU-copy bandwidth evidence.
- **Affected behavior and safe fallback:** public setup uploads retain their existing automatic route and select a
  valid Graphics/Compute transport when a dedicated Transfer-only family is unavailable. The skip does not present
  that fallback as Transfer evidence and does not alter the default routing policy.
- **Removal condition:** run `python launcher.py transfer-queue` (and the texture variant) on a dedicated-Transfer
  target adapter, attach the paired external profiler report with `--external-profiler-report`, retain the route,
  validation, JSON, and trace artifacts under `.cozter/out/ab-results/`, then update this audit with the reviewed
  bandwidth/copy-engine findings and an explicit policy decision.

### W-LEGACY-PARITY-001 — retired legacy-renderer pixel-parity corpus

- **Deferred requirement:** reconstruct a historically matching legacy-renderer corpus and compare it with the
  graph renderer across the approved renderer profiles. A valid parity verdict would require matching scene
  settings, capture mode, temporal inputs, adapter/driver qualification, validation mode, and documented
  tolerances for both arms.
- **Reason and decision:** the legacy renderer path is retired. Rather than revive it or claim parity from
  incomplete historical artifacts, the project has selected the checked-in
  [`current-renderer-v1`](../tests/ab/renderer_baseline/current_renderer_corpus.json) registry as its formal,
  immutable forward reference for the current graph renderer. This is a scope decision, not historical migration
  evidence.
- **Affected behavior and safe fallback:** this waiver changes no renderer behavior. Future graph-renderer changes
  must continue to qualify against `current-renderer-v1` on its documented adapter/driver configuration. Results
  from that corpus must be described only as forward-regression evidence and must never be presented as
  legacy-to-graph pixel parity.
- **Removal condition:** obtain a runnable retired legacy binary or reproducible legacy source plus the complete
  matching scene/capture configuration and qualified artifacts; capture or verify all approved profiles, retain
  the paired images, manifests, logs, comparison reports, and tolerances under `.cozter/out/ab-results/`, then
  replace this waiver with an audit row that reports the reviewed legacy-to-graph comparison. A current-renderer
  re-capture alone cannot remove this waiver.

### W-CYCLE-001 — resource-derived and mixed-cycle expression

- **Deferred requirement:** the resource-derived-cycle and mixed explicit/resource-cycle cases in §78.3, together
  with §27's corresponding same-frame feedback example, require producer/consumer intent that is independent of a
  preselected task order. Only those resource-version cases are deferred; explicit dependency-cycle rejection,
  cross-frame imported-completion handling, and the defensive final topological validation remain in accepted scope.
- **Reason:** `GpuTaskResourceUse` identifies a resource range and its state/access contract, but does not identify a
  produced or consumed resource version. The analyzer first rejects cycles in explicit edges, computes their stable
  topological order, and then orients every inferred RAW/WAR/WAW edge forward through that order. The final
  explicit-plus-inferred graph is therefore acyclic by construction. In particular, §27's two-task example selects
  one deterministic serial order instead of expressing that each read consumes the other task's output. A synthetic
  resource-cycle test would require a private edge-injection seam and would not exercise the public contract.
- **Affected behavior and safe fallback:** same-frame ordering must be stated with explicit dependencies, and feedback
  must use imported prior-frame state/completions. Explicit cycles are rejected before queue assignment and report a
  closed task path plus typed cycle edges; the analyzer also retains its final post-hazard cycle check as a defensive
  invariant. Overlapping RAW/WAR/WAW uses remain correctly serialized in explicit stable order, but this behavior must
  not be presented as resource-derived or mixed-cycle rejection coverage.
- **Removal condition:** add a version-addressable resource API that lets declarations name produced and consumed
  versions for exact ranges; validate version provenance, compatible ranges, and producer uniqueness; derive version
  producer-to-consumer edges independently of declaration or scheduling order; then run cycle detection on the union
  of explicit and version-derived edges. Add the two deferred §78.3 tests with task, edge-type, resource/version, and
  closed-path diagnostics. Imported prior-frame versions/completions must remain external roots rather than same-frame
  edges.

## Open criteria preventing strict final acceptance

These are substantive scope gaps, not failures hidden by the hardware waiver.

1. **End-to-end physical queue identity (partially addressed).** Vulkan now assigns every accepted
   `QueueSubmissionToken` a concrete queue index and device generation. Graph recording, packet acceptance, and
   imported completion bindings require that identity; the renderer and native smoke topologies obtain it from the
   Device; and a token from a retired device is rejected at the native submission boundary. Descriptor-heap
   quarantine now retains and queries that same exact physical token, so an auxiliary same-class queue cannot be
   mistaken for the primary timeline during descriptor retirement. An opt-in registry now exposes every additional
   queue from each active primary Graphics, dedicated Compute, and dedicated Transfer family; explicitly selected
   graph packets route to those exact identities deterministically, and same-family state handoffs use exact
   physical timeline waits without a spurious ownership barrier. A separately enabled alternate Graphics-, dedicated
   Compute-, or dedicated Transfer-capable family may now be registered too; tasks explicitly opt into that
   higher-cost route, and exclusive resource crossings use the same physical-owner release/acquire plan already used
   by cross-class handoffs. Windowed presentation remains deliberately primary-Graphics-only: both the
   renderer and Vulkan presentation hook reject an auxiliary terminal until a future acquired-image/swap-chain
   sharing contract names that transport. Large public setup uploads are the first ordinary-work adopter: their
   isolated producer may deliberately select an auxiliary Graphics, Compute, or Transfer transport, while their
   final graph-owned readiness packet is required to remain on the exact primary physical queue before the
   synchronous API returns. A serial multi-subresource batch retains its first selected auxiliary queue across direct
   dependencies and accepts cross-family use only when its existing resource-sharing contract names that class.
   Graph-owned large ImGui uploads are a third bounded adopter: their persistent Graphics/Compute/Transfer-sharing
   resources permit the exact same selected auxiliary class, but their overlay/standalone completion remains primary
   Graphics. Shadow Visibility, Software Caustics, Surfel GI, and Hardware Caustics are the fourth through seventh
   bounded adopters: each graph-owned effect chain may choose a same-family auxiliary transport in its selected
   queue class, then preserves that physical queue across each direct successor. Shadow Visibility, Software
   Caustics, Surfel GI, and Hardware Caustics additionally opt into the device-wide cross-family route. AVBOIT's
   split Compute-only Depth-Warp and Integration packets also opt in; its Graphics raster/interleaving packets
   retain the existing primary-Graphics policy. Concurrent resources use their declared sharing families, while the
   compiler emits paired ownership transfers for exclusive crossings. Broader dynamic policy and target-scene
   performance evidence remain open.

2. **Complete graph ownership.** Renderer code still controls high-level recording/submission sequencing and holds
   legacy state-handoff data, but the shared deferred renderer now gives the task runtime semantic task endpoints
   for every normal and late recovery/readback/history record/submit span; the runtime alone resolves current
   compiler packet ranges. The accepted-frontier recovery tail plus late counter-readback and history tails now
   use generic runtime record/submit operations that reject unaccepted work on failure; their renderer callers
   retain only payload/final-state validation, timing arming, and device-recreation policy. The shared deferred path now owns per-frame ImGui draw/font/texture uploads and freezes
   its late-record draw commands plus their exact bindless sampled-texture handles. When no renderer-owned graph is
   available, the safe immutable ImGui overlay and requested texture uploads use an isolated graph and publish
   status only from its terminal accepted callback. An arbitrary ImGui callback now uses a separately named,
   serial opaque standalone graph task: it records synchronously against the verified live draw list, declares the
   ordinary UI buffers and NWB-owned textures around an opaque callback hazard domain, and publishes only on terminal
   acceptance. A rejected callback-free frame remains pending for another graph attempt; rejection after an opaque
   callback requests device recreation because replay is unsafe. No ImGui path submits a renderer-owned native
   command list. Frame GPU-timing query-pool reset is now a strict primary-Graphics graph packet whose accepted
   callback alone publishes query availability; failed work remains revoked. Public buffer/texture setup uploads, decoded texture-asset uploads, and the shared deferred
   mesh-view, scene-light, scene-shading, runtime skinning selector/palette/copy/compute updates, and opaque material instance/typed updates
   now use the graph-owned primitive path. Prepared opaque, CSG, and AVBOIT material consumers also inherit their
   shared mesh-view/material state batch and their frozen selected per-mesh source-buffer state batch from the graph,
   while opaque and transparent frozen material plans declare their exact bindless sampled-image assets as immutable
   `ShaderResource` sets.
   Direct/unprepared material callers and generated emulation-vertex intra-draw transitions remain local, except
   for the pairwise-distinct regular opaque, opaque CSG receiver-surface, and opaque CSG interval-sample producer/raster handoffs, plus the
   exact two-, three-, four-, or five-item non-CSG regular opaque shared-output sequences, which are graph-owned in G-buffer's
   existing primary-Graphics packet, and the exact-two-, three-, four-, or five-draw regular Occupancy, Extinction, and Accumulation shared-output sequences, which are
   graph-owned in their unsplit AVBOIT-Pre Graphics packet. Pairwise-distinct regular split-AVBOIT Extinction producer/raster handoffs are likewise graph-owned in their existing
   primary-Graphics Extinction packet, while regular Occupancy, Extinction, and Accumulation plus pairwise-distinct CSG-only Occupancy, Extinction, and Accumulation handoffs are graph-owned in their existing split or unsplit AVBOIT-Pre/terminal Graphics packets. Larger or mixed shared-output groups, shared-output CSG, mixed CSG, and transparent compute-emulation streams outside those pairwise-distinct, exact-two-, three-, four-, or five-draw shared-output regular opaque, Occupancy, Extinction, and Accumulation, and CSG-only Occupancy, Extinction, and Accumulation routes
   remain deliberately local because their persistent generated-vertex buffer may require dispatch/raster
   interleaving.
   The graph-owned skinning dispatch likewise receives its immutable bindless selector, static skin/meshlet inputs,
   and copied rest streams in their declared descriptor-visible states. Active-pose deformation, bounds/repack, and
   final publication are mergeable graph stages: the compiler lowers their generated-stream UAV/SRV handoffs while
   retaining one primary-Graphics packet. Its accepted prior-frame native state is now retained by the reusable
   graph-runtime cache, which owns live-handle retention, pruning, and accepted final-state fan-in before attaching
   the source to consuming graph task declarations rather than selecting a post-compile packet-record override.
   Its terminal timing ticket now binds to the same semantic terminal task and is resolved by the submitter only
   after compilation, so skinning no longer mirrors the compiler packet ID merely to submit timing queries.
   The normal deferred Lighting, Composite, and Present timing scopes, accepted-token lookups, lifecycle
   callbacks, and recorded final-state lookups use the same semantic task path. Dynamic Prefix timing coalescing,
   task merge guards, task queue-class checks, and recovery/revocation now also resolve through semantic task
   topology. Caller-owned timing tickets, acceptance callbacks, and pre-submit hooks resolve only from declared
   task anchors; compiler packet identity remains part of compilation, native submission, and packet telemetry, but
   no longer selects those renderer obligations. The exact presentation signal is derived from the terminal task.
   Normal Surfel GI timing likewise resolves from its semantic task, while its separate late counter-readback and
   recovery submissions retain their established task-anchored timing behavior.
   Hardware Caustics timing also resolves from its semantic producer task while packet identity remains with its
   accepted-state and optional lagged-history completion path.
   The Shadow Visibility and Software Caustics timing scopes now follow their semantic effect tasks. Accepted
   packet-token handling is unchanged; hybrid material/scene fallback ownership is now blob-only and fail-closed.
   AVBOIT timing now follows its semantic Pre-through-Accumulation chain on both its normal and opt-in async
   routes, while packet identity remains for completion and finalizer handling.
   Shadow Prepare and the dynamic Graphics Prefix now submit timing through semantic task anchors; compiler packet
   inspection remains only to preserve their existing one-ticket-per-merged-command-list alias rule.
   Direct compatibility helpers retain their native transitions. The normal deferred effects chain now declares its
   persistent first-use state sources on Shadow Preparation, Shadow Visibility, Software/Hardware Caustics,
   Surfel-GI preparation/copy/final work, AVBOIT-pre, Lighting, and Composite; normal recording no longer builds a
   renderer-owned table keyed by compiled packet IDs or supplies runtime state bindings. Shadow Preparation routes
   a graph-runtime filtered candidate of its dynamic BLAS/SW-BVH state through its task declaration, then commits the
   recorded final-state candidate only after packet acceptance; this preserves the same packet-wide filtering and
   rejection behavior while retiring its renderer-local raw handoff/backing vector and retaining the native record
   fallback itself. The optional lagged-history tail remains
   separately late-recorded after Present acceptance, but its declared irradiance dependency receives a compiler-owned
   state seed from the accepted normal producer and therefore records without a packet override or late binding. Imported texture/buffer/acceleration-structure metadata can now also require
   an external final state: the compiler appends a terminal export after the last overlapping declared use and the
   packet handoff retains that state even when task-local work changed it. This removes a generic final-state bridge.
   Acceleration structures lower their state snapshot and ownership transfer through their retained backing buffer,
   without requiring renderer declarations to duplicate that buffer solely for graph state transport. Imported
   texture/buffer/acceleration-structure metadata now also retains an optional initial exclusive physical owner. Owner-only
   imports require a current, nonconcurrent, exact first-packet match; a different first packet must name the fixed
   external release destination, imported completion, and native state-handoff source. The graph captures an
   immutable owner-state snapshot at declaration, so the producer may release its original handoff before late
   recording. The compiler attaches that completion to the first consumer and rejects a token that does not identify
   the declared source physical queue.
   Graph-to-external routing supports one terminal producer packet for an exclusive typed buffer or acceleration
   structure, and one or more disjoint terminal subresource producer packets for an exclusive texture: the
   descriptor declares its final state and fixed destination, the compiler exports then releases ownership, and the
   accepted transaction publishes semantic producer provenance, a compact physical-queue wait frontier, and a
   merged texture-range state snapshot. The source-compatible one-producer query remains intentionally fail-closed
   for a true multi-producer export. Concurrent sharing remains ineligible; Buffer/AS ranges stay one-packet because
   native tracking is whole-allocation. A later graph now consumes a multi-producer texture handoff through copied,
   non-overlapping `initialOwnerHandoffSources`: each first use must select exactly one declared source, wait on a
   graph-local completion token that is at least as recent as that source's accepted timeline value, then lower the
   paired ownership acquire from the immutable source snapshot.
   Task-declared external state sources likewise capture immutable graph-owned snapshots at declaration, so accepted
   cross-frame producer handoffs may be released before recording. Current-frame late tasks declare their resource
   uses before compilation and receive prior packet state through compiler-produced seeds; recording has no mutable
   state-binding bridge.
   The transparent AVBOIT interval producer, occupancy, extinction, and
   accumulation phases now freeze and publish their own per-write-point material/CSG streams, preserving the shared
   interval sample state across the low-resolution raster passes. Current and lagged deferred bindless selectors,
   the ray-trace material-context selector, normal Shadow Visibility, Software Caustics, Hardware Caustics, and Surfel GI initialization/descriptor-visible entry states plus the prepared-frame age/free-to-cell-head-clear-to-hash-build-to-Spawn-to-trace-build-args-to-trace-to-resolve-to-upsample-to-consumer handoffs, caustic
   emission-target stream, surfel-frame constants, hardware-selected,
   naturally selected software-only, and healthy hybrid shadow material-context batches plus their retained immutable hardware
   fallback context and graph-owned successful restore, software-only and hybrid scene-BVH uploads plus their
   fresh-pair/cache-reuse frozen software traversal tables, software-only per-mesh SW-BVH typed sentinel/callback-boundary setup/build/refit
   and hybrid per-mesh SW-BVH build/refit,
   opaque and healthy hybrid hardware TLAS, and opaque and hybrid hardware BLAS build/refit transactions are acceptance-safe
   graph-owned preparation work. Every eligible prepared adaptive software-shadow frame freezes its diagnostic
   lifecycle; the conditional stats/counter clears and optional stats readback are typed graph primitives in the
   Shadow Visibility packet. Its private CPU mirror advances only when that shared packet accepts, and completed
   readback processing occurs before the next graph setup rather than during recording. Normal AVBOIT target setup likewise records
   its complete nine-value clear sequence as typed graph primitives in the accepted Pre packet, preserving its
   existing timing scope with no native aggregate clear helper. Normal opaque and prepared
   transparent CSG interval production likewise records its interval-ID and receiver-event-count work regions as
   paired typed rectangular-`UInt` graph clears, preserving the original `CsgIntervalClear` timing scope in the
   existing accepted G-buffer or AVBOIT-pre packet. Direct and unprepared CSG callers retain the native helper.
   Hybrid shadow material and scene-BVH payloads now have no renderer-native write escape: fresh bytes arrive only
   through immutable graph blobs, static cache hits emit no upload, and a required upload without a declared batch
   fails the optional software tail. The optional tail restores opaque-HW material state only from its complete declared
   three-blob fallback; a stale restore rejects and discards the merged packet for fresh preflight. A renderer
   contract test fixes `rt_swbvh.cpp` at zero `writeBuffer(` calls and explicitly requires zero adaptive native
   copies/clears and zero ImGui native submits. Native BLAS/TLAS acceleration-structure build work and its narrow
   state bridges remain separately named compatibility boundaries. Accepted static-cache traversal records from its
   frozen preflight identities without regathering CPU comparison data; neither that path nor the AS boundaries can
   overwrite the material or scene-BVH byte streams outside declared graph uploads.

3. **Packet scheduling and recording completion (partially addressed).** Explicit merging remains the renderer
   default. A separate opt-in `FrontierScored` compiler policy can coalesce a cheap directly dependent same-queue
   successor only after confirming that the preceding packet has no cross-queue consumer frontier; existing
   `FrontierSafe` splitting and explicit consumer-frontier overrides remain intact. The compiler-derived native
   ready-frontier recorder is implemented for explicit opt-in packets, with isolated per-packet state scratch and
   worker-affined command-buffer leases: each nonzero ready-frontier worker receives a queue-owned Vulkan command
   pool shard, which grows command buffers as needed and returns each completed buffer only to that same worker
   after the physical queue timeline retires it. Default/direct lease zero retains a private pool because serial
   recording does not select a worker shard. Periodic Device garbage collection now
   performs that completed-timeline sweep without waiting for another command-list acquire, releasing retained
   resources while preserving the worker lease. Graph-owned declaration state snapshots may use worker
   recording only when every packet task explicitly opts in; command-IR capture remains serial to preserve one
   deterministic graph-generation artifact. Graph declarations, initial-owner handoffs, accepted state, and
   compiler-produced seeds now supply every packet's initial state without a recording-time override bridge.

4. **Generic recovery and invalidation proof.** The accepted-token transaction is sound for the exercised paths,
   stale imported completion tokens have graph and native-submission rejection coverage, and recording/transaction
   state is now invalidated and recreated across a compiled-graph generation change. A real headless Graphics
   device-lifetime test releases retired native command lists before teardown but deliberately retains CPU-only plan
   and transaction state. Its replacement recorder, submitter, token validator, and command-arena registry all reject
   the retired generation without task invocation or arena growth, after which a fresh plan records and submits on
   replacement-generation storage. The transaction unit path covers accepted Compute and
   Transfer producers, and a dedicated-Transfer headless test now accepts a graph-owned upload, rejects its later
   Graphics suffix, verifies the exact physical Transfer frontier token, and confirms that the Device receives it
   on an independent late Graphics recovery submission. The qualified-hardware execution remains topology-gated:
   this adapter correctly skips it because it has no dedicated Transfer-only family.

5. **Forward pixel reference and performance evidence.** `current-renderer-v1` is the formal adapter-local,
   source-revisioned eight-profile reference for future graph-renderer changes. Its checked-in registry pins each
   ignored baseline image, manifest, and log; a later candidate must match its profile, frozen environment, capture
   mode, fixed temporal inputs, settle duration, validation mode, and conservative per-profile limits. The project
   has intentionally selected this forward reference instead of reconstructing a retired legacy-renderer corpus, so
   it must never be presented as proof of legacy-to-graph pixel parity. The graph now provides immutable
   exact-resource-set expansion, and opaque G-buffer plus CSG interval sampling and the transparent interval producer
   plus AVBOIT occupancy, extinction, and accumulation phases declare both prepared material geometry and
   sampled-texture assets; Hardware Caustics attributes, Shadow Prepare trace, software-BVH build state, BLAS geometry
   build inputs, hybrid-tail inputs, and acceleration-structure finalization, Post-G-Buffer Normalize, and the
   Software Caustics, Shadow Visibility, and Surfel GI trace-geometry consumers adopt it across
   prepared material and traversal geometry. The shared shadow material-context preflight now also freezes each
   generated surface dispatcher's bindless sampled textures, and Shadow Visibility, Software/Hardware Caustics, and
   Surfel GI declare that exact immutable read set. Every prepared raster-material consumer now fails graph
   construction if it cannot declare both its frozen geometry and sampled-image sets, so it cannot record an
   untracked dynamic bindless access. The supported dynamic sampled-image audit is complete: material `Texture2D`
   assets and NWB-owned ImGui textures are the only runtime-selected image domains, and both freeze their exact
   handles before graph declaration; static target-generation bindless images remain explicit task resource uses.
   The explicitly opaque user-callback task does not claim to discover user-owned descriptor accesses and instead
   serializes that compatibility boundary through its hazard domain. The retired general legacy-to-graph
   pixel-parity corpus is explicitly deferred by [`W-LEGACY-PARITY-001`](#w-legacy-parity-001--retired-legacy-renderer-pixel-parity-corpus);
   `current-renderer-v1` does not replace it.
   The current paired target-scene critical-path comparison covers the natural healthy hybrid route against a test-owned
   transparency-free opaque baseline. The graph-owned soft-transparent-fold versus retained-monolithic comparison is
   historical evidence only and is no longer a runnable selector workflow; neither comparison replaces the deferred legacy-to-graph parity evidence. Dedicated-Transfer ownership/bandwidth evidence and an
   external profiler trace remain deferred because the available adapter lacks that queue family.

## Required decision for a strict closeout

Do not relabel this result as final architectural completion without either implementing the five areas above or
explicitly waiving them in this version-controlled acceptance scope. `W-LEGACY-PARITY-001` records the deliberate
forward-corpus decision; it does not waive the remaining runtime, resource-ownership, or target-hardware evidence
requirements. The next implementation work should finish the remaining graph-owned asset/resource-update paths and
specialized descriptor/resource updates, then collect the dedicated-Transfer evidence still called out above.
