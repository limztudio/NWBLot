// limztudio@gmail.com
////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////


#ifndef NWB_GRAPHICS_CSG_BINDLESS_RESOURCES_SLANGI
#define NWB_GRAPHICS_CSG_BINDLESS_RESOURCES_SLANGI


////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////


#include "runtime.slangi"
#include "../deferred/bindless_resources.slangi"


////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////


// Interval/peel resources are all target-generation StorageImage heap entries.  The CSG context UniformBuffer
// selects the already heap-backed deferred-slot payload; no standalone CSG descriptor set remains.
#ifndef NWB_GRAPHICS_CSG_RUNTIME_SLANGI
#error "CSG bindless resources require the CSG runtime context selector"
#endif

#define g_NwbCsgBindlessResources \
    NwbHeapDeferredBindlessResources(g_NwbCsgClipContextSlots.deferredBindlessResources)


////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////


#endif


////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////

