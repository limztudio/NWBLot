# Caustics — Design + Plan

## Status
Understand+design pass complete (architecture-mapping workflow). User decisions:
- **Groundwork first**: Phase 0 = the IOR/refraction *material model*; the caustic *producer* is deferred until after Phase 0 (decide then).
- **Richer IOR model**: scalar IOR + transmission color + thickness + a *dedicated refractive-caster flag* (separate from `transparent`).

## Architecture findings (the mapping)
- The RT foundation is ~80% of caustics and directly reusable: per-frame HW TLAS/BLAS + SW GPU-LBVH (static **and** skinned, refit per frame); the per-instance `NwbRtInstanceMaterial` table (instanceID == HW `InstanceID()` == SW leaf); per-mesh index/attribute descriptor arrays ([64]); the per-light importance/slot allocator; the deferred per-light combine point. Caustics = "same foundation, **opposite ray direction, additive deposit**".
- **THE BLOCKER (confirmed by code-read):** there is NO IOR / eta / Fresnel / transmission-direction anywhere. Material schema (`metadata.h` `s_AllowedAssetFields`) allows only `transparent`/`two_sided`. The surface contract (`NwbMeshSurface`) exposes only baseColor/normal/param0/param1/transmittance-tint. Every transparent path — AVBOIT extinction `-log(1-alpha)` and the colored-shadow any-hit — **attenuates** light but never **bends** it.
- Reusable bits already present: `Vector3Refract`/`RefractV` (Snell + total-internal-reflection) in `global/math/vector.h:3043` (pure math, currently unwired); `rt_shadow_plan.md` Ch.2 caustics intent (light-side photon/caustic-map, refract via Snell, additive deposit, colored via Beer-Lambert, dispersion optional).

## Phase 0 — IOR/refraction material groundwork (CURRENT)
Goal: refraction as a first-class refraction model end-to-end — the material classifies (a `refractive` boolean), the shader supplies the values — validated by upgrading the EXISTING colored-shadow absorption to Fresnel + Beer-Lambert. No caustic producer yet. HW + SW in lockstep (mandatory).

### VOLUME-CORRECT engine-integrated shadow refraction (2026-06-25, supersedes the per-hit/unified transmittance model)
Shadow refraction is now **engine-integrated over the true volume path**. The per-hit (and the earlier "unified, hook-owns-the-final-transmittance") transmittance model is **RETIRED**: it could not represent volume — it multiplied a hardcoded-thickness per-FACE transmittance once per crossed triangle, so a convex occluder double-darkened at entry+exit and ignored the actual path through the glass.
The shadow visibility through transparent occluders is:
`visibility = (Π over surface crossings of Fresnel_transmitted(ior)) · exp(−Σ over crossings of absorptionCoeff(transmission) · t · faceSign)`
where `faceSign = +1` on an exit face (ray leaving along the normal) and `−1` on an entry face — so the signed-t sum **telescopes** to the true inside path length per occluder (robust for convex, concave, and overlapping volumes). Fresnel is applied **per crossing**; Beer-Lambert is integrated **once** over the real path. The ENGINE owns the integration — it alone sees a ray's entry AND exit faces — and the hook supplies only the optical params.
- **The hook supplies only `ior` (Fresnel) + `transmission` (per-unit Beer-Lambert absorption color).** `NwbMeshSurface = { baseColor, normal, param0, param1, ior, transmission, alpha }` (neutral defaults `ior = 1`, `transmission = float3(1,1,1)` = no attenuation; `thickness` is GONE — the engine measures the real path). The old `transmittance` field is removed. The shadow trace reads `ior`/`transmission`; AVBOIT/G-buffer read `baseColor`/`normal`/`param0`/`param1`/`alpha` and ignore `ior`/`transmission`.
- **The dispatch returns the whole surface:** `NwbMeshSurface nwbShadowDispatchSurface(uint shadingModel, NwbShadowHit hit)` — each per-id wrapper returns `nwbShadowSurfaceModel<id>()`; `default:` returns a neutral surface (`nwbMakeMeshSurface(float3(0), hit.worldNormal, 0, 0)` → ior 1, transmission white). (This reverts the float3-returning `nwbShadowDispatchTransmittance` + the U-revert.)
- **Two engine-side physics helpers on `mesh/surface.slangi`** (Slang built-ins only → byte-identical HW/SW): `float nwbShadowFresnelTransmittedFraction(float ior, float cosI)` (Schlick) and `float3 nwbShadowAbsorptionCoefficient(float3 transmission)` (`-log(clamp(transmission,1e-4,1))`, so `exp(-coeff·len) == transmission^len`). The old `nwbShadowRefractiveTransmittance` is removed.
- **Payload** `NwbShadowOcclusionPayload` carries `float3 transmittance` (the Fresnel product, seeded float3(1)) **and** `float3 opticalDepth` (the signed Beer-Lambert sum, seeded float3(0)); the SW path holds the analog locals. The HW raygen + SW `nwbSwShadowLightVisible` finalize identically: `return transmittance * exp(-opticalDepth)`.
- **HW any-hit + SW traversal are byte-identical** in math: same `surface = nwbShadowDispatchSurface(...)`, same `cosI = saturate(abs(dot(rayDir, worldNormal)))`, same `faceSign`, same helpers, same `transmittance *= Fresnel` + `opticalDepth += absorptionCoeff * t * faceSign`; only the `t` source differs (`RayTCurrent()` HW vs the SW `triangleHit.t`). The SW ray direction (`toLight`) is normalized so the two `t` values are the same world distance. `worldNormal` is the shading normal (correct for the flat-shaded octahedron; a geometric-normal refinement would harden it for smooth meshes).
- `NwbMeshSurfaceInputs.incidentDirection` (+ `inIncidentDirection`) still carries the shadow ray's world dir on the hit; the engine uses it directly for `cosI`/`faceSign`. The rasterizer/AVBOIT still supply a documented dummy where no view buffer exists (harmless — those discard `ior`/`transmission`).
- `asset.refractive` / `RtInstanceMaterialFlag::Refractive` / `NWB_RT_INSTANCE_MATERIAL_FLAG_REFRACTIVE` STAY end-to-end as the material's refractive **classification** plumbed to the GPU instance record — groundwork for the future caustic producer. The shadow trace does **NOT** read `FLAG_REFRACTIVE` (the opaque flag still gates opaque casters; transparent occluders integrate via the optical params).

### Design decisions (CORRECTED 2026-06-24: material decides only the boolean; the shader decides the values)
- **The material decides only the `refractive` boolean; the shader decides the values.** Same principle as the colored-shadow transmittance (the `.surface` hook already decides the per-hit tint). Only `refractive` (a 0/1 classification flag, exactly like `transparent`) is an authored `asset.*` material field: parsed by `ParseMaterialRenderProperties` → `MaterialCookEntry.refractive` → `Material` (rides the existing `materialFlags` word, **no POD/version change**) → `MaterialSurfaceInfo.refractive` → the RT per-instance occluder record. The refraction VALUES — `ior` (scalar), `thickness` (scalar), `transmission` (Beer-Lambert absorption color) — are **shader-side**: fields the `.surface` hook returns via `NwbMeshSurface`, authored as `.bind` constants. No material POD version bump (MTL4 stays MTL4; the `refractive` flag is a new bit2 in the existing flags word, backward-compatible — old data has bit2=0=not refractive).
- **Engine-generic physics, opt-in via `refractive`.** A `refractive` occluder's per-hit transmittance gets `Fresnel(ior, Schlick)` × `BeerLambert(transmission, thickness/cosθ)` applied by the ENGINE in the shadow any-hit + SW traversal, reading the ior/thickness/transmission the hit's surface hook returned. Non-refractive materials keep current flat-tint behavior → **no visual change to existing demos**. Rationale: Fresnel/Snell is universal physics; the surface supplies the values, the engine owns the physics (mirrors how the engine owns the lighting loop and the material provides the BXDF). Reuses `Vector3Refract`/Schlick.
- **Surface-contract IOR exposure is IN Phase 0 (U0-1).** `NwbMeshSurface` carries `ior`/`thickness`/`transmission` (neutral defaults: 1 / 0 / float3(1,1,1)), set by `nwbMakeMeshSurface` overloads so existing hooks/overloads + the G-buffer packing are unaffected. NOTHING consumes them yet (the shadow-dispatch return + the physics are U0-2/U0-3); this unit only makes the contract able to carry them.

### Units
- **U0-1 Material flag + surface contract**: add ONLY `refractive` to `s_AllowedAssetFields` (`metadata.h`) + `ParseMaterialRenderProperties` (`cook.cpp`) + `MaterialCookEntry.refractive`; thread through `BuildMaterialAsset`; ride the existing `materialFlags` word via `s_MaterialFlagRefractive` (bit2) — **no `s_MaterialMagic` bump (MTL4 unchanged), no new POD fields**; `Material::refractive()` accessor (`asset.h`); `MaterialSurfaceInfo.refractive` (`renderer_pipeline_types.h`) + copy in `material_surface.cpp`. ADD `ior`/`thickness`/`transmission` to `NwbMeshSurface` (`mesh/surface.slangi`) with neutral defaults in every `nwbMakeMeshSurface` overload (nothing consumes them yet).
- **U0-2 RT instance record + surface-dispatch return**: add `ior`/`thickness`/`transmission` (the values now flow from each hit's surface hook, NOT a material POD) + `NWB_RT_INSTANCE_MATERIAL_FLAG_REFRACTIVE` (flags bit) to `NwbRtInstanceMaterialGpu` (`raytracing_system.cpp`) + `NwbRtInstanceMaterial` (`instance_material.slangi`); set the flag in `ResolveInstanceShadowMaterial`; have the shadow-transmittance dispatch return the surface hook's ior/thickness/transmission. Record may grow → re-pack to aligned Float4 lanes; keep HW/SW mirror byte-exact.
- **U0-3 Refraction physics (HW + SW)**: in `shadow_ahit.slang` + `shadow_sw_traversal_cs.slang`, for `FLAG_REFRACTIVE` occluders modulate the per-hit transmittance by `Fresnel(ior; rayDir·worldNormal, Schlick)` × `BeerLambert(transmission ^ (thickness / cosθ))` using the surface-hook-supplied values. Identical math both backends (the byte-equality mandate). Reuses the existing per-hit `rayDirection`/`worldNormal` already on `NwbShadowHit`.
- **U0-4 Validate**: mark a transparent smoke material `refractive` and have its `.surface` hook return ior ~1.5, a thickness, a transmission tint; capture A/B (refractive on vs off) → Fresnel grazing (more opaque at grazing angles) + thickness/transmission Beer-Lambert tint visible in the *cast shadow*. Keep `nwb_assets_graphics_tests` green; engine preset builds clean.
- *(Deferred within/after Phase 0: AVBOIT-side refractive appearance using the same surface values.)*

## Deferred — the caustic PRODUCER (choose after Phase 0)
Avoid the rasterized light-view caustic map (a 3rd rendering path that fights the engine's one-abstraction/two-backend discipline). Two viable:
- **Approach 1 — photoreal (light-side photon splat):** true focused crescents; needs a forward light-trace, atomic splat into a black-cleared additive RGBA16F caustic buffer, a denoise/resolve, a NEW HW pipeline (closest-hit + recursion>1 + larger payload), and a SW compute photon kernel; **relaxes** the HW/SW byte-equality bar.
- **Approach 2 — stylized (eye-side refractive gather):** clones the shadow pass's screen-space dispatch + per-slot Texture2DArray; cheap; byte-identical HW/SW; physically *fake* (refractive brightening, not focusing).

Decisions to make then: parity bar (gates Approach 1 vs 2); per-light caustic budget (directional/spot only, 1-4 slots; point lights = omnidirectional = expensive); dispersion (skip v1); additive deposit point (`composite_ps.slang` vs `scene/lighting.slangi`); atomic-splat target (float-image atomics vs R32-packed + resolve pass); the HW per-mesh **position** SRV-array gap (HW shadows omit positions because the BLAS owns them; a refraction closest-hit / receiver-intersection needs explicit positions — the SW path already binds them); non-uniform-instance-scale normal correctness (currently a documented latent divergence in the shadow normal transform — becomes real for refraction).

## Conventions (standard.md — re-read before each edit)
CRLF + UTF-8 + banner + exact-EOF for all `.cpp/.h/.slang/.slangi/.bind/.nwb`; aligned `Float4` lane packs for GPU/runtime vec4 data; Slang-only shaders; HW+SW lockstep mandatory; per-material authored model (no hardcoded engine defaults); "never approach in a hacky way — don't dread full-redesign".
