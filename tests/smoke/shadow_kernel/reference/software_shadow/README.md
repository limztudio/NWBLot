# Software shadow predecessor

The two `shadow/` includes are byte-identical copies from commit `12f6be6c9`, before streaming instance accumulation. They preserve the three-occluder bin limit and duplicate completed-chord finalization for independent native comparison. The test loader selects this root before the production graphics root only for the reference pipeline; unchanged bindings/intersection/material helpers still come from production.

The reference must remain frozen when the production accumulator changes. Tests compare up to three instances within one final half ULP and compare the candidate with independent analytic box transmission for all cases. Four-or-more-instance cases intentionally differ from this capped predecessor.
