// limztudio@gmail.com
////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////


#ifndef NWB_ASSETS_CSG_SPHERE_CAP_PROXY_HELPERS_SLANGI
#define NWB_ASSETS_CSG_SPHERE_CAP_PROXY_HELPERS_SLANGI


////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////


#include "../../graphics/csg/cap_proxy/round_common.slangi"


////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////


uint nwbCsgCapProxySphereRowCount(const uint segments){
    return min(NWB_CSG_CAP_PROXY_MAX_ROWS, max(5u, segments / 2u));
}

uint nwbCsgCapProxySphereSegmentCountForBudget(const uint requestedSegments){
    uint segments = min(requestedSegments, NWB_CSG_CAP_PROXY_MAX_SEGMENTS);
    for(uint attempt = 0u; attempt < NWB_CSG_CAP_PROXY_MAX_SEGMENTS; ++attempt){
        if(nwbCsgCapProxyGridBudgetFits(segments, nwbCsgCapProxySphereRowCount(segments)))
            return segments;
        if(segments <= 3u)
            return segments;
        --segments;
    }
    return 3u;
}

float nwbCsgCapProxySphereRadiusSlack(const float radiusShape, const uint segments, const uint rows){
    const float segmentHalfAngle = NWB_CSG_CAP_PROXY_PI / max(float(segments), 3.0);
    const float rowHalfAngle = 0.5 * NWB_CSG_CAP_PROXY_PI / max(float(rows), 1.0);
    return nwbCsgCapProxyRoundRadiusSlack(radiusShape, max(segmentHalfAngle, rowHalfAngle));
}

#endif


////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////
