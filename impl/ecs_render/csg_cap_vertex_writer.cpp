// limztudio@gmail.com
////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////


#include "csg_cap_private.h"


////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////


NWB_IMPL_BEGIN


////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////


namespace ECSRenderCsgCapDetail{


////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////


namespace __hidden_csg_cap_vertex_writer{


////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////


[[nodiscard]] static bool AppendCapVertex(
    CsgCapVertexGpuDataVector& vertices,
    const CapPoint& point,
    const CapCutterEval& cutterEval,
    const u32 receiverIndex,
    const u32 cutterIndex
){
    if(vertices.size() >= static_cast<usize>(Limit<u32>::s_Max))
        return false;

    const SIMDVector position = LoadFloat(point.position);
    const SIMDVector sourceNormal = LoadFloat(point.normal);
    SIMDVector capNormal;
    if(!EvaluateWorldCapNormal(cutterEval, position, sourceNormal, capNormal))
        return false;

    CsgCapVertexGpuData vertex;
    StoreFloat(VectorSetW(position, static_cast<f32>(receiverIndex)), &vertex.positionReceiverIndex);
    StoreFloat(VectorSetW(capNormal, static_cast<f32>(cutterIndex)), &vertex.normalCutterIndex);
    StoreFloat(VectorSetW(LoadFloat(point.tangent), static_cast<f32>(cutterIndex)), &vertex.tangent);
    vertex.color = point.color;
    vertex.uv0 = point.uv0;
    vertices.push_back(vertex);
    return true;
}


////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////


};


////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////


bool AppendCapTriangle(
    CsgCapVertexGpuDataVector& vertices,
    const CapPointVector& points,
    const CapCutterEval& cutterEval,
    const u32 a,
    const u32 b,
    const u32 c,
    const u32 receiverIndex,
    const u32 cutterIndex
){
    return
        __hidden_csg_cap_vertex_writer::AppendCapVertex(vertices, points[a], cutterEval, receiverIndex, cutterIndex)
        && __hidden_csg_cap_vertex_writer::AppendCapVertex(vertices, points[b], cutterEval, receiverIndex, cutterIndex)
        && __hidden_csg_cap_vertex_writer::AppendCapVertex(vertices, points[c], cutterEval, receiverIndex, cutterIndex)
    ;
}


////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////


};


////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////


NWB_IMPL_END


////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////

