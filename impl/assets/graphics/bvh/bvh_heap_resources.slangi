// limztudio@gmail.com
////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////


// Writable typed views over the global StorageBuffer descriptor heap used exclusively by the SW-BVH build passes.
// A StorageBuffer descriptor has no baked structured stride or access qualifier: these uint and node views alias the
// same heap binding as RWByteAddressBuffer in bindless_heap.slangi, while keeping the build kernels readable. Slots
// are dispatch-uniform push constants, so unlike per-instance traversal no NonUniformResourceIndex decoration is
// needed here.


#ifndef NWB_GRAPHICS_BVH_HEAP_RESOURCES_SLANGI
#define NWB_GRAPHICS_BVH_HEAP_RESOURCES_SLANGI


////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////


#include "bvh_common.slangi"
#include "../bindless/bindless_heap.slangi"


////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////


#pragma warning(push)
#pragma warning(disable: 39001) // Deliberate writable typed aliases of the global StorageBuffer descriptor table.
[[vk::binding(NWB_BINDLESS_HEAP_BINDING_STORAGE_BUFFER, NWB_BINDLESS_HEAP_RESOURCE_SET)]]
RWStructuredBuffer<uint, Std430DataLayout> g_NwbHeapBvhWritableUintBuffers[];
[[vk::binding(NWB_BINDLESS_HEAP_BINDING_STORAGE_BUFFER, NWB_BINDLESS_HEAP_RESOURCE_SET)]]
globallycoherent RWStructuredBuffer<NwbBvhNode, Std430DataLayout> g_NwbHeapBvhWritableNodeBuffers[];
#pragma warning(pop)


////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////


#endif


////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////

